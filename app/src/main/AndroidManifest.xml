<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Music Recognizer widget runs its recognition capture as a microphone foreground service (API 34+). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <!-- Lets the user exempt the app from battery optimization so aggressive OEM skins (MIUI/HyperOS,
         etc.) stop freezing/killing the playback service and Android Auto can always bind to it. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <!-- Legacy (API ≤28): first MP3 export may request this before the folder picker. SAF covers API 29+. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <!--    bluetooth connect-->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- For Android 11 (API 30) and below -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <!-- Optional: If you also need to scan for Bluetooth devices -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Microphone feature for music recognition (not required) -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

<!--    install permission-->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Android TV -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <queries>
        <intent>
            <action android:name="android.media.action.DISPLAY_AUDIO_EFFECT_CONTROL_PANEL" />
        </intent>
        <!-- Resolve the default / available mail apps for Report & suggest (no generic share sheet). -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>
    <application
        android:name="iad1tya.echo.music.App"
        android:allowBackup="true"
        android:appCategory="audio"
        android:banner="@mipmap/tv_banner"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:extractNativeLibs="true"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.echomusic"
        tools:targetApi="tiramisu">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".ui.screens.CrashActivity"
            android:exported="false"
            android:theme="@style/Theme.echomusic"
            android:excludeFromRecents="true"
            android:process=":crash" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|navigation|density|uiMode"
            android:launchMode="singleTask"
            android:theme="@style/Theme.echomusic"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MUSIC_PLAYER" />

                <category android:name="android.intent.category.APP_MUSIC" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- Local audio files intent filter (Open with / View from file manager, downloads, etc.) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:mimeType="audio/*" />
                <data android:mimeType="application/ogg" />
                <data android:mimeType="application/x-ogg" />
                <data android:mimeType="application/flac" />
            </intent-filter>

            <!-- Listen Together invite link (HTTPS App Link - works after APK installation verification) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="https" />
                <data android:host="echomusic-listen-together.onrender.com" />
                <data android:pathPrefix="/listen" />
            </intent-filter>

            <!-- Custom scheme for direct opens from browser (works in ALL browsers) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="echomusic" />
                <data android:host="listen" />
            </intent-filter>

            <!-- Tidal OAuth (PKCE) redirect for the playlist migration importer -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="echomusic" />
                <data android:host="tidal-callback" />
            </intent-filter>

            <!-- Youtube filter -->
            <!-- autoVerify is DELIBERATELY OMITTED here (it was android:autoVerify="true"). autoVerify asks
                 Android to confirm, via Digital Asset Links on the domain, that we OWN youtube.com /
                 music.youtube.com / youtu.be — we don't, so on Android 12+ that verification ALWAYS fails,
                 and a failed-autoVerify web filter is treated as "do not offer this app for these links":
                 Aura stops appearing in the "Abrir con" chooser and the links can't even be enabled by hand.
                 As a plain (unverified) web filter it instead shows up in Ajustes → Apps → Aura → Abrir de
                 forma predeterminada, so the user can pick Aura as the handler. (autoVerify is kept ONLY on
                 the Listen Together filter above, whose domain the project actually controls.) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="youtube.com" />
                <data android:host="m.youtube.com" />
                <data android:host="www.youtube.com" />
                <data android:host="music.youtube.com" />
                <!-- video prefix -->
                <data android:pathPrefix="/v/" />
                <data android:pathPrefix="/embed/" />
                <data android:pathPrefix="/watch" />
                <data android:pathPrefix="/shorts/" />
                <!-- channel prefix -->
                <data android:pathPrefix="/channel/" />
                <data android:pathPrefix="/user/" />
                <data android:pathPrefix="/c/" />
                <!-- channel handle (music.youtube.com/@name) -->
                <data android:pathPrefix="/@" />
                <!-- playlist prefix -->
                <data android:pathPrefix="/playlist" />
                <!-- browse prefix -->
                <data android:pathPrefix="/browse/" />
                <!-- search prefix -->
                <data android:pathPrefix="/search" />
            </intent-filter>

            <!-- youtu.be short links. autoVerify omitted for the same reason as the filter above. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="http" />
                <data android:scheme="https" />
                <data android:host="youtu.be" />
                <data android:pathPrefix="/" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.media.action.MEDIA_PLAY_FROM_SEARCH" />
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="vnd.youtube" />
                <data android:scheme="vnd.youtube.launch" />
            </intent-filter>

            <!-- Share filter -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity-alias
            android:name=".MainActivityAlias"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Android TV launcher entry (same alias serves phone + TV). -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <activity-alias
            android:name=".MainActivityStatic"
            android:enabled="false"
            android:exported="true"
            android:icon="@mipmap/ic_launcher_static"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_static_round"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.FileProvider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <provider
            android:name="com.dpi.DensityScaler"
            android:authorities="${applicationId}.com.dpi.DensityScaler"
            android:enabled="true"
            android:exported="false" />

        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <service
            android:name=".playback.MusicService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
                <action android:name="androidx.media3.session.MediaLibraryService" />
                <action android:name="android.media.browse.MediaBrowserService" />
            </intent-filter>
        </service>

        <service
            android:name=".playback.ExoDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="androidx.media3.exoplayer.downloadService.action.RESTART" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <service
            android:name=".playback.AudioExportService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Music Recognizer widget: captures audio for recognition as a microphone foreground service. -->
        <service
            android:name=".widget.MusicRecognizerWidgetService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Transparent trampoline for the recognition tile: with mic permission it goes straight to the
             microphone foreground service; otherwise it opens the app to request the permission. -->
        <activity
            android:name=".recognition.RecognitionLaunchActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:launchMode="singleTask"
            android:noHistory="true"
            android:theme="@style/Theme.Metrolist.Transparent" />

        <!-- Microphone foreground service for in-app/tile recognition: keeps the ~10s listen capturing
             audio when the app is backgrounded or the screen locks (Android 12+ mutes AudioRecord for
             cached apps) and shows progress/result in a notification. -->
        <service
            android:name=".recognition.RecognitionForegroundService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Quick Settings tile to start music recognition (Shazam) without opening the app. -->
        <service
            android:name=".widget.RecognitionTileService"
            android:exported="true"
            android:icon="@drawable/mic"
            android:label="@string/recognition"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- WorkManager runs the resumable update download as a foreground worker; Android 14+ requires
             the requested foregroundServiceType (dataSync) to be declared on this service, else
             startForeground crashes with "foregroundServiceType ... is not a subset". -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Receiver for Listen Together notification actions (Approve/Reject) -->
        <receiver
            android:name=".listentogether.ListenTogetherActionReceiver"
            android:exported="false" />

        <receiver
            android:name="androidx.media3.session.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <!-- Music Widget -->
        <receiver
            android:name=".widget.MusicWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_music_player">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="iad1tya.echo.music.widget.PLAY_PAUSE" />
                <action android:name="iad1tya.echo.music.widget.LIKE" />
                <action android:name="iad1tya.echo.music.widget.UPDATE_WIDGET" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/music_widget_info" />
        </receiver>

        <!-- Turntable Widget -->
        <receiver
            android:name=".widget.TurntableWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_turntable">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="iad1tya.echo.music.widget.TURNTABLE_PLAY_PAUSE" />
                <action android:name="iad1tya.echo.music.widget.TURNTABLE_LIKE" />
                <action android:name="iad1tya.echo.music.widget.UPDATE_TURNTABLE_WIDGET" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/turntable_widget_info" />
        </receiver>

        <!-- Google Cast Options Provider -->
        <meta-data
            android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME"
            android:value="iad1tya.echo.music.cast.CastOptionsProvider" />

        <!-- Playlist Widget -->
        <receiver
            android:name=".widget.PlaylistWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_playlists">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/playlist_widget_info" />
        </receiver>

        <!-- Music Recognizer Widget -->
        <receiver
            android:name=".widget.MusicRecognizerWidgetReceiver"
            android:exported="true"
            android:label="@string/recognition">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="iad1tya.echo.music.widget.recognizer.TAP_MIC" />
                <action android:name="iad1tya.echo.music.widget.recognizer.UPDATE" />
                <action android:name="iad1tya.echo.music.widget.recognizer.RESET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/recognizer_widget_info" />
        </receiver>

    </application>

</manifest>