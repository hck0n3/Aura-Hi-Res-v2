---
name: Estado del port SimpMusic (capa de streaming)
description: Estado 2026-08-24 — reproducción RESTAURADA y confirmada por dueño (BravePipe); modernización de dependencias COMPLETA (5 pasos verdes); APK beta listo, pendiente prueba en celular
type: project
---

**🆕 2026-08-24: MODERNIZACIÓN DE DEPENDENCIAS COMPLETA** (orden del dueño "has lo que quedó pendiente en el orden"). Cadena de `docs/audit/MODERNIZACION.md` ejecutada, un build verde + commit por paso: Gradle 9.6.1 (`2e01a84`) → AGP 9.2.0 (`22eb193`) → Kotlin 2.4.0 + KSP 2.3.9 + Hilt **2.60.1** (`e40009b`; Hilt 2.59.2 NO basta — su kotlin-metadata-jvm topea en metadata 2.3.0, Dagger 2.60.1 lo arregla) → batch seguro + Compose 1.11.0 (`63beeb2`, verde en foss Y gms: coil 3.5.0, ktor 3.5.0, protobuf 4.34.2, Firebase BOM 34.15.0, etc.) → youtubedl-android 0.18.1 solo-catálogo (`6cbd321`; hallazgo: ningún módulo usa esas entradas — remanente del fork, sin efecto runtime). Intocados: material-icons-extended 1.7.8, materialKolor, media3, room, Material3 alpha, Superpowered, license/. Diferidos rojo: ffmpeg-kit EOL, rework Haze. APK beta listo en `app\build\outputs\apk\universalFoss\debug\app-universal-foss-debug.apk`; celular e6f2c8eb desconectado al cierre → la prueba beta queda pendiente de que el dueño conecte.

**⚠️ ESTADO 2026-08-24 MADRUGADA: FALLBACK BRAVEPIPE INTEGRADO, PENDIENTE PRUEBA EN CELULAR.** El muro anti-bot de YouTube rompió la reproducción durante la sesión del 2026-08-23. Cadena de evidencia (logcat):
- Con cookie (dueño hizo login, paso que se le pidió): PipePipeExtractor falla con `ExtractionException: android_vr player response is not valid` — YouTube devuelve respuesta SUSTITUIDA cuando el request ANDROID_VR va autenticado con esa cookie/contexto (confirmado en fuente del fork).
- Anónimo (fix de emergencia, commit `023e65f`): PipePipe falla con `AntiBotException: Sign in to confirm you're not a bot`.
- TeamNewPipe v0.25.2 (la vía que reprodujo itag 18 en la mañana): pasó a devolver lista VACÍA sin excepción — el bot-limit de esa IP subió de grado.
- Resultado: resolución caía a URLs directas InnerTube (clase quemada: probe 206, fetch real 403) y loop infinito de retry.
- El dueño (frustrado) pidió volver a la versión que funcionaba (`88b81a3`); se le explicó con evidencia que esa vía YA está seca en su IP y el revert no arreglaría. Salió con el celular desconectado (~18:15 local del 23).

**✅ YA INTEGRADO (commit `5e62f4b`, BUILD SUCCESSFUL en build-bravepipe2.txt):** BravePipeExtractor `com.github.maxrave-dev:BravePipeExtractor` pin `fa5d4a8b4c` — el MISMO fallback que usa SimpMusic cuando su PipePipe falla (Extractor.android.kt de maxrave-dev/core). Es fork de TeamNewPipe con cliente de extracción ANDROID (no WEB; no limitado al itag 18) y usa el MISMO paquete `org.schabi.*`, así que REEMPLAZÓ al TeamNewPipe stock en libs.versions.toml (clases duplicadas si conviven). Cambios: `pages/TeamNewPipe.kt` → `pages/BraveNewPipe.kt` (clases BraveNewPipe*), fallback en `NewPipe.kt` ahora PipePipe → BravePipe, force de nanojson `c7a6c1c08d…` en todas las configuraciones desde el root build.gradle.kts (igual que SimpMusic; sin él, NoSuchMethodError porque PipePipe trae nanojson viejo). Orden futuro si hace falta: SimpMusic además verifica itags requeridos (audio 250/251/774/141, video 137/136/134) y hace HEAD-check antes de confiar en PipePipe — NO implementado aún (mantener simple hasta probar).

**SIGUIENTE PASO (al reconectar el dueño el celular e6f2c8eb):** HECHO 2026-08-23 ~19:15 local: APK instalado, dueño tocó play, **BraveNewPipe devolvió 15 streams (set adaptativo completo: 139/140/137/136/134/248/247/244/243/242/160/278/18)**, AudioTrack activo, chunks 5 MB con 206 sostenidos y CERO 403 (PipePipe sigue AntiBotException; BravePipe lo salva). **Dueño confirmó ~19:20: suena bien, varias canciones OK y toggle música↔video OK → REPRODUCCIÓN RESTAURADA.** Si BravePipe cae algún día, siguiente cartucho = login correcto para PipePipe (cliente `mweb` vía setYoutubePlayerClient, o PoTokenProvider del fork con poToken/visitorData).

**Hallazgos PipePipe (investigación previa):** tokens = String plano (cookie completa); con cookie el fork añade Cookie + SAPISIDHASH + X-Origin + DNT a TODOS los /youtubei/v1/player; exige SAPISID o __Secure-3PAPISID; poToken/visitorData opcionales vía `NewPipe.setYoutubeSessionPoTokenProvider` (SimpMusic NO lo instala); SimpMusic usa `ServiceList.YouTube.tokens = cookie ?: ""` en su logIn, igual que Aura — su ventaja de supervivencia era el fallback BravePipe, ya portado. El fork soporta `setYoutubePlayerClient("mweb")` como alternativa no probada. setTokens de Aura sigue forzando anónimo (emergencia del commit 023e65f) hasta verificar extracción autenticada.

**Recetas:** Build: Corretto JDK 21 (`set "JAVA_HOME=C:\Program Files\Amazon Corretto\jdk21.0.12_9"`), verificar literal BUILD SUCCESSFUL en el log (los pipes mienten); si falla JdkImageTransform o "Unable to delete classes.jar" → `gradlew --stop` y reintentar. ADB: `C:\Users\AURA\AppData\Local\Android\Sdk\platform-tools\adb.exe -s e6f2c8eb`; paquete `iad1tya.aura.music.debug`.

**Reglas duras vigentes:** SOLO proveedores de SimpMusic; NO degradar calidad sin orden; commit SIEMPRE (feedback/commit-siempre.md); nunca push/tag sin permiso; dueño no programador → español latino, mantenerlo informado en cada paso; NUNCA loguear datos de usuario (la cookie JAMÁS a logs).
