name: "CodeQL Advanced"

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  schedule:
    - cron: '16 9 * * 2'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ${{ (matrix.language == 'swift' && 'macos-latest') || 'ubuntu-latest' }}
    permissions:
      security-events: write
      packages: read
      actions: read
      contents: read

    strategy:
      fail-fast: false
      matrix:
        include:
        - language: actions
          build-mode: none
        - language: c-cpp
          build-mode: manual
        - language: java-kotlin
          build-mode: manual
        - language: javascript-typescript
          build-mode: none
        - language: python
          build-mode: none

    steps:
    - name: Checkout repository
      uses: actions/checkout@v7

    - name: Set up JDK 21
      uses: actions/setup-java@v6
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Cache Gradle
      if: matrix.build-mode == 'manual'
      uses: actions/cache@v6
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-codeql-${{ matrix.language }}-${{ hashFiles('**/*.gradle*') }}
        restore-keys: |
          ${{ runner.os }}-gradle-codeql-${{ matrix.language }}-
          ${{ runner.os }}-gradle-

    - name: Create local.properties
      if: matrix.build-mode == 'manual'
      run: echo "sdk.dir=$ANDROID_SDK_ROOT" > local.properties

    - name: Ensure Android debug keystore
      if: matrix.build-mode == 'manual'
      run: |
        mkdir -p ~/.android
        if [ ! -f ~/.android/debug.keystore ]; then
          keytool -genkeypair -v -keystore ~/.android/debug.keystore \
            -storepass android -keypass android -alias androiddebugkey \
            -keyalg RSA -keysize 2048 -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US"
        fi

    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}
        build-mode: ${{ matrix.build-mode }}

    # CodeQL traces kotlinc/javac. Gradle caches / daemon hide those invocations and the analyze
    # step then fails with "could not process any of it". Force a clean traced compile.
    - name: Run manual build steps
      if: matrix.build-mode == 'manual'
      shell: bash
      env:
        LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
        LASTFM_SECRET: ${{ secrets.LASTFM_SECRET }}
        SUPERPOWERED_LICENSE_KEY: ${{ secrets.SUPERPOWERED_LICENSE_KEY }}
      run: |
        ./gradlew --stop || true
        set -o pipefail
        ./gradlew clean assembleUniversalGmsDebug --no-daemon --no-build-cache --console=plain 2>&1 | tee codeql-build.log

    # Build logs need a signed-in session to read, annotations do not. When the traced build fails,
    # publish the Gradle failure summary and the log tail as annotations so the cause is readable.
    - name: Report manual build failure
      if: failure() && matrix.build-mode == 'manual'
      shell: bash
      run: |
        [ -f codeql-build.log ] || exit 0
        WHY=$(grep -E "What went wrong|^e: |Exception|error:" -A8 codeql-build.log | head -80 | sed 's/%/%25/g' | awk '{printf "%s%%0A", $0}')
        echo "::error title=CodeQL Gradle failure::${WHY}"
        TAIL=$(tail -60 codeql-build.log | sed 's/%/%25/g' | awk '{printf "%s%%0A", $0}')
        echo "::error title=CodeQL Gradle log tail::${TAIL}"

    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
      with:
        category: "/language:${{matrix.language}}"
        # Large Android monorepo — give the analyzer more RAM headroom on the runner.
        ram: 14336
        threads: 4
