# Libro de crashes reportados por usuarios

> Regla permanente: TODO crash report que llegue (del dueño o de clientes) se registra aquí con su
> análisis y estado — nunca se pierde uno ni se re-diagnostica de cero. Complementa
> `REGRESSION_REGISTRY.md` (que registra errores/lecciones de desarrollo).
> Herramientas: el CI sube el mapping R8 de cada release (artifact `r8-mapping`) para desofuscar los
> frames; desde 0.6.134 el propio crash report incluye mensaje + cadena de causas + las últimas 25
> líneas del registro de reproducción (CROSSFADE_TRACE / RESOLVE_TIMING) = contexto de qué pasaba.

| # | Fecha | Versión | Dispositivo | Excepción | Contexto | Diagnóstico | Estado |
|---|---|---|---|---|---|---|---|
| 1 | 2026-07-21 | 0.6.117 (822) | Xiaomi 2512BPNDAC, Android 16 | `IllegalStateException: A migration from 38 to 39 was required but not found` | Abrir la app tras actualizar | MIGRATION_38_39 registrada solo en el builder muerto, no en el de Hilt (registry fila 89) | ✅ Hotfix 0.6.118 |
| 2 | 2026-07-27 | 0.6.133 (838) | Xiaomi 23090RA98G, Android 16 | `IllegalStateException` (sin mensaje) en `iad1tya.echo.music.utils.s2.n` — main thread, Runnable posteado, caller `p6.c0.X1` | Cliente "escuchando música normal", sin interacción | **RETRACEADO COMPLETO** (partition map local reprodujo el naming del CI, verificado frame a frame — `mapping.prt` es un ZIP de R8, una entrada por clase; el primer agente que lo "grepeó" como texto FABRICÓ un retrace — verificar siempre): `s2.n` = helper sintético de throw compartido de R8 (el frame superior MIENTE sobre la ubicación); el lanzador real es **media3 `ExoPlayerImpl.evaluateMediaItemTransitionReason:2550`** dentro de `updatePlaybackInfo` — la rama del "estado imposible" (UID actual cambió sin discontinuidad ni timelineChanged): carrera entre el hilo de reproducción de media3 y una MUTACIÓN de playlist de la app en el instante de una transición. Nuestro mayor mutador sobre players muriendo: el teardown del crossfade (`stop+clearMediaItems+release` sobre el saliente justo cuando su contenido TERMINA — y el cap `durOut` del 0.6.133 hace ese solape rutinario). Fix 0.6.134: quitar `clearMediaItems` (redundante antes de `release()`) de los 3 teardowns del crossfade + guard `runCatching` en el resume desprotegido de PoTokenWebView.onConsoleMessage (latente, mismo hunt). Clase de carrera de librería: no 100% demostrable sin repro — monitorear con los reportes auto-diagnosticables | ✅ Resuelto en 0.6.134 — **sin recurrencia**: el dueño confirma (2026-07-28) que no ha vuelto a pasar tras 0.6.134/135/136 |
| 3 | 2026-07-28 | 0.6.136 (841) | Dispositivo del dueño | **Ninguna excepción visible** — proceso desaparecido | "Reproduciendo música, canción a la mitad, en pantalla de Inicio; se cerró sin ningún error; al reabrir todo normal" | Investigación de código (sin repro): **NO fue un crash Java con diálogo**. Dos mecanismos compatibles: (a) **muerte por el Low-Memory-Killer** — Coil al 40 % del heap sin `largeHeap`, `onTrimMemory` **inerte en dispositivos capaces** (el `runCatching{memoryCache.clear()}` estaba dentro de `if (tier == LOW)`), portada decodificada **a resolución nativa (~5,8 MB por canción, heap software)** solo para extraer el color del tema en `MainActivity` (todos sus hermanos sí capan a 100×100), bitmap del widget de 360 KB recreado **cada segundo** (~21 MB/min), dos `DefaultAllocator` de 64 MB vivos durante el crossfade, y cola infinita que nunca se poda — agravada porque 0.6.136 fue la PRIMERA versión en la que la paginación corre con crossfade ON; (b) **OOM cuyo propio reporte falla**: `CrashHandler` construía el log completo ANTES de escribir nada y su rescate era `catch (Exception)`, que **no atrapa `Error`** → un OOM dentro del handler mataba el proceso sin diálogo y sin `last_crash.txt`. Además NO existía ninguna superficie que avisara de un reporte guardado (solo visible entrando a mano a Ajustes ▸ Registros) | 🔧 0.6.137: (1) **lector de `ApplicationExitInfo`** (API 30+) al arrancar → `logs/exit_reasons.txt` + chip "Cierres del sistema" en Ajustes ▸ Registros: Android guarda el MOTIVO real (LOW_MEMORY / ANR / CRASH_NATIVE / …) con PSS/RSS, y hace backfill de los 16 registros previos, así que **este cierre debería aparecer al abrir la nueva versión**; (2) `CrashHandler` escribe cabecera mínima ANTES de construir el log y captura `Throwable`; (3) portada del tema capada a 100×100; (4) `onTrimMemory` responde a CRITICAL en todos los dispositivos; (5) bitmap del widget memoizado. **Protocolo: al reabrir 0.6.137, mirar el chip "Cierres del sistema" — ahí estará la causa real, sin adivinar.** |
| 4 | 2026-08-06 | 0.6.148-beta1 (867) | Xiaomi 2512BPNDAC, Android 16 | `IllegalArgumentException: Key "aura_explore_row_Estados de ánimo y momentos_FEmusic_moods_and_genres_category" was already used` | El dueño **tocó Buscar** en la interfaz nueva. Cierre inmediato, en la fase de DIBUJO (`dispatchDraw` → `Choreographer.doFrame`), 47 s tras arrancar el proceso. Reproducción sin incidencias en el log (5 resoluciones OK justo antes) | **No hizo falta desofuscar: el mensaje nombra la clave duplicada.** `ui/newui/AuraSearchTabs.kt:183` (pestaña Explorar, nueva en 0.6.148-beta1) generaba la clave de cada fila como `"aura_explore_row_${section.title}_${row.first().endpoint.browseId}"`. Las entradas se agrupan de dos en dos (`section.items.chunked(2)`), y **TODAS las categorías de «Estados de ánimo y momentos» comparten el mismo `browseId`** (`FEmusic_moods_and_genres_category`) — solo se diferencian en `params`. Así que la clave es idéntica **desde la segunda fila en adelante** y Compose lanza al componer la lista. No es intermitente: cualquiera que abra Buscar con esa sección se lleva el cierre. La misma forma afectaba a las cabeceras (`"aura_explore_header_${section.title}"`) si dos secciones compartieran título | ✅ Arreglado antes de publicar: claves **por POSICIÓN** (`sectionIndex` + `rowIndex`) en lugar de por contenido. No pueden colisionar devuelva lo que devuelva el servidor, y son estables entre recomposiciones (esta lista no se reordena), que es lo que necesita `animateItem`. **Lección: una clave de lista derivada de datos del servidor es una apuesta a que el servidor no repite — y aquí repetía por diseño.** |
| 5 | 2026-08-09 | 0.6.155 (875) | Xiaomi 25069PTEBG (goya), Android 16 | `IllegalStateException` (sin mensaje) — main thread, `Handler` → Runnable → media3 | Cliente actualizó y puso a reproducir su playlist; crossfade ON/5s; log muestra varios `CROSSFADE_TRACE … swap-ok` y resoluciones OK justo antes | **Misma clase que #2, fix incompleto.** El 0.6.134 quitó `clearMediaItems` del teardown del fade, pero el swap seguía haciendo `fadingPlayer.removeMediaItems(next, count)` para impedir el auto-avance del saliente — eso es UNA mutación de playlist en el instante de la transición y dispara la misma rama "impossible state" de `evaluateMediaItemTransitionReason` (carrera async en el Handler; el `try/catch` del call site no la atrapa). Huella: Xiaomi + Android 16 + crossfade ON + playlist, ISE bare, sin interacción. Además quedaban `clearMediaItems` en el teardown del instant-video swap | ✅ Hotfix 0.6.163: `pauseAtEndOfMediaItems = true` (+ `REPEAT_MODE_OFF`) en lugar de `removeMediaItems`; sin `clearMediaItems` residuales en instant-video |

| 6 | 2026-08-09 | 0.6.163 (883) | Xiaomi 2512BPNDAC, Android 16 | (no crash) crossfade salta la canción | Canción con silencio al inicio (“A Man You Would Write About” / `OjMLBY2cVPk`); a ~7s pasa a la siguiente; al volver a ponerla sí suena | **BUG de lógica, no de stream.** `CROSSFADE_TRACE … tail-fire tier=silence remaining=290960ms` — el detector de cola va armado TODA la pista; el silencio del INTRO acumuló ≥7s y el gate “lejos del final pide 7s” **disparó el fundido como si fuera el final**. Al reponerla, o ya había audio, o el camino no rearmó igual. Afecta a **cualquier** tema con ≥~7s de silencio/intro bajo ~-42 dBFS y crossfade ON | ✅ Fix: en `onTailSilenceDetected`, si `silentNow` y aún no hay loud frame (`leadingSilenceUsOrNegative() < 0`) → return (no recheck 7s, no swap) |

## Protocolo al llegar un crash
1. Registrar fila aquí (fecha, versión, dispositivo, excepción, contexto del usuario).
2. Desofuscar con el artifact `r8-mapping` del run del tag correspondiente (retrace / lookup manual).
3. Verificar contra el código ANTES de arreglar (regla de siempre); el fix referencia la fila.
4. Actualizar estado (✅ versión del fix / 🔴 abierto) y, si dejó lección, fila en REGRESSION_REGISTRY.
