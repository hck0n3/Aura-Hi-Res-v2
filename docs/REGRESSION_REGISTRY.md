# Registro de regresiones — Aura Hi-Res Player

Objetivo: que **ningún bug reportado se repita**. Cada fix queda aquí con su archivo/línea guardián.
**Antes de publicar CUALQUIER versión:** revisar esta tabla y verificar (leer/grep el código, no solo compilar) que el diff nuevo NO revierte ni rompe ningún fix. Prestar atención a los archivos compartidos (una función tocada por muchas pantallas es la fuente #1 de regresiones): `MusicService.kt`, `Player.kt`, `App.kt`, `utils/Utils.kt`, `Lyrics.kt`, `YTPlayerUtils.kt`, `BackupRestoreViewModel.kt`, `HomeScreen.kt`, `ArtistItemsViewModel.kt`, `Thumbnail.kt`.

---

## ⚠️ LEER ANTES DE TOCAR CÓDIGO — los errores que YA he repetido

Las 73 filas de abajo registran bien, pero **no previenen**: nadie relee una tabla larga antes de cada
cambio, así que he vuelto a cometer la misma clase de error varias veces. Esto son solo los patrones
**REINCIDENTES**, con la cuenta real. Si un cambio toca alguno, verificarlo **explícitamente** antes de
decir que está arreglado.

**1. "Compila y parece correcto" NO es un arreglo. (5 veces)**
Placebos que llegaron a estar dados por buenos: los plegables (umbral 700 que la API redondeaba a 840 →
cero efecto), "volver a obtener" (refrescaba metadatos, servía el mismo audio), el Volumen Seguro (la
mitad que sube se entregaba a un objeto vacío), WorkManager (arreglaba fallos de un proceso que no era
el de las funciones) y la sesión caducada (placebo por 3 motivos a la vez).
→ **Trazar todo fix hasta el efecto que el usuario percibe:** ¿ese código se EJECUTA?, ¿en SU dispositivo
y configuración?, ¿el valor llega hasta lo que produce el efecto?, ¿hay un gate/default que lo anule?

**2. Un guardia debe reclamarse en el MISMO turno del hilo que lo comprueba. (2 veces)**
`radioSeedInFlight` movido dentro de un `launch` sobre `Dispatchers.Main` no-inmediato → media3 entrega
todos sus callbacks en un flush síncrono y el guardia no existía para ese flush → doble siembra.
`lastResolveWasAuthShaped` como campo de `object` → la precarga concurrente lo pisaba.
→ **Todo flag compartido en un `object`/singleton es sospechoso**: preguntar quién más corre a la vez
(precarga de cola, crossfade, descargas, export).

**3. Al copiar un patrón existente, copiarlo ENTERO. (2 veces)**
`player(noLogin)` copió de `browse(noLogin)` el quitar la cookie pero **no** el anular `dataSyncId` → el
reintento "anónimo" seguía nombrando la cuenta.
→ **Leer el patrón original completo y listar sus partes** antes de replicarlo.

**4. En este repo, loguear NO es solo logcat. (2 veces)**
`AppLogger.plant()` corre **sin guard en release** y persiste `>= INFO` a `filesDir/logs/app.log`, el
archivo que el usuario **comparte** desde Ajustes → Registros. Se colaron 42 volcados con títulos,
artistas, IDs y cuerpos de respuesta… y en el mismo commit que los quitaba, reañadí uno.
→ **Antes de loguear una variable: ¿contiene datos del usuario?**

**5. Ante posible PÉRDIDA DE DATOS, poner la barrera también en el CONSUMIDOR.**
Convertir un `throw` en "lista vacía" hizo que una respuesta ilegible desmarcara toda la biblioteca de
subidas. El parser puede equivocarse; el consumidor debe negarse a borrar con una lista vacía.
→ Las copias solo cubren **biblioteca** ⇒ esa pérdida es **irrecuperable**.

**6. Un servicio de terceros hay que SONDEARLO EN VIVO, no leerlo.**
Clave Last.fm vacía, rotación del cipher, `player_configs` desfasado: el código se veía bien y el
servicio ya había cambiado. **Si fallan MUCHAS canciones a la vez, mirar el cifrado ANTES que timeouts
o dispositivo.**

**7. La estrictez de un filtro la decide el ORIGEN de la señal, no su valor.**
Dos orígenes con fiabilidad opuesta que dan el mismo valor **no son el mismo hecho** (caché de géneros;
y agrupar `400..499` mezcló "el problema soy yo" con "el problema es este recurso").

---

| # | Bug reportado | Causa raíz | Fix (archivo:línea guardián) | Ver. | Estado |
|---|---|---|---|---|---|
| 1 | Portada del reproductor no aparece (estilos de fondo) / a veces sí a veces no | `Box(matchParentSize())` sin hijo que aporte tamaño → colapsa a 0×0 | `Thumbnail.kt` Box exterior debe usar `modifier.fillMaxSize()` | 0.6.92 | ✅ |
| 2 | Carrusel de Inicio "Para ti" parpadea vacío | pageCount cambia bajo un `rememberCarouselState` persistido | `HomeScreen.kt` `key(distinctQuickPicks.size){ HorizontalCenteredHeroCarousel }` | 0.6.92 | ✅ |
| 3 | Estilo Metro ignora tamaño/interlineado de letra | valores hardcodeados 36f/1.3 | `MetroLyrics.kt` params `lyricsTextSize/lyricsLineSpacing` (×1.5 baseline) | 0.6.92→0.6.93 | ✅ |
| 4 | Botón aleatorio en flotantes (no querido) | shuffle FAB en el toolbar | `MainActivity.kt` no pasa `onShuffleClick` a FloatingNavigationToolbar | 0.6.92 | ✅ |
| 5 | IA "ocupada / no disponible" | 429 no reintentado; 1 solo modelo; sin fallback | `AiPlaylistService.kt` (retry 429 + cascada modelos) + `AiPlaylistGenerator.kt` (fallback search/radio) | 0.6.92 | ✅ |
| 6 | Tema automático no sigue al sistema en vivo | `localeAwareContext` copiaba TODA la Configuration → fijaba uiMode | `utils/Utils.kt` `localeAwareContext` = Configuration vacío + solo setLocale | 0.6.93 | ✅ (auditado: NO rompió Android Auto — createConfigurationContext MERGE-a; car usa UiModeManager) |
| 7 | Letra Apple "letra por letra" no anima (LRC por-línea) | fork bloqueó el barrido fabricado tras `hasRealWordTimings` | `Lyrics.kt` APPLE_V2: fabricar timings char-weighted en el else | 0.6.93 | ✅ |
| 8 | Reconocimiento no funciona | endpoint Shazam externo + captura con AGC/ruido | `MusicRecognitionService.kt` (VOICE_RECOGNITION + AGC/NS off + re-captura) + config auto-sanable + relay | 0.6.93 | ✅ (endurecer: 404→relay, reset config) |
| 9 | Botón mic-glass pegado al borde | sin margen en el FAB glass | `FloatingNavigationToolbar.kt` `padding(end=10.dp)` si useGlass | 0.6.93 | ✅ |
| 10 | "No reproduce" tras restaurar copia | cookie stale del backup entra en `validateStatus` HEAD → 401/403 → descarta URL | `YTPlayerUtils.kt` `validateStatus` NO manda `YouTube.cookie` | 0.6.94 | ✅ |
| 11 | Restore puede corromper/dejar la app rota | sin validación/rollback; DB podía quedar cerrada | `BackupRestoreViewModel.kt` restore: integrity_check + rollback + restart garantizado + `dbFileIntegrityOk` | 0.6.94 | ✅ |
| 12 | **Android Auto falla, aparece/desaparece** | El fix #6 (localeAwareContext) fue EXONERADO (createConfigurationContext MERGE-a; car se detecta con `UiModeManager`, no la config). Causa REAL latente: `MediaLibrarySessionCallback` usaba `Job()` no `SupervisorJob()` → una excepción en un browse cancelaba TODO el scope → Auto vacío → flicker | `MediaLibrarySessionCallback.kt:67` `SupervisorJob()` + try/catch en `onGetChildren` | 0.6.95 | ✅ |
| 13 | **Portadas de artistas (sugeridos Y locales) no cargan** | `resize()` truncaba los avatares `yt3.ggpht.com` con `split("-s")[0]` → 404. Álbumes usan `lh3…=w…` (otra rama, ok) | `YouTubeUtils.kt` `resize()` devuelve raw para yt3 (fix raíz, todos los sitios); + `Items.kt` param `resize` | 0.6.95 | ✅ |
| 14 | **Discografías incompletas** (albums/singles/EP) | caché con clave `artistId` colisiona Albums↔Singles (ahora `artistId:browseId`); EP/Single nunca se completaban (ahora sección-aware); republish rebobinaba paginación (ahora merge + continuación viva) | `ArtistItemsViewModel.kt` cacheKey + isSinglesSection + merge | 0.6.95 | ✅ |
| 15 | **Copias guardaban settings (cookie/toggles)** → restore rompía | el ZIP incluía `settings.preferences_pb` (cookie/tokens/init-guards) | `BackupRestoreViewModel.kt` backup NO escribe settings; restore NO aplica settings (solo DB + EQ) | 0.6.95 | ✅ |
| 16 | **Discografía completa "no lo hace"** (iTunes→YouTube) | regresión `866b4c8`: excluía EP/Single de la ruta Álbumes; iTunes devuelve la mayoría como "-Single/-EP" → missing vacío → nada se publica. iTunes verificado OK en vivo | `ArtistItemsViewModel.kt` Álbumes completa el catálogo ENTERO (sin excluir); cap 80 | 0.6.96 | ✅ |
| 17 | **Sync YouTube Music no importa playlists (nunca)** | inanición: playlists iban ÚLTIMAS en executeFullSync; el re-fetch de CADA álbum + 250 imágenes de artista quemaban el presupuesto ~10min → worker cortado → retry desde arriba → playlists nunca | `SyncUtils.kt` playlists movidas arriba + álbumes solo-nuevos (worker extra descartado en audit: insert playlist no es upsert por browseId → duplicados) | 0.6.96 | ✅ |
| 18 | **Import Spotify: una fuente que falla aborta todo** | `importSources` sin try/catch por fuente → 429/playlist borrada/hash rotado tumbaba el resto silenciosamente | `SpotifyImportRepository.kt` try/catch por fuente + continúa + resumen de fallo | 0.6.96 | ✅ (pendiente: auto-sanado de hashes Spotify = acción owner, publicar JSON) |
| 19 | **Crash `SQLException code 21: connection is closed`** (Crashlytics 0.6.95, Xiaomi A16) | carrera del restore (confirmado; Xiaomi descartado): `database.close()` cierra el singleton Room mientras Flows del player/VM lo consultan en la ventana copia→restart → code 21 → uncaught → CrashHandler fatal mata el proceso antes del exitProcess del restore | `CrashHandler.kt` flag `isRestoring` + traga "connection is closed" SOLO durante restore; `BackupRestoreViewModel.kt` setea el flag antes de close() | 0.6.97 | ✅ |
| 22 | **Cola infinita REPITE canciones** (usuario odia repetidos) | 2 caminos: Path A (paginación onMediaItemTransition) SIN dedupe → continuaciones YouTube re-meten seed/oídas; Path B dedupe soft-demote + `recentRadioIds` acotado 60 | `MusicService.kt` set de sesión `sessionPlayedIds` (~4000 LRU, sembrado con cola inicial) + filtro duro en CADA append + Path B drop (no demote) | 0.6.100 | ✅ |
| 23 | **Cola infinita: seeding por CONTEXTO + IA de verdad** (mejora pedida; el usuario "no estaba convencido" — review 2-agente confirmó ~90% radio de YouTube) | (A) solo re-seedeaba del last-song, ignoraba el contenido de playlist/álbum; (B) el gusto NO ordenaba en Path A (escucha continua = orden crudo de YouTube); (C) sin diversidad de artista; (D) sin señal a nivel canción; (E) sin descubrimiento; (F) sin feedback graduado | **Fase A** (`MusicService.kt`): `radioSeedPool` capturado en `playQueue` (no para `YouTubeQueue`) + `tryContextRadio()` multi-seed (single→1, álbum→pistas distintas, playlist→artistas distintos, round-robin merge) antes de `tryRadio`; Path A ruteado por `orderedByTaste()`; `spacedByArtist()` (diversidad). **Fase B**: `SongGraphCache.kt` (grafo de relación de canciones gustadas, WiFi/bounded) + `MusicService.coRelMap()` (cacheado 5-min, OFF-main) → término `- coRel*2.0`; `withExplorationQuota()` + `TasteProfile.isKnownArtist` (~1/5 artista nuevo); `DislikeStore` soft sets + `+ soft*6.0` (demota, no elimina) + "Menos de esto" en `YouTubeSongMenu`. **INVARIANTES: `index - taste*4.0` (#25) INTACTO — #5/#7 solo SUMAN términos al key; no-repeat (#22) intacto; nunca dead-end (unheard→heardTail); `coRelMap` NUNCA per-song (batería); enrich SOLO junto a GenreCache (WiFi). NOTA batería (LOW aceptado): Path A ahora llama `orderedByTaste` por-canción → rebuild taste cada 5min en escucha infinita, bounded/off-main/cacheado.** | 0.6.103 | ✅ |
| 21 | **Last.fm/Scrobbling: login da "invalid parameters" (error 6)** para todos | el APK publicado salía con la **clave Last.fm VACÍA**: CI define `LASTFM_API_KEY=${{secrets...}}`; secreto vacío → env `""` → `System.getenv()` devuelve `""` (no null) → el `?:` no caía al fallback embebido → api_key vacío → Last.fm error 6. Clave embebida probada VÁLIDA (da error 4 con creds dummy) | `app/build.gradle.kts` `?.takeIf { it.isNotBlank() }` en env+localProperties → cae a la clave embebida | 0.6.99 | ✅ |
| 20 | **Discografía: aparece álbum TRUNCADO/incompleto + duplicado** (Lauren Daigle homónimo: instrumental + "oficial" con 8 temas truncados ~1:03) | el gate `isSourceComplete` solo corría en la ruta community-playlist; los AlbumItem (lista base + FILTER_ALBUM) nunca se filtraban; dedupe solo por id → 2 versiones; trackCount de iTunes descartado | `ArtistItemsViewModel.kt` gate de calidad de álbumes (mediana <90s / <60% temas iTunes) + reconciliación por título + filtro instrumental; `iTunesDiscography.kt` expone trackCount | 0.6.98 | ✅ |
| 24 | **Suscribirse / activar clave desde Ajustes** (pedido: no esperar a que expire la demo) | no había acceso a suscripción/activación fuera del gate de licencia | `SettingsScreen.kt` entrada "Suscripción Pro" que abre el `SubscriptionEntryScreen` YA existente (trae botón suscribir **y** campo de clave); gate `REQUIRE_SUBSCRIPTION && estado != SUBSCRIPTION_ACTIVE` + `BackHandler`. **INVARIANTE: NO tocar `license/` (grace/validación/master-key/Worker); solo superficia el flujo.** | 0.6.101 | ✅ |
| 25 | **Last.fm mejora recomendaciones** (secundario, opt-in — cross-app + cold-start) | `AffinityEngine` solo usaba historial local; ciego a lo que el usuario escucha en otras apps (scrobbles Last.fm) | `AffinityEngine.kt` `buildProfile(externalArtistWeights=)` merge en `byName` **SOLO** con cap `LASTFM_KEY_CAP=2.0`; `LastFmTasteSource`/`LastFmTasteWorker` (diario, WiFi, **cache-only en la ruta reco = sin red/batería**); gate toggle+username en `MusicService.tasteProfile()`+`HomeViewModel`. **INVARIANTES: local SIEMPRE primario — `maxArtistWeight` se calcula de `byId` (plays locales), NUNCA meter Last.fm en `byId`; reads UNSIGNED GET, NO tocar scrobble/`sessionKey` (ver #21); coeficiente `orderedByTaste index-taste*4.0` intacto; off/logout → emptyMap = cero cambio.** | 0.6.101 | ✅ |
| 26 | **Android Auto DESAPARECE de la pantalla en Xiaomi/MIUI** (reportado; recurrente, relacionado con #12) | MUERTE DE PROCESO/SERVICIO (NO la plomería — #12 SupervisorJob + caller accept-all confirmados intactos). (a) `onGetChildren` devolvía la biblioteca ENTERA vía `paginate(pageSize=Int.MAX_VALUE)` (AA suscribe sin paginar) → media3 parcela todos los MediaItem sobre el límite Binder ~1MB → `TransactionTooLargeException` en el hilo binder → proceso muere → desaparece (peor con biblioteca grande). (b) artwork 512×512×ARGB=1MB exacto → en Xiaomi alta densidad el downscale 320dp no salta → parcela metadata rompe el límite. (c) MIUI reap-ea el servicio no exento y el FGS-guard traga el crash pero no el kill; la exención batería/autostart estaba enterrada en ajustes | `MediaLibrarySessionCallback.kt` `paginate` capa MAX_VALUE→`take(500)` (paginación finita intacta) + `onGetLibraryRoot`/`onGetItem` try/catch (gates nunca lanzan); `CoilBitmapLoader.kt` `MAX_ARTWORK_PX` 512→384; `BackgroundReliability.isAggressiveOem()` + `BackgroundReliabilityDialog.kt` + `MainActivity.kt` prompt proactivo 1-vez (`BatteryReliabilityPromptShownKey`). **Límite honesto: autostart MIUI sin API pública — requiere confirmación del usuario.** | 0.6.102 | ✅ |
| 27 | **Reproducción FANTASMA: el player empieza solo** (reportado: ~8am sin tocar nada) | **CONFIRMADO:** tras restaurar la cola persistente el player queda preparado-pausado; un **PLAY externo (BT/audífonos/carro/reloj que se reconecta al despertar / AVRCP / notificación)** lo arranca en frío. **CLAVE (audit adversarial con bytecode media3 1.10.1):** dejar la cola IDLE NO basta — `Util.handlePlayButtonAction` hace `if(IDLE) prepare(); play();`, así que media3 la prepara Y reproduce igual. Y `onPlayRequested` NO existe en 1.10.1 (solo `onPlayerCommandRequest`) | `MediaLibrarySessionCallback.kt` `onPlayerCommandRequest` **VETA** (`RESULT_ERROR_PERMISSION_DENIED`) el `COMMAND_PLAY_PAUSE` externo mientras `MusicService.awaitingFirstUserPlay` (armado al restaurar). El play in-app llama al ExoPlayer DIRECTO (bypass del callback) → normal intacto. Flag se limpia al: play genuino, `isPlaying=true`, abrir la app (`onAppForegrounded` desde MainActivity onStart/bind), o tap del widget (prepare+play directo). Además `onSetMediaItems` (AA: navegar→tocar una canción) limpia el veto (selección explícita = interacción genuina). **Trade-off honesto (chico): tras un reinicio, un PLAY externo "a secas" (BT reconexión) sobre la cola restaurada no la arranca hasta que el usuario interactúe una vez (tocar canción en AA / widget / abrir app / play in-app).** | 0.6.104 | ✅ |
| 28 | **Streaming degradado tras cada update: arranque lento / reanudar cuesta / no reproduce para algunos** (recurrente "tras cada update"; pista: "limpiar caché mejora" = en realidad **Borrar datos** o "limpiar caché de canción", porque los cachés viven en `filesDir` no `cacheDir`) | **CONFIRMADO (1):** `songUrlCache` es EN-MEMORIA (`MusicService.kt:596`) → cada update reinicia el proceso → primer play/resume de CADA canción re-resuelve todo (cipher + hasta 12 clients + HEAD `validateStatus` con timeouts 15s+15s, cap 30s) → lento (4b/4c). **CONFIRMADO (2):** media3 SimpleCache NoOpEvictor sin límite en `filesDir` + ruta "Ghost cache entry" (`:5010`) BORRA los bytes y re-resuelve todo cuando `songUrlCache` está vacío tras reinicio. **PLAUSIBLE (3, para 4d):** `visitorData`/`dataSyncId` persistidos rancios → poToken/player 403 para algunos. **PLAUSIBLE (4):** self-heal cipher solo dispara con `sigInfo==null`, no con match-pero-URL-mala. REFUTADO: poToken (en-memoria), cipher/config (re-analizados cada cold start, self-heal intacto) | (1) persistir `songUrlCache` (id→url+expire) + bajar timeouts HEAD a ~4-5s + short-circuit al primer client bueno; (2) Ghost path: re-resolver solo la URL, CONSERVAR bytes; (3) migración 1-vez (flag nuevo) que refresca `visitorData`/`dataSyncId` en version bump (cookie intacta); (4) extender self-heal a 403-tras-match + **[owner] publicar player_configs.json** | 0.6.104 | ✅ (audit PUBLISH-SAFE; notas LOW: HEAD 15s tolerante ok; ghost mismatch-itag baja prob) |

| 29 | **0.6.104 NO REPRODUCE NADA para usuarios logueados** (regresión P0 introducida por el fix #28.3) | La migración `applySessionRefreshFor104` borraba `VisitorDataKey` **y `DataSyncIdKey`**. El resolve usa `sessionId = if(isLoggedIn) YouTube.dataSyncId else visitorData` (`YTPlayerUtils.kt:114/528`) para el poToken. `visitorData` SÍ se re-fetchea (`App.kt:932`), pero **`dataSyncId` NO** — solo se deriva en el login (`App.kt:943-950`, `LoginScreen`). Borrarlo → `YouTube.dataSyncId=null` → sessionId null → poToken/resolve falla → sin reproducción para todo usuario logueado | `App.kt` `applySessionRefreshFor104` YA NO borra `DataSyncIdKey` (solo `visitorData`); `YTPlayerUtils.kt` fallback `sessionId = (if(isLoggedIn) dataSyncId else visitorData) ?: visitorData` para **recuperar a los ya-rotos SIN re-login**. **LECCIÓN: nunca borrar en una migración un token que no se re-fetchea (login-derived); verificar el re-fetch antes de limpiar.** | 0.6.105 | ✅ |

| 30 | **"El vídeo no está disponible" / no reproduce — YouTube ROTÓ el player** (log: hash `66a6ea83`, `N/SIG FUNCTION EXTRACTION FAILED`, self-heal no lo tiene) | YouTube rotó a un player nuevo (`66a6ea83`) cuyas funciones `sig`/`n` los regex de `FunctionNameExtractor` NO reconocen; no está en los hardcoded ni en `player_configs.json`. NO es la sesión (#29 es aparte). Es el #28.4 / #4d (rotación de cipher) que ya estaba marcado como acción del owner | **Vía rápida (sin update):** publicar la config VERIFICADA de `66a6ea83` en `player_configs.json` del repo (`RemotePlayerConfig.REMOTE_CONFIG_URL` = raw.githubusercontent.com/hck0n3/…/main/player_configs.json) → self-heal para todos. **REGLA: jamás publicar valores adivinados (romperían más); solo verificados.** Alternativa: actualizar los regex del extractor a los patrones actuales (yt-dlp/NewPipe). Config VERIFICADA (Chromium headless + CDN googlevideo HTTP 206; sig=`C3(14,932,INPUT)`, n=`g.WM` trick, STS 20640) publicada en `player_configs.json` (raw URL confirmada sirviendo). Los regex NO pueden extraer estos players VM-dispatch (por diseño → config remoto es el vector correcto). | player_configs.json | ✅ (self-heal LIVE, sin update) |
| 31 | **Traducir letras se cuelga en "traduciendo" / error 404** | el camino "IA gratis" ruteaba por `OpenRouterService.translate`→Pollinations (`text.pollinations.ai/openai`, **502 caído**) + fallback Worker `/ai` **no desplegado** (404) → colgado/sin traducir | `LyricsTranslationHelper.kt`: camino keyless usa **Google Translate gratis** (`translate.googleapis.com/translate_a/single?client=gtx`, sin clave, 200 verificado) PRIMERO + `withTimeoutOrNull(30s)` (nunca se cuelga) + fallback IA; normaliza segmentos al #líneas. Ruta con clave del usuario intacta | 0.6.106 | ✅ |
| 32 | **Novedades: hay que refrescar manual cada vez que entro** | `ReleaseRadarScreen`/`ReleaseRadarViewModel` a propósito NO auto-refrescaban (diseño "weekly Friday drop"); el worker semanal lo mata MIUI → siempre stale | `ReleaseRadarWorker.refreshIfStale` (gate 6h vía `LAST_RUN_KEY` en `app_prefs`) llamado en `ReleaseRadarViewModel.init`; worker semanal + botón manual intactos. **Batería: gate 6h + WorkManager KEEP.** | 0.6.106 | ✅ |
| 33 | **"Ir al álbum" no sale en algunas canciones** | la acción se gateaba con `album != null`; InnerTube deja `album` null en semillas de radio/cola/búsqueda cuyo byline no trae el álbum enlazado (videos = correcto null) | `models/AlbumResolver.kt` `resolveAlbumForSong` (vía `YouTube.queue`/`next`, browseId MPRE compatible) + `rememberResolvedAlbum` (hooks incondicionales, red SOLO si falta el álbum, muere con el menú) cableado en 6 menús. **NO muta el MediaMetadata/cola; red on-demand acotada.** | 0.6.106 | ✅ |

| 34 | **Cola por CONTEXTO: al terminar una playlist mixta la continuación NO tiene que ver** (reportado; playlist de clásicos+otros → infinita suena algo ajeno) | **2 causas CONFIRMADAS:** (1) **un MOOD viejo del Home gana** — `tryMood()` es PRIMERO en la cadena y `activeMoodParams` NUNCA se limpiaba al reproducir una colección; un chip de mood tocado hace rato y olvidado siembra la infinita de ese mood (ajeno). (2) `radioSeedPool` era snapshot de la 1ª página (género cabecera), nunca el tramo final. **REFUTADO:** taste/coRel/exploración (solo reordenan DENTRO del pool, no meten géneros ajenos) y autoplay de YouTube (no se cuela) | `MusicService.kt` (A) `playQueue`: al iniciar una colección (`queue !is YouTubeQueue`) limpiar `activeMoodParams/Title` (el mood-chip NO llama a playQueue → un mood tocado DESPUÉS sobrevive, caso legítimo intacto); (B) `tryContextRadio` siembra del **tramo reciente REAL** (`player.getMediaItemAt` últimos ~24, recent-first) no del snapshot; gate `radioSeedPool.size>1` intacto (radios sin tocar). **#25/no-repeat/never-silence intactos.** | 0.6.107 | ✅ |

| 35 | **La calidad de audio se queda PEGADA: una canción ya oída nunca sube a LOSSLESS** (hallado en la auditoría del port 5.2.6, no reportado por el usuario) | `MusicService.kt:5118-5128` fija `lockedQuality` desde la **fila persistida** `FormatEntity` (`dbFormat`), no desde un estado de sesión → el pin **sobrevive reinicios para siempre**: suena A en OPUS → cambias a LOSSLESS → repones A → `dbFormat` dice opus → vuelve a resolver en OPUS y **re-escribe** la fila opus. El guard de contenedor (`:5127-5145`) NO puede saltar (compara contra el mismo `dbFormat` del que se derivó). Solo suben las nunca-oídas (`dbFormat==null`) o las pre-cargadas por la cola (resuelven al `audioQuality` global, `:6720`). Además el pin depende del **hilo**: `currentPlayingMediaId` se setea en Main (`:3500`) y el resolver corre en el loader (`:5091`). **(b)** `:1238` `?: return@collect` → cambiar calidad **sin nada sonando** (el caso normal: estás en Ajustes) no limpia NADA. **(c)** el clear de `:1242` no re-persiste → el blob `SongUrlCacheBlobKey` conserva URLs de la calidad vieja | Llevar la calidad en el **VALOR** del caché (`Triple<url, expiry, quality>`), NUNCA en la clave — el `substringAfter("_")` del upstream es **buggy** (los videoId son base64url, ~16% traen `_`). `:5118` → `cachedQuality ?: audioQuality` (mantiene el lock a mitad de canción, permite que una **reposición** suba de calidad); `:1238` quitar el early-return; re-persistir tras el clear. Blob viejo sin `"q"` → null → cae al `audioQuality` global (sin migración). **INVARIANTE #28: preservar la búsqueda per-mediaId y la persistencia de URL; el guard de contenedor `:5127-5145` NO se toca (protege los BYTES cacheados).** | 0.6.108 | ✅ 0.6.108 |
| 36 | **"Volver a obtener" es un PLACEBO para el audio** (hallado en la auditoría del port 5.2.6) | **CORREGIDO tras verificar (mi brief inicial decía 4 menús: FALSO).** Solo **DOS** sitios tenían el bug — `SongMenu.kt:886` y `QueueMenu.kt:531` — y son refetch **por canción**: `YouTube.queue()` + `database.update()`, solo metadata → se seguía sirviendo el stream/bytes viejos. **NO eran el bug** (y NO se tocaron): `OldPlayerMenu`/`PlayerMenu.kt:533` ya recargan audio de verdad vía `refetchCurrentInOpus()` (`MusicService.kt:3749`: `songUrlCache.remove` + bypass + stop/seek/prepare); y `AlbumMenu.kt:588` refresca un **ÁLBUM** (`YouTube.album()`), no una canción — aplicarle limpieza por-canción habría re-descargado el álbum entero desde una acción de metadatos (decisión aparte si alguna vez se quiere). Faltaban 2 piezas: ni handler `ACTION_CLEAR_SONG_CACHE` en `onStartCommand` (solo widgets) ni `deleteFormat` en el DAO. **Pieza que casi se escapa:** `DownloadUtil` tiene su **PROPIO `songUrlCache` privado** (`:73`, leído en `:106`) → sin `invalidateSongUrl()` la descarga re-encolada habría vuelto a bajar el stream rancio: el fix se vería bien y no haría nada. **Quirk pre-existente (no tocado, decisión del owner):** `refetchCurrentInOpus` fuerza Opus → degrada un track LOSSLESS/SAAVN hasta la siguiente canción | `DatabaseDao.deleteFormat` (@Query plano, **sin migración**, DB sigue v38); rama `ACTION_CLEAR_SONG_CACHE` en `onStartCommand` que quita del `songUrlCache` **y re-persiste** (si no, la URL vieja vuelve al reiniciar); los 4 menús limpian bytes + fila de formato. **Limpiar `songUrlCache` es OBLIGATORIO, no opcional: sin eso el hit de caché (`:5180`) reusa la URL vieja y borrar los bytes no sirve de nada.** Descargadas (decisión owner): quitar la descarga **y re-encolarla** a `downloadQuality` (`DownloadUtil.kt:113`, pref SEPARADA) — ni se pierde el offline ni el refetch es un no-op (el short-circuit de descarga completa `:5099` serviría bytes viejos para siempre). **NO choca con #28.2: aquel prohíbe el borrado IMPLÍCITO al faltar URL; esto es petición EXPLÍCITA del usuario.** | 0.6.108 | ✅ 0.6.109 |
| 37 | **El interruptor de hápticos globales es casi un PLACEBO** (hallado en la auditoría del port 5.2.6) | `GlobalHapticsKey` (`PreferenceKeys.kt:171`) + `HapticClickable.kt` existen, pero **solo 3 sitios lo respetan** (`IconButton.kt:47,75`, `Material3SettingsGroup.kt:96`). ~60 `performHapticFeedback(LongPress)` lo esquivan; no hay háptico de scroll. Además `AppearanceSettings.kt:1029-1030` tiene literales en español **hardcodeados** (no recursos) | Interceptor raíz `pointerInput` en el `BoxWithConstraints` de `MainActivity` (pasada **Initial**, observa-no-consume): click→`CONTEXT_CLICK`, scroll→`CLOCK_TICK` con **throttle 100ms** (sin throttle = vibración continua = batería). Borrar `HapticClickable.kt` + sus 3 usos o **doble-buzz**. **CLAVE: reusar `GlobalHapticsKey`, JAMÁS renombrar a `enableHaptics` del upstream — un rename resetea el ajuste a todos (misma clase que [[one-time-default-migrations-need-fresh-key]]).** | 0.6.108 | ✅ 0.6.109 |
| 38 | **MI ERROR (latente, no explotado): código muerto mandaba a los usuarios al APK del upstream** | `echomusic/UpdateNotificationHelper.kt:35,37` construía el PendingIntent de "hay actualización" contra `github.com/EchoMusicApp/Echo-Music/releases/.../echomusic.apk` y `nightly.link/EchoMusicApp/...`. Estaba **importado en `MainActivity.kt:175` pero nunca llamado** → nunca explotó, pero cualquiera que lo cableara mandaba a los usuarios de Aura al binario de OTRO proyecto. El updater real (`echomusicupdater.kt:356/684/704/734`) sí apunta a `hck0n3/Aura-Hi-Res-Player` ✅. También fugaban branding las cabeceras `HTTP-Referer: github.com/EchoMusicApp/Echo-Music` + `X-Title: echomusic` en las 3 llamadas IA | Archivo **borrado** + import fuera; cabeceras → Aura (`AiPlaylistService.kt:215-216`, `OpenRouterService.kt:144-145`, `OpenRouterStreamingService.kt:118-119`). **LECCIÓN: en un fork, el código muerto heredado es una mina — una URL del upstream en una ruta apagada es un incidente esperando a que alguien encienda la ruta. Barrer `EchoMusicApp` en cada release.** Fugas VIVAS pendientes (decisión owner): `ListenTogetherServers.kt:25` (lista de servidores desde el repo de Echo) y `commitscreen.kt:108` (feed de commits de Echo) | 0.6.108 | ✅ 0.6.109 |
| 39 | **MI ERROR (cazado en review ANTES de publicar): generalizar el carril de género lo habría vuelto sesgado a la biblioteca** | Al generalizar el carril duro de autoplay (cristiano → género real), el filtro quedó "conserva solo los candidatos cuyo carril **coincide**". Pero `GenreCache` **solo se enriquece con artistas de tu biblioteca** (`HomeViewModel.kt:170,180`) → un artista nuevo del radio es desconocido **por construcción** → `laneOfTrack` = null → el filtro los tiraba TODOS → la infinita habría colapsado a repetir artistas que ya tienes (cero descubrimiento). Justo lo contrario de lo pedido | Filtro **asimétrico** por origen de la señal: `CHRISTIAN` viene de palabras clave sobre el texto de la propia canción (no necesita caché, y un desconocido es en la práctica secular) → **estricto**, comportamiento original intacto. Un carril de **género** viene del caché → **suave**: solo se descarta lo que SÉ que es de otro género; desconocido sigue elegible. Además `GenreCache.enrich` de los artistas que el radio va sacando (fire-and-forget tras encolar, **solo WiFi**, tope `GENRE_LEARN_PER_RUN=12`, los fallos se cachean → el coste decae a 0) para que el carril deje de ser ciego fuera de la biblioteca. Guard `inLane.size >= 2` intacto (nunca dead-end). **LECCIÓN: una señal derivada de un caché hereda el SESGO de cómo se llena ese caché. Antes de filtrar por ella, preguntar: ¿quién puebla el caché, y se parece al conjunto que voy a filtrar?** | 0.6.108 | ✅ 0.6.108 |

| 40 | **MI ERROR (BLOCKER, cazado por el audit ANTES de publicar): el fix de calidad #35 recreaba el #28** | El stamp del `songUrlCache` guardaba `lockedQuality` = **la calidad PEDIDA**, no la entregada. Pero el fallback es RUTINA (`YTPlayerUtils.kt:206` LOSSLESS→Qobuz 2×9s → `:320` Saavn 15s → Opus; `:1054` m4a cuando no hay webm para un OPUS). Con global=LOSSLESS y una canción sin match en Qobuz/Saavn (≈ todo el catálogo cristiano/español del usuario): llega Opus, la fila `FormatEntity` dice `codecs="opus"`, pero el caché decía LOSSLESS. Ese caché es el INPUT del `lockedQuality` de la canción sonando (`:5221`), que a su vez es el input del **guard de contenedor** (`:5225-5239`), que compara contra `dbFormat` → **mismatch permanente** → `shouldBypassCache=true` + `playerCache.removeResource(mediaId)` → **borra los bytes de la canción EN CURSO** → re-resolve completo (~33s de timeouts Qobuz+Saavn en el hilo del loader) → vuelve a caer a Opus → re-estampa LOSSLESS → **bucle infinito en cada `open()`** (replay, seek fuera del buffer, reintento 403). El código PRE-diff era inmune **por construcción**: `lockedQuality` se DERIVABA de `dbFormat`, así que `cacheMatchesTarget` nunca podía ser falso para la canción sonando. El guard quedó **textualmente intacto pero funcionalmente invertido** al cambiarle el input — el #35 prometía por escrito "el guard NO se toca" | Estampar la calidad **ENTREGADA**, derivada con el **MISMO predicado que usa el guard** (`isFinalLossless`/`isFinalSaavn`, ya calculados en el mismo bloque `run{}`) — en el resolver **y** en el prefetch (`data.format.mimeType`). El #35 sigue intacto: un cambio de calidad limpia el mapa salvo la entrada en curso, así que una **reposición** no encuentra entrada, cae al `audioQuality` global y sube de calidad. **LECCIÓN (clase nueva): cuando dos sitios comparan el mismo hecho, deben DERIVARLO del mismo origen con el mismo predicado. Guardar "lo que pedí" donde otro lee "lo que hay" es un mismatch garantizado. Y un guard puede romperse SIN tocarlo: basta cambiar lo que le entra.** | 0.6.108 | ✅ 0.6.108 |
| 41 | **MI ERROR (HIGH, mismo audit): la estrictez del carril iba por VALOR y no por ORIGEN → #39 por la otra puerta** | El fix del #39 quedó `strictLane = (currentLane == CHRISTIAN)`. Pero tras generalizar, CHRISTIAN puede venir de **dos orígenes**: palabras clave (texto de la propia canción, sin caché) **o del GenreCache** (iTunes etiqueta "Christian & Gospel" → `normalizeGenre` → CHRISTIAN). Hillsong UNITED / "Oceans" no tiene marcador en el texto pero SÍ género cacheado (viene de los artistas más oídos del usuario, `HomeViewModel.kt:180`) → carril CHRISTIAN **cacheado** → rama **estricta** → tira todo candidato desconocido → colapso a la biblioteca **en el género principal del usuario**. Exactamente el #39 que yo mismo acababa de arreglar, reintroducido por la otra vía. En 0.6.107 ese caso daba lane=null (sin enforcement) → **es regresión nueva, no comportamiento previo** | `GenreLane.isKeywordChristian(title, artist, album)` + `strictLane = currentLane == CHRISTIAN && isKeywordChristian(...)` → **solo** el carril derivado de palabras clave es estricto; cualquier cosa derivada del caché va por la rama suave. El `&& currentLane == CHRISTIAN` es necesario: un marcador SOLO en el nombre del artista ("Christian Nodal", cacheado "latin") daría `isKeywordChristian=true` con `currentLane="latin"` → aplicaría estrictez a un carril latino. **LECCIÓN: la estrictez de un filtro se decide por el ORIGEN de la señal, NUNCA por su valor. Dos orígenes con fiabilidades opuestas que producen el mismo valor NO son el mismo hecho.** Ver [[cache-derived-signal-inherits-cache-bias]] | 0.6.108 | ✅ 0.6.108 |

| 42 | **La "cascada de 4 modelos gratis" de la IA era FICCIÓN: 3 de los 4 no existen** (hallado probando los endpoints EN VIVO, no leyendo código) | `AiPlaylistService.POLLINATIONS_MODELS = listOf("openai","mistral","llama","deepseek")` y el mismo listado en `LyricsTranslationHelper.FREE_KEYLESS_MODELS`. Probado contra `https://text.pollinations.ai/models`: el tier **anónimo (sin clave) expone EXACTAMENTE UN modelo** — `openai-fast` (GPT-OSS 20B), cuyos alias incluyen `openai`. Los otros tres devuelven `{"error":"Model not found: <n>. This is our legacy API..."}`. O sea: **1 modelo probado 4 veces con 3 nombres inexistentes**. Efecto real: cuando el único modelo falla se queman **3 viajes de red garantizados a fallar** del presupuesto de 60s → la IA tarda MÁS en rendirse, y todo el mundo (yo incluido, ver [[ai-playlist-never-dead-end]]) creía que había redundancia. **NUNCA hubo.** Nadie lo probó nunca: el código *parecía* correcto | `listOf("openai")` en ambos sitios + comentario con el resultado del sondeo. **La redundancia real necesita un SEGUNDO PROVEEDOR, no más nombres del mismo.** Estado verificado (2026-07-16): Pollinations **VIVO** (HTTP 200, 1.4s) pero **intermitente** (502 horas antes); Worker `/ai` **404 = no desplegado** (acción owner, código listo en `docs/worker/ai-route-README.md`); licencia `/verify`+`/demo` **VIVOS** (404 en GET porque solo aceptan POST; con POST → `{"status":"invalid"}` 400 = correcto). **CREAR playlist nunca muere igualmente** (`AiPlaylistGenerator.buildFallbackSongs:163` búsqueda+radio, usado en `:114`); **EDITAR con IA sí depende del proveedor** (a propósito: un cambio inventado en tu playlist es peor que ninguno). **LECCIÓN: un endpoint/modelo/clave de terceros hay que SONDEARLO EN VIVO, no leerlo. La misma clase que #21 (clave Last.fm vacía) y #30 (rotación de cipher): el código se ve bien y el servicio ya cambió debajo.** | 0.6.108 | ✅ 0.6.109 |

| 43 | **El vídeo se friza, el audio sigue; salir de la ventana y volver NO recupera** (reportado 2026-07-16: *"siempre el video se friza de la nada y solo sigue el audio, y si me salgo de la ventana y vuelvo el video se friza y no vuelve"*) | ✅ **RESUELTO — CAUSA PROBADA CON BYTECODE.** ⚠️ **DOS SUPUESTOS MÍOS ERAN FALSOS y esta fila los rectifica:** (1) la versión anterior de esta fila señalaba `ui/player/MusicVideoPlayer.kt`, que resultó ser **CÓDIGO MUERTO — cero call sites** (borrado en este fix): investigué un archivo que nunca se ejecuta. (2) el invariante "modo vídeo = ExoPlayer dedicado con el principal en pausa" está **RANCIO**: `MusicService.kt:773` dice *"Video mode is INTEGRATED into the main player (one engine)"*, implementado con `MergingMediaSource` (vídeo-only + audio) sobre el player PRINCIPAL. La ruta VIVA es `ui/player/PlayerVideoSurface.kt` (5 call sites). **CAUSA RAÍZ:** su `update` afirmaba en comentario que *"setVideoTextureView(sameTv) es un no-op cuando ya es la vista actual… barato y seguro"*. **FALSO** — desensamblado el AAR real de media3 1.10.1, `ExoPlayerImpl.setVideoTextureView` SIEMPRE ejecuta `removeSurfaceCallbacks()` y, si `!textureView.isAvailable()`, llama **`setVideoOutputInternal(null)`**: destruye activamente la salida de vídeo. Y `update` corre en **CADA recomposición** (la lambda se recrea → Compose siempre la ve cambiada). Cualquier recomposición que caiga en la ventana en que el `SurfaceTexture` aún no está disponible → salida a null. El renderer de AUDIO no necesita superficie → **el audio sigue sobre un fotograma congelado**, exactamente el síntoma reportado. **Gatillo del "salgo y vuelvo":** `MainActivity.onStop` hace `unbindService` **sin `dispose()`**, y `onServiceDisconnected` — único sitio que llama `dispose()` — **NO se invoca en un unbind normal** (Android solo lo llama en desconexión inesperada). Al volver, `onServiceConnected` construía un `PlayerConnection` **NUEVO** dentro de un `mutableStateOf` → (a) fuga del anterior, que seguía registrado como `Player.Listener`; (b) **recomposición total** de todo lo que lo lee → `update` → superficie a null justo mientras la Activity se reinicia. Si esa recomposición cae DESPUÉS de que el `SurfaceTexture` ya se recreó, no queda callback pendiente que reenganche: **congelado para siempre**. | (1) `PlayerVideoSurface.kt`: `update` solo re-engancha **si `tv.isAvailable`** — conserva el fix de rotación (re-attach cuando la superficie nueva ya vive) y elimina la única rama que la destruía; media3 instala su propio `SurfaceTextureListener` en `factory()`, que cubre no-disponible→disponible. (2) `MainActivity.onServiceConnected` **REUSA** el `PlayerConnection` cuando se re-vinculó al MISMO servicio vivo (`existing.service === service.service`) → mata la fuga y la tormenta de recomposición; solo construye uno nuevo si el servicio cambió de verdad, y entonces hace `dispose()` del viejo. (3) `MusicVideoPlayer.kt` **BORRADO** + su referencia en el KDoc de `SongPreviewController`. **NO es regresión del 0.6.84** (aquello tocó el prefetch de vídeo, no el ciclo de vida de la superficie). **LECCIÓN DOBLE: (a) un comentario que afirma "esto es un no-op" sobre una librería de terceros es una HIPÓTESIS, no un hecho — verificarla contra el bytecode; (b) antes de investigar un archivo, comprobar que se USA (grep de call sites), o se depura código muerto.** | 0.6.109 | ✅ |

### Tanda reportada 2026-07-18 (#44–#58) — registrada en bloque al llegar, ANTES de investigar

| 44 | **"Me gusta" en Android Auto no actualiza** (reportado 2026-07-18) | **El like SÍ se guardaba** — fallo puramente VISUAL. `toggleLike` (`MusicService.kt:3213-3257`) es correcto: `upsert` dentro de UN solo `database.query{}` (invariante [[likes-must-upsert-not-update]] intacto), auto-download en try/catch propio, y **no toca `FormatEntity`** (no re-normaliza audio). El camino AA llega bien (`MediaLibrarySessionCallback.kt:127` ACTION_TOGGLE_LIKE). **CAUSA, verificada en el bytecode de media3 1.10.1:** `updateNotification()` usaba el overload GLOBAL `mediaSession.setCustomLayout(list)`, que en `MediaSessionLegacyStub.setPlatformCustomLayout` **solo hace `putfield customLayout; return`** — escribe un campo y nada más. El overload POR CONTROLADOR `setCustomLayout(ControllerInfo, list)` además llama `updateLegacySessionPlaybackState()`, que es lo que reconstruye y **emite** el `PlaybackStateCompat` con las custom actions. **Android Auto es cliente LEGACY y lee el corazón de ese PlaybackStateCompat**, así que con la llamada global se quedaba con el icono viejo hasta que un evento DEL PLAYER (play/pause, cambio de pista, seek) republicara el estado por otra vía — y un "me gusta" no cambia nada del player. La notificación del móvil sí se actualizaba porque su provider se re-invoca aparte: por eso parecía un bug exclusivo de Auto | `MusicService.updateNotification()` extrae el layout a una variable y, tras el `setCustomLayout` global de siempre, hace además `mediaSession.mediaNotificationControllerInfo?.let { mediaSession.setCustomLayout(it, customLayout) }`. Aditivo e idempotente; null hasta que ese controlador conecta. **LECCIÓN: en media3, el overload global de setCustomLayout NO republica el estado legacy — cualquier cosa que deba verse en Android Auto necesita el overload por controlador.** | 0.6.109 | ✅ |
| 45 | **Redmi 14C: cuesta muchísimo iniciar; una canción sí y otra no ("no disponible" cuando SÍ lo está)** — en el móvil del owner todo arranca al instante | 🔴 ABIERTO. **Clase #28/#29/#40.** Sospecha fuerte: es el MISMO bucle purgar-bytes/re-resolver que cazó el audit de 0.6.108 (`currentPlayingMediaId` rancio con crossfade ON → el guard borra los bytes de la canción sonando → re-resolve de ~33s). Que a él le vaya bien y a otros no encaja con: cuenta logueada vs no (dataSyncId, #29), gama baja (Redmi 14C es entrada), o red. **Verificar si 0.6.108 ya lo arregla ANTES de tocar nada** | PENDIENTE — pedir log | — | ✅ 0.6.110 (cifrado) |
| 46 (=#10) | **Plegables: la letra no sale centrada con el dispositivo ABIERTO** (repetido 2 veces por el owner ⇒ le importa) | **NO había NINGÚN tope de ancho en toda la ruta de letras.** El item de línea es `fillMaxWidth()` + 24dp de padding, el `LazyColumn` (`Lyrics.kt:964`) no tenía `horizontalAlignment`, y el `BoxWithConstraints` (~:848) solo leía `maxHeight` — `maxWidth` no se usaba nunca (de ahí el `@SuppressLint("UnusedBoxWithConstraintsScope")` en :228). Con la alineación por defecto **LEFT**, en pantalla ancha cada línea ocupa los ~900dp completos y el texto se pega al borde izquierdo. **Hallazgo que simplifica el fix:** los 3 renderers (`Lyrics.kt`, `EchoMusicLyrics.kt`, `MetroLyrics.kt`) **NO** son pantallas separadas — `echomusicLyricsLine` y `MetroLyricsLine` son composables de ITEM llamados desde el MISMO `LazyColumn`, así que **un solo tope los arregla los tres** | `Lyrics.kt`: `widthIn(max = LYRICS_MAX_WIDTH = 560.dp)` sobre el `LazyColumn`, activado solo con `rememberIsWideLayout()`; el `BoxWithConstraints` padre ya tiene `contentAlignment = TopCenter`, así que centra solo. **INVARIANTE: NO se toca `LyricsTextPositionKey` ni su default** — se limita la COLUMNA, no la alineación, así que la elección LEFT/CENTER/RIGHT del usuario sigue resolviéndose igual, solo que dentro de una medida más estrecha. `widthIn` es no-op por debajo de 560dp ⇒ móvil idéntico | 0.6.110 | ⏳ falta confirmación en dispositivo |
| 47 | **Plegable abierto: el reproductor debería pasar a vista dividida automáticamente (estilo Spotify)** | **Todo existía ya, pero colgaba de la ORIENTACIÓN.** El split player (`Player.kt`, rama `ORIENTATION_LANDSCAPE`) y el nav rail + panel lateral (`MainActivity.kt`) se seleccionaban con `isLandscape`. Un plegable **se usa abierto EN VERTICAL**: ventana casi cuadrada (~700×900) que reporta `ORIENTATION_PORTRAIT` ⇒ caía siempre en el layout de móvil por mucho sitio que hubiera. La rama ancha ya estaba gateada internamente con `isWideLayout`, así que solo faltaba **llegar** a ella | `Player.kt`: el `when(orientation)` pasa a `when { useWidePlayer -> … else -> … }` con `useWidePlayer = isRealLandscape \|\| (isWideLayout && !inVideoMode)`. **Solo cambia el DESPACHO — los dos cuerpos de rama quedan byte-idénticos** (se reusa el código, no se escribe layout nuevo). `MainActivity.kt`: `showRail = (isLandscape \|\| isWideLayout) && …`. **VÍDEO EXCLUIDO A PROPÓSITO del criterio de ancho:** tiene rutas propias por orientación y es la zona más propensa a regresión del archivo (#43, congelación por ciclo de vida de la superficie) ⇒ el vídeo sigue enrutando por orientación real. **Guardas conservadas:** `contentHasRoom >= 560dp` intacta (una ventana ancha sigue negándose a partirse si el panel de contenido quedaría apretado). **Bug latente corregido de paso:** `forceSplitView \|\| rememberIsWideLayout()` hacía **cortocircuito** ⇒ el composable se llamaba condicionalmente (nº de llamadas inestable entre recomposiciones, justo lo que corrompe la slot table); ahora se lee UNA vez y se reusa (`rememberIsWideLayout` ya incluye `ForceSplitViewKey`) | 0.6.110 | ⏸️ sin plegable a mano — no verificable |
| 48 | **Plegable en HORIZONTAL: se pierde toda la interfaz, solo queda la imagen animada** | **DOS causas.** (1) `Player.kt`, dentro del `when(orientation)`, la rama `else if (canvasArtwork != null)` **interceptaba antes** que la rama ancha/split y renderizaba un Box a pantalla completa que contiene **SOLO** una Row de transporte con auto-ocultado a 3,5s — sin portada, título, progreso, cola ni letras. El fondo animado que él veía es la capa de fondo del player (`BackgroundVideoView`), que se dibuja siempre. Reproduce con `playerBackground == APPLE_MUSIC` + canvas activo (vídeo de artista, default true) ⇒ casi todas las canciones. (2) **Fallo estructural:** `rememberIsWideScreen()` y `rememberIsTvOrCar()` devolvían **el MISMO booleano** (ambos `deviceTvCar || forceSplit || ancho>=840dp`) — la separación "layout vs anillo D-pad" existía **solo en los comentarios**. Y 840dp **nunca se alcanza** en el dispositivo que importa: un Galaxy Z Fold desplegado mide ~690×829dp ⇒ todo plegable se quedaba en layout de móvil | `ui/utils/TvUi.kt`: `rememberIsTvOrCar()` = `deviceTvCar || forceSplit` (SIN término de ancho, solo afordancias D-pad) y **nuevo** `rememberIsWideLayout()` = `forceSplit || ancho >= 700dp` (solo layout). 700dp cubre plegables desplegados y tablets pequeñas, y sigue excluyendo móviles en horizontal (~600-690dp). `Player.kt`: la toma de pantalla completa del canvas queda condicionada y pasa a ser **opt-in** con `ImmersiveCanvasOnRotateKey` (default **false**), para que "la interfaz se mantiene en cualquier ángulo" sea cierto también en móvil; la rama de VÍDEO conserva su fullscreen. **Efecto colateral prevenido:** los fondos difuminados dimensionaban su petición Coil con `isTvOrCar`, así que al quitarle el ancho habrían caído de 720px a 100px en tablets → introducido `wantsHiResBackdrop = isTvOrCar || isWideLayout`. **INVARIANTE: no encender el anillo D-pad en un móvil ancho (habría roto TV/Auto, #12/#26) — por eso el desacople va PRIMERO.** | 0.6.109 | ✅ |
| 49 | **El ecualizador debe expandirse según el tamaño de pantalla** | ⚠️ **ÚNICA excepción autorizada a [[never-touch-eq-unless-asked]]: el owner lo pidió EXPLÍCITAMENTE, y SOLO el LAYOUT/UI.** **TRES causas.** (a) `AxionEqScreen.kt` bifurcaba a dos columnas por encima de un `840.dp` hardcodeado, y la columna DERECHA contenía **únicamente `Spacer(height = 60.dp)`** (los "interruptores DSP" que prometía su comentario nunca se cablearon) ⇒ en pantalla grande el EQ quedaba **aplastado en media pantalla con la otra mitad EN BLANCO — peor que el layout estrecho**. (b) la fila de bandas era `horizontalScroll` con cada `EqBandSlider` en un `Column(width(46.dp))` fijo y sin `weight` ⇒ ancho intrínseco **constante ~502dp**, así que el EQ **seguía haciendo scroll horizontal** en una pantalla enorme. (c) alturas fijas que nunca crecían (curva 130dp, gráfico PEQ 220dp, recorrido de slider 200dp) | `AxionEqScreen.kt` **SOLO** (nada bajo `eq/`). (a) Se **elimina** la bifurcación de dos columnas → **una sola columna centrada** con `widthIn`-equivalente (`EQ_MAX_CONTENT_WIDTH = 900.dp`). Elegido frente a "rellenar la columna derecha" porque rellenarla exige **inventar UI nueva** y re-ubicar controles (la petición era de tamaño), `EqMainContent` ya es una extensión `ColumnScope` escrita para exactamente una columna, y así se **borra** una ruta duplicada en vez de añadir otra. (b) Cuando el ancho disponible ≥ el intrínseco, se quita el scroll y cada banda pasa a `weight(1f)`; **por debajo, el scroll de hoy queda EXACTAMENTE igual**. El nº de bandas sale SIEMPRE de `EqConstants.BAND_COUNT` (nunca literal) y el intrínseco se **calcula** a partir de él. (c) Alturas escaladas desde el `maxWidth` medido vía `eqVerticalScale()`, **con techo `1.45f`** — crecen con criterio, nunca "gigante" (#50). `840.dp` sustituido por `rememberIsWideLayout()`. **CÓDIGO MUERTO BORRADO** (cero call sites verificados con grep en todo el repo): `CircularEqControl.kt` completo + el cluster `PeqEditorCard`→`PeqBandRow`→`PeqNumberField` (estaba marcado `@Suppress("unused")` y "no wired in any more"). `PeqTypeSelector` y `formatHz` **SÍ** están vivos y se conservan. **INVARIANTE: NADA bajo `eq/` tocado** — ni `EqConstants` (BAND_COUNT=10, FREQUENCIES, Q), ni `FilterType`, presets, ganancias, `EqualizerService`, `EQProfileRepository`, `BiquadFilter`, `CustomEqualizerAudioProcessor` ni `SuperpoweredBridge.cpp` | 0.6.110 | ⏸️ sin plegable a mano — no verificable |
| 50 | **Apariencia con el plegable desplegado: "muy brusco, todo lo pone gigante"** | **Causa dominante localizada: la portada del reproductor NO tenía tope en VERTICAL.** `ui/player/Thumbnail.kt` `calculateThumbnailDimensions` calculaba `effectiveSize = containerWidth - 2*padding` **sin `coerceAtMost`** ⇒ en una ventana de ~700-930dp la carátula se convertía en un cuadrado de ~650-880dp. (El horizontal ya hacía `minOf(width, height)`, por eso solo se notaba abierto en vertical.) Contribuye también que el contenido de una columna se estiraba de borde a borde sin medida legible | `Thumbnail.kt`: nuevo parámetro `maxThumbnailSize` (default `Dp.Unspecified` = comportamiento viejo intacto) + `WIDE_MAX_THUMBNAIL_SIZE = 420.dp`, aplicado **solo** con `rememberIsWideLayout()` ⇒ móvil byte-idéntico. **Se topa SOLO `thumbnailSize`; `itemWidth` sigue siendo el ancho completo**, así que el tamaño de página del pager horizontal y el snapping no se tocan y la carátula más pequeña simplemente se centra en su página. `AxionEqScreen.kt` aporta la columna centrada de 900dp (ver #49). **NO se añade escalado de tipografía por breakpoint** — la queja es que las cosas se ven demasiado GRANDES, no demasiado pequeñas, así que ningún cambio agranda nada. **Límite honesto:** el helper opcional `Modifier.wideContentWidth()` para Inicio/Biblioteca/Ajustes **NO se aplicó**: `HomeScreen.kt` es fuente de regresión conocida (#2, carrusel) y la causa verificada es la portada ⇒ mejor confirmar esta en dispositivo antes de ampliar | 0.6.110 | ⏸️ sin plegable a mano — no verificable |
| 51 | **Volumen Seguro: el owner no sabe si funciona** | 🔴 ABIERTO — **VERIFICACIÓN, no bug confirmado.** Comprobar que la normalización+limitador opt-in hace algo audible y medible. Ojo al antecedente: [[audio-dsp-current-reality-and-safe-volume]] (los procesadores viejos quedaron como stubs muertos) — puede ser placebo otra vez | PENDIENTE | — | ⚠️ parcial (+3 dB) |
| 52 | **Los botones inferiores (inicio/búsqueda/biblioteca/micrófono) tienen animación TRABADA, no fluida** | 🔴 ABIERTO. Jank en la barra de navegación. Sospechas: recomposición cara en el destino, trabajo en el hilo principal al navegar, o la animación compite con carga de datos | PENDIENTE | — | ⏳ falta confirmar en tu móvil |
| 53 | **La cola infinita falla: a veces funciona, a veces no** | 🔴 ABIERTO. **Reincidente** (#22/#23/#25/#34/#39/#41). "A veces" ⇒ depende de estado/carrera, no de lógica pura. **Verificar si 0.6.108 lo cambia** (carril de género + seeding) antes de tocar | PENDIENTE | — | ✅ 0.6.109 |
| 54 | **Carro L200 2025: tras uso largo la miniatura no se actualiza hasta media canción** | **Mecanismo de publicación localizado con certeza** (bytecode media3): `ControllerLegacyCbForBroadcast.updateMetadataIfChanged` publica el `MediaMetadataCompat` **con bitmap null de inmediato** y republica CON carátula solo cuando el future del bitmap completa. Es decir: título/artista salen al instante y la miniatura llega cuando termina `CoilBitmapLoader.loadBitmap()` ⇒ "no se actualiza hasta media canción" = ese future tardó ~60-90s. **Contribuyentes reales:** (1) `CoilBitmapLoader` **sin NINGÚN timeout** (la rama Coil no está acotada; el fallback HTTP son 10s+10s); (2) se descargaba y decodificaba **1200×1200** (~5,7 MB ARGB_8888) para un icono que se recorta a **384** — el `artworkUri` de sesión es `resize(1200,1200)` y el `ImageRequest` no llevaba `.size()` ⇒ ~10× los bytes por aire + 3 asignaciones grandes por pista, con la app en segundo plano; (3) fugas: `playerSilenceProcessors` recibía una entrada por ExoPlayer creado pero **no se limpiaba** en `cleanupCrossfade` ni en el error del player secundario ⇒ una entrada muerta por crossfade reteniendo un ExoPlayer ya liberado durante todo el viaje | `CoilBitmapLoader`: `.size(MAX_ARTWORK_PX)` en el `ImageRequest` (la UI in-app NO se ve afectada — `Thumbnail.kt` re-aplica su propio `resize(1200,1200)`) + `withTimeoutOrNull(ARTWORK_LOAD_TIMEOUT_MS = 6s)` sobre TODO el load (una carátula que falta es mucho menos malo que una carátula rancia). `MusicService`: `playerSilenceProcessors.remove(it)` añadido en `cleanupCrossfade` y en el `onPlayerError` del secundario. **INVARIANTE #26 reforzado, no revertido: `MAX_ARTWORK_PX` sigue en 384 y el cap de browse de 300 no se toca.** **Pendiente si reincide:** la cola del player crece de forma monótona (nunca se poda la cabeza) y media3 la parcela ENTERA a AA en cada cambio de timeline — misma clase que #26 por otra puerta; requiere log del dispositivo antes de tocarla (territorio #22/#23/#27). | 0.6.109 | ✅ (parcial: podar la cola queda pendiente de log) |
| 55 | **Redmi Note 13 Pro+ 5G: batería se agota MUY rápido y calienta** | **Dos bucles INCONDICIONALES, vivos toda la sesión** (idénticos en 0.6.107 publicado): (1) `startWidgetUpdates` hacía `updateWidgetUI` **cada segundo** durante toda la sesión **tengas o no widgets puestos** — cada tick abanica a varios round trips binder (`getAppWidgetIds` por tipo, `getAppWidgetOptions`, `updateAppWidget` parcelando un RemoteViews completo) y el widget de playlist añade **cinco consultas Room**, incluida una de más-reproducidas **sin índice** (full scan). Para la mayoría de usuarios (sin ningún widget) era 100% desperdicio, cada segundo, **con la pantalla apagada**. (2) El save de cola de 30s **no tenía gate de `isPlaying`** y serializaba **TRES** grafos de objetos completos con `ObjectOutputStream` aunque no hubiera cambiado nada, para siempre. (3) Fuga de `playerSilenceProcessors` en cada crossfade (ver #54) | `MusicService.startWidgetUpdates`: sondea `widgetManager.hasAnyWidget()` cada ~30 ticks y **se salta el cuerpo entero** si no hay ninguno (nuevo `EchoMusicWidgetManager.hasAnyWidget()` + `PlaylistWidgetManager.hasAnyWidget()`, ambos con `getOrDefault(true)` — ante la duda, seguir actualizando: nunca congelar un widget real). Save de 30s: nuevo `queueSignatureChanged()` (count:index:currentId leído en Main, barato) → solo guarda si la cola cambió de verdad; **se mantiene sin gate de `isPlaying` A PROPÓSITO** (una cola editada en pausa debe persistir). **Pendiente:** el resto de contribuyentes catalogados (backdrops animados sin gate de `isPlaying`, workers sin `requiresBatteryNotLow`, prefetch de vídeo sin cap tras el primer uso) siguen abiertos. | 0.6.109 | ✅ (parcial) |
| 56 | **Xiaomi 14 CE: se queda pegada a cada rato, sale "no responde" (ANR)** | **CAUSA PRINCIPAL:** `rememberPreference`/`rememberEnumPreference` (`utils/DataStore.kt`) pasaban `context.dataStore[key]` como valor semilla de `collectAsState` **FUERA de `remember`**. Esa lectura es un `runBlocking { data.first() }` que atraviesa el actor de DataStore y **espera detrás de cualquier `edit{}` en vuelo**. Al no estar memorizada corría en **CADA recomposición**, y con **~560 call sites** en la app (46 en una sola pantalla de ajustes, 33 en `Lyrics.kt`, 31 en `MainActivity.kt`) un único fotograma podía apilar decenas de lecturas bloqueantes de disco en el hilo principal. Eso es literalmente la definición de ANR | `utils/DataStore.kt`: la semilla va dentro de `remember { }` en ambas funciones. Semánticamente idéntico — la semilla solo importa hasta que el Flow emite, y el Flow ya estaba `remember`ado sin keys, así que ahora ambos van en paralelo. **Cambio de una línea por función, efecto en ~560 sitios.** **Probablemente también mitiga #52 (animación trabada).** **Pendiente (catalogado, no hecho):** deserialización de la cola persistente en `Service.onCreate` (hilo principal, 3× `ObjectInputStream`) — agravado en MIUI, que mata el servicio constantemente (#26) y obliga a repetirlo; y `saveQueueToDisk(synchronous=true)` en `onDestroy`. **Requiere traza real:** `adb shell dumpsys appexit iad1tya.echo.music`, o Crashlytics (sabor gms reporta ANR automáticamente). | 0.6.109 | ✅ (causa principal; quedan 2 rutas catalogadas) |
| 57 | **Las canciones "se adelantan": cortan microsegundos y saltan un poco, de forma aleatoria** | 🔴 ABIERTO. **Muy sospechoso de ser el mismo bug de #45/#55**: si el guard purga los bytes y re-resuelve a mitad de canción, el player re-abre la fuente en otra posición → micro-corte + salto. **Puede que 0.6.108 ya lo arregle** | PENDIENTE | — | 🔴 |
| 58 | **(=#43) El vídeo se friza, el audio sigue; salir y volver NO recupera** | Duplicado de #43 — ver esa fila: causa probada (setVideoTextureView NO es no-op + PlayerConnection recreado en cada rebind) y resuelta. | Ver #43 | 0.6.109 | ✅ |

| 59 | **MI ERROR (BLOQUEANTE, cazado por el audit): TODO el trabajo de plegables era un NO-OP** | `WindowSizeClass.isWidthAtLeastBreakpoint(int)` **solo conoce los cortes 0/600/840** y redondea cualquier valor propio HACIA ABAJO al cubo inferior (verificado desensamblando `androidx.window:window-core-android:1.5.0` + `material3.adaptive:1.3.0-alpha09`: los inicializadores estáticos de `DpWidthSizeClasses` solo tienen esos buckets). Por tanto pedir **700 es aritméticamente idéntico a pedir 840**. Un Z Fold desplegado mide ~690×829dp → cae en el cubo **600** → `>= 700` seguía siendo `false`, exactamente igual que `>= 840`. **El número que el propio commit citaba como justificación era el que probaba que no funcionaba.** Consecuencia: el cap de letras (#46), el split player (#47), el cap de portada (#50) y el nav rail seguían APAGADOS en el dispositivo objetivo — mientras que `rememberIsTvOrCar()` **sí** perdió su término de ancho. Balance neto: **cero mejora de layout + una regresión real de TV** | `TvUi.kt`: leer el ancho REAL con `LocalWindowInfo.current.containerSize.width` convertido a dp y comparar contra `WIDE_LAYOUT_BREAKPOINT_DP = 600` (el corte "medium" de Material y la línea clásica sw600dp). **LECCIÓN: una API de *size class* es por CUBOS — nunca sirve para expresar un umbral propio. Si el valor que pasas no es exactamente un límite de cubo, estás pidiendo otra cosa sin saberlo. Y un comentario que cita un número concreto (690dp) para justificar un umbral (700) merece que se compruebe la aritmética: aquí el propio comentario contenía la refutación.** | 0.6.109 | ✅ |
| 60 | **MI ERROR (BLOQUEANTE): mover `radioSeedInFlight` dentro de la corrutina causaba DOBLE SIEMBRA determinista** | Lo moví dentro del `scope.launch` para arreglar una fuga real (un launch sobre un scope ya cancelado nunca ejecuta el cuerpo → el `finally` tampoco → el flag se quedaba pegado bloqueando TODAS las re-siembras). Pero `scope` es `Dispatchers.Main` **no-inmediato**, así que `launch` SIEMPRE encola y la escritura del flag cae en un mensaje POSTERIOR: el guardia queda **sin reclamar durante el resto del dispatch actual**. Y el `ListenerSet` de media3 entrega todos los callbacks de una misma actualización del player **en un único flush síncrono** → `onMediaItemTransition` (pre-seed B3) y `onEvents` (`scheduleCrossfade` → seed) ven ambos `false` y **ambos disparan**. La segunda siembra ejecuta `appendSeed`, cuyo `removeMediaItems(liveIndex+1, itemCount)` **borra la cola que la primera acababa de añadir**, con esos ids ya quemados en `sessionPlayedIds` por el filtro no-repetición (#22) → **perdidos para siempre**; además arma el crossfade dos veces contra un índice que la segunda elimina | Reclamo **SÍNCRONO** otra vez (que es la función del guardia) + liberación con `seedJob.invokeOnCompletion{}`, que **sí se ejecuta aunque el cuerpo nunca arranque** — resuelve la fuga original sin abrir la ventana de carrera. **LECCIÓN: un guardia de exclusión debe reclamarse en el mismo turno del hilo que lo comprueba. Con un dispatcher no-inmediato, mover la escritura dentro de una corrutina la traslada a otro turno y el guardia deja de existir para todo el flush actual — que es exactamente cuando media3 dispara sus callbacks.** | 0.6.109 | ✅ |
| 61 | **Carátula en blanco CACHEADA + respaldo HTTP inalcanzable** (audit de mi fix del #54) | (a) Al agotar el timeout devolvía `createFallbackBitmap()` = un 64×64 **totalmente transparente**. media3 envuelve el loader en `CacheBitmapLoader`, que memoiza el último par `(uri → future)` → ese blanco quedaba **cacheado para esa carátula** y el coche/pantalla de bloqueo mostraba un recuadro vacío **toda la canción**, en vez de conservar la portada anterior. (b) El tope de 6s envolvía TODO el load, incluido el respaldo HTTP directo que el mismo archivo permite 10s+10s → ese respaldo era **inalcanzable por construcción**, justo el camino que existe para cuando Coil no arranca en el contexto del MediaSession | Al timeout se **lanza** `TimeoutException` (media3 conserva la metadata previa) y el presupuesto sube a **22s** para que el respaldo quepa. **LECCIÓN: devolver un placeholder donde el consumidor CACHEA el resultado convierte un fallo transitorio en permanente; fallar es mejor que mentir.** | 0.6.109 | ✅ |
| 62 | **La firma de cola se perdía las REORDENACIONES** (audit de mi fix del #55) | `"count:index:currentId"` no cambia cuando el usuario **arrastra para reordenar** pistas próximas por debajo del índice actual (`player.moveMediaItem`) — ni en quitar-uno-y-añadir-otro, ni en un reemplazo en sitio. El KDoc afirmaba que "toda mutación cambia count, index o id": **falso**. Escenario perdido: usuario en PAUSA reordena la cola, pantalla apagada, MIUI mata el servicio (#26) → no hay transición ni cambio de estado que persista → **orden perdido**. Justo el caso que el save sin gate de 30s decía proteger | Flag sucio `queueDirty` puesto desde un `onTimelineChanged` nuevo — media3 lo dispara para add, remove **y `moveMediaItem`**. **LECCIÓN: un hash de forma solo detecta lo que sus campos representan; para "¿cambió algo?" usar la señal que el framework ya emite, no una huella inventada.** | 0.6.109 | ✅ |

| 63 | **PLACEBOS DETECTADOS EN MIS PROPIOS FIXES** (caza dedicada, 44 agentes, pedida por el usuario: *"cero placebos"*) | Tres "arreglos" compilaban, parecían correctos y **no cambiaban nada perceptible** — y uno era ACTIVAMENTE PEOR. (a) **Carátula del carro (#54):** `.size()` de Coil acota el DECODE, no la descarga — el fetcher pide la URL tal cual, y el `artworkUri` de sesión ya trae `=w1200-h1200`, así que por el aire iba el MISMO JPEG; en un enlace lento la latencia ES la descarga, o sea que esa mitad era inerte. Y el timeout de 22s **no podía funcionar**: el `runCatching` de la fase Coil captura `Throwable` → se tragaba la `TimeoutCancellationException` → caía al fallback bloqueante (HttpURLConnection + BitmapFactory, sin puntos de suspensión, **no interrumpible** por una corrutina), corría los 10+10s enteros y **descartaba una portada ya descargada**. Peor aún: fallar el future **NO restaura la carátula anterior** (verificado en el bytecode de media3 1.10.1: `MediaSessionLegacyStub` publica la metadata con bitmap NULL de inmediato y su `onFailure` solo loguea) → expirar **garantizaba que la portada correcta no llegara nunca**, que en una pantalla de coche que conserva su último bitmap se ve EXACTAMENTE como el bug reportado. (b) **Letra en plegables (#46):** capar el ancho no centra nada visible — el Box padre ya centraba la COLUMNA, pero con la alineación LEFT por defecto cada línea corta se pega al borde inicial de esa columna. (c) **`ImmersiveCanvasOnRotateKey`:** la condición incluía `!isWideLayout` y un plegable ES ancho → un ajuste nuevo, visible y traducido que **no hacía nada** en el hardware al que se ofrecía | (a) reescribir la URL con el `resize()` propio de la app (googleusercontent → `=w384-h384`, i.ytimg → sddefault) en la ruta Coil **y** en el fallback HTTP; timeout **eliminado** (lento es mejor que nunca). (b) en pantalla ancha, centrar el TEXTO cuando el ajuste sigue en su valor por defecto. (c) quitar `!isWideLayout`: el opt-in es la puerta. **LECCIÓN: "compila y parece correcto" no es un arreglo. Todo fix debe trazarse hasta el efecto que el usuario percibe — ¿se ejecuta ese código?, ¿en SU dispositivo?, ¿el valor llega a lo que produce el efecto? Y un timeout dentro de un `runCatching` no acota nada.** | 0.6.109 | ✅ |
| 64 | **#45 (Redmi 14C) — dos contribuyentes reales arreglados, la causa raíz sigue abierta** | (a) La ruta REAL de resolución no tenía el fallback `?: visitorData` que `prewarmPoToken` sí tenía: una cuenta logueada con `dataSyncId` null (registro #29 — solo se deriva en el login) daba `sessionId == null` → **ningún poToken** para los clientes que lo requieren → esos clientes fallan y el resolve quema su presupuesto de 30s saltando entre ellos → al usuario le sale "canción no disponible". (b) `skipOnError` sumaba **2** contra `MAX_CONSECUTIVE_ERR = 5` → el presupuesto real eran **2 saltos**, así que **TRES canciones irresolubles seguidas paraban la reproducción** — literalmente "una canción sí y otra no, y luego se para" | (a) `YTPlayerUtils` sessionId con fallback en la ruta real; (b) `+= 1`. **ABIERTO:** el tope de 30s de `boundedResolve` que convierte un TIMEOUT en "no disponible" sigue sin tocarse — es la causa raíz y depende de la velocidad del dispositivo. | 0.6.109 | ✅ causa real = #65 |

| 65 | **🔴 CAUSA RAÍZ REAL de "canción no disponible" / arranque lento: YouTube ROTÓ su reproductor** (log del dueño, 18-jul) | `No hardcoded config for hash: 882a803a` + `Could not extract signature function info from player JS` → **sin descifrar, NINGUNA canción resuelve**. La tabla interna trae 13 hashes y el `player_configs.json` publicado solo cubría `66a6ea83`/`2afc9693`. **Corrige mi diagnóstico de la fila 62/#45: el tope de 30s NO era la causa, era el SÍNTOMA** — el resolve se quedaba sin presupuesto intentando descifrar. Por eso salían mezclados "tardó demasiado", "no está disponible" e "inicia sesión para confirmar que no eres un bot". **NO era del Redmi: afectaba a todos.** | Config derivada comparando con `66a6ea83` (config buena): el sitio de firmado es estructuralmente idéntico, solo rotaron los identificadores — `C3(14,932,f3(21,4296,s))`→`WB(27,5330,Vk(38,2047,s))`, `ED`→`qV`, `g.WM`→`g.yk`, sts 20640→20648. Verificado antes de publicar: cada función tiene **una sola** definición top-level (las otras `Vk` son métodos de prototipo), y los roles coinciden (`Vk`=decodeURIComponent como `f3`, `qV`=encodeURIComponent como `ED`, `WB`=descifrador como `C3`). **LECCIÓN: cuando fallen MUCHAS canciones a la vez, mirar el cifrado ANTES de culpar a timeouts o al dispositivo. El auto-reparado existe justo para esto y arregla a todos sin publicar APK.** | remoto | ✅ **CONFIRMADO EN DISPOSITIVO** (2026-07-20: "la musica ya reproduce") |
| 66 | **Aleatorio no podía QUEDARSE encendido** (reporte: "el modo aleatorio no funciona") | El azar nunca estuvo roto (`applyShuffleOrder` genera una permutación válida y todos los saltos usan APIs shuffle-aware). Lo roto: `playQueue()` limpiaba `shuffleModeEnabled` en toda cola nueva — **incluida la RESTAURACIÓN al abrir la app** — y ese borrado programático disparaba `onShuffleModeEnabledChanged(false)`, que **persistía `false` en `ShuffleModeKey`**, destruyendo la preferencia del usuario. Los dos ajustes se contradecían por defecto: recordar=ON + persistir-entre-colas=OFF ⇒ garantizado que se olvida. Huella: `previousShuffleEnabled` declarada y **jamás leída**, muerta desde el primer commit del archivo — la línea que restauraba nunca se escribió | Gate `!isRestore` + flag `suppressShuffleModePersist` para no confundir el reseteo propio con una acción del usuario. **LECCIÓN: una variable declarada y nunca leída es la firma de un arreglo a medio escribir — vale la pena buscarla.** | 0.6.110 | ✅ |
| 67 | **MI ERROR: el fix de la fila 66 introdujo re-barajado de una cola que ibas en orden** | Al suprimir la escritura, `ShuffleModeKey` pasó a guardar el último TOGGLE, no el estado vivo → aleatorio ON en lista A, empiezas lista B (correctamente sin aleatorio), la oyes en orden, matas la app → al volver leía un `true` viejo y **devolvía B BARAJADA**. Además quedaron alcanzables dos bugs de índice: (a) `appendSeed` truncaba con `removeMediaItems(liveIndex+1, itemCount)` — índice de TIMELINE, y bajo aleatorio eso es una porción arbitraria: empezar radio a mitad de una cola barajada de 50 **borraba ~46 canciones sin oír**; (b) el disparador de autocarga medía `mediaItemCount - currentMediaItemIndex <= 5`, también timeline → paginaba antes de tiempo ~1 de cada 10 transiciones | Restaurar el aleatorio del snapshot POR COLA (`PersistPlayerState.shuffleModeEnabled`, que ya se escribía y **se tiraba sin leer**); truncar solo sin aleatorio; medir "casi al final" con `getNextWindowIndex()`, que respeta el orden real. **LECCIÓN: al cambiar QUÉ se persiste, comprobar si esa clave sigue siendo la fuente correcta al restaurar — y si el dato correcto ya se estaba guardando sin usar.** | 0.6.110 | ✅ |
| 68 | **MI PLACEBO: el fix de WorkManager no arregla lo que dije** | Diagnostiqué que 3 features morían (Release Radar, Last.fm, buscar actualizaciones). La auditoría lo refutó: `InitializationProvider` está intacto y `WorkManagerImpl.getInstance` solo lanza esa ISE si NO hay instancia **y** el contexto no es `Configuration.Provider` — imposible en el proceso por defecto, donde los providers se instalan ANTES de `Application.onCreate`. Prueba independiente: el mismo log muestra `UpdateCheckWorker` **ejecutándose** ("Job was cancelled"), lo que exige que el proceso por defecto sí lo encolara. Los fallos venían de `:crash`/`:phoenix` | El cambio es correcto y no regresiona (silencia ruido en procesos auxiliares), pero **NO restaura ninguna función**: ya se programaban bien. **Si el usuario reporta que no corren, la causa sigue ABIERTA** (probable: MIUI matando el proceso, #26). **LECCIÓN: un stacktrace en `onCreate` no implica el proceso principal; contar los fallos y buscar evidencia de que la función SÍ corre desmiente la teoría.** | 0.6.110 | ⚠️ sin efecto real |
| 69 | **ABIERTO: sesión de YouTube caducada no se recupera sola** | La rotación de sesión está condicionada a `cookie == null` (solo invitados). Un usuario logueado con cookie muerta **no tiene ninguna vía de vuelta**: cada canción quema el bucle de 12 clientes hasta los 30s y sale como "no disponible". Nada detecta el fallo repetido (`forgetAccount` es manual). **Independiente del cifrado**: `playabilityStatus` lo decide el servidor ANTES de firmar | Arreglo correcto identificado: enhebrar `noLogin` por `InnerTube.player`→`YouTube.player`→`playerResponseForPlayback` y sus 7 llamadas, copiando el patrón `browse(noLogin)` que ya existe. **El atajo —anular temporalmente `YouTube.cookie` global— se escribió y se REVIRTIÓ**: dos resoluciones concurrentes (principal + precarga del crossfade) intercalan guardar/restaurar y pueden dejar la cookie en null → **deslogueo silencioso**. No se envía sin probar. Matchear por `playabilityStatus.status` (`LOGIN_REQUIRED`…), NUNCA por el texto, que YouTube localiza. | — | ✅ 0.6.111 |

| 70 | **🔴 MI BLOQUEANTE: el fix de "sin subidas" podía BORRAR la marca de subido de TODA la biblioteca** | Convertí un `throw` en "página vacía" y escribí en el commit que NO era genérico. **El código no cumplía lo que decía el commit**: `library()` miraba solo `contents[0]` y devolvía vacío para cualquier sección que no fuera grid/shelf. Como el Json es `ignoreUnknownKeys + explicitNulls=false`, **CUALQUIER renderer no modelado** (messageRenderer, aviso de "inicia sesión", promo, un layout futuro) deserializa a un Content con los 9 campos null → caía en esa rama. Y el propio repo ya documenta en `fetchNewReleaseAlbums` que la sección de datos **no siempre es la primera**. Aguas abajo, `SyncUtils` compara remoto vs local y llama `toggleUploaded()` a todo lo ausente → **desmarcaba la biblioteca entera con solo abrir Subidas con sync activo**. Las copias de seguridad solo cubren biblioteca ⇒ irrecuperable | Doble barrera: (a) `library()` escanea TODAS las secciones y solo reporta vacío si la lista de secciones está genuinamente vacía — lo no reconocido **sigue lanzando**; (b) `SyncUtils` se niega a ejecutar el desmarcado si el remoto viene vacío y hay locales. **LECCIÓN: si el commit dice "deliberadamente NO genérico", verificar que el código lo cumpla — y ante pérdida de datos, poner la barrera también en el CONSUMIDOR, no solo en el parser.** | 0.6.111 | ✅ |
| 71 | **MI PLACEBO: la recuperación de sesión caducada no recuperaba nada** (3 motivos independientes) | (a) `noLogin` quitaba la cookie pero `toContext` **seguía enviando el `dataSyncId`** como `user.onBehalfOfUser` → el reintento "anónimo" **seguía nombrando la cuenta** de la que huía; copié la mitad del patrón `browse(noLogin)`, que sí lo anula. (b) El flag era un **campo de objeto compartido** mientras las resoluciones se **solapan de verdad** (loader de media3, bucle de PRECARGA de cola, descargas, export, preview): el reset de una precarga borraba rutinariamente el resultado de la canción en curso antes de leerlo. (c) `AGE_CHECK_REQUIRED`/`CONTENT_CHECK_REQUIRED` en el set de disparo, pero `isLoggedIn` sale de `YouTube.cookie` (sigue puesta) → el reintento recaía en la rama `WEB_CREATOR` (que exige login) y **nunca llegaba** a `TVHTML5_SIMPLY_EMBEDDED_PLAYER`, que existe justo para eso ⇒ **30s extra garantizados** | (a) `toContext(..., if (noLogin) null else dataSyncId)`; (b) `AtomicBoolean` por llamada enhebrado por `resolvePlaybackData`; (c) set reducido a `LOGIN_REQUIRED` — la edad es propiedad del CONTENIDO, no de una cookie muerta. **LECCIÓN: al copiar un patrón existente, copiarlo ENTERO; y un flag compartido en un `object` es siempre sospechoso donde hay precarga concurrente.** | 0.6.111 | ✅ |
| 72 | **MI FUGA: el commit que cerraba la fuga de datos ABRIÓ otra igual** | Añadí `Timber.w("... for query: $query")` donde `query` = `"$titulo $artista"`. `AppLogger.plant()` corre **sin guard en release** y `FileTree` acepta `>= INFO` ⇒ acababa en `filesDir/logs/app.log`, **el archivo que el usuario lee y comparte** desde Ajustes → Registros. Exactamente la clase de fuga que ese mismo commit venía a cerrar | Solo el status HTTP. **Contexto:** el barrido quitó **42** printlns (no ~15) que en RELEASE volcaban títulos, artistas, IDs y **cuerpos de respuesta completos**. **LECCIÓN: en este repo `Timber.w` NO es solo logcat — persiste a un archivo compartible. Antes de loguear una variable, preguntar si contiene datos del usuario.** | 0.6.111 | ✅ |
| 73 | **Cortacircuitos de letras mal calibrado: silenciaba proveedores SANOS** | `400..499` genérico. Pero YouTube Subtitles y YouTube Lyrics corren sobre un cliente `expectSuccess` y `get_transcript` responde 4xx para **cualquier vídeo sin panel de transcripción** ⇒ **una sola canción sin subtítulos mataba ese proveedor para toda la biblioteca durante 6h** | Solo `401/403/429`: describen al QUE LLAMA. `400/404/410` describen al RECURSO. **LECCIÓN: al agrupar códigos de error, separar "el problema soy yo" de "el problema es este recurso concreto".** | 0.6.111 | ✅ |

| 74 | **MI PLACEBO #3 (mismo bug #57, segundo intento fallido): quitar el purgado global NO causa ni evita el salto** | Culpé al `performAggressiveCacheClear` incondicional de `onPlayerError`. **Refutado desde el bytecode de media3 1.10.1:** `handleIoException → stopInternal → resetInternal → disableRenderers() → MediaCodecAudioRenderer.onDisabled() → audioSink.flush()` corre **en el hilo interno de reproducción ANTES** de postear el error al hilo de la app ⇒ el AudioTrack **ya está vaciado** cuando mi código se ejecuta; nada de lo que haga ahí puede causar un vaciado que ya ocurrió. Refutación independiente: `SimpleCache.removeResource` solo hace unlink de ficheros, `CacheDataSource` **no** es `Cache.Listener`, y un FD ya abierto **sobrevive al unlink POSIX** ⇒ solo afecta al SIGUIENTE open, al mismo offset, en el hilo del loader. Y la fila #40 lo corrobora: esa misma llamada sobre la canción en curso produjo **stalls de 33s**, nunca un salto adelante | Mantengo la eliminación (el purgado forzaba re-descarga completa ante errores pasajeros y borraba ficheros **en el hilo principal** bajo el monitor de `SimpleCache`) pero **como mejora de rendimiento, NO como fix de #57**. **LECCIÓN: antes de atribuir un síntoma de audio a código propio, verificar en qué HILO y en qué ORDEN ocurre respecto al framework. Un flush que ya pasó no lo causa nada posterior.** | 0.6.112 | ✅ (pero #57 sigue abierto) |
| 75 | **MIS 2 REGRESIONES por quitar el purgado: bytes corruptos que ya no se borran, y URL muerta reintentada igual** | Dos ramas dependían **en silencio** del purgado global. (a) `CONTAINER_MALFORMED` (3001) y `READ_POSITION_OUT_OF_RANGE` (2008) significan **bytes malos**, pero `isCacheOrStreamCorruptionError` enruta a `handleExpiredUrlError`, que **los conserva a propósito** (correcto para un 403, incorrecto aquí) ⇒ el resolver re-servía **los mismos bytes corruptos** en cada reintento: una canción que antes hipaba una vez y se curaba pasaría a tartamudear sus 3 reintentos y **saltarse, en cada reproducción**, hasta usar "volver a obtener". (b) La rama `isNetworkRelatedError` —cuyo propio comentario diagnostica "URL descifrada MUERTA"— reintentaba entrando al resolver, que **reutiliza `songUrlCache`** mientras dure su TTL ⇒ 3 reintentos contra la MISMA URL muerta y salto, donde una resolución fresca habría sonado | (a) purgado real antes de re-resolver en la rama de corrupción; (b) `songUrlCache.remove(mediaId)` en la rama network-like (los BYTES se quedan: una URL muerta no dice nada del audio ya bajado). **LECCIÓN: al quitar una acción global, listar qué ramas la usaban SIN pedirla — lo redundante y lo load-bearing se ven igual hasta que lo quitas.** | 0.6.112 | ✅ |
| 76 | **PENDIENTE — candidato REAL de #57: el `throw` deliberado del guard de formato** | `MusicService` lanza `ERROR_CODE_PARSING_CONTAINER_MALFORMED` **a propósito** ("Throwing to force player restart") cuando una re-resolución a mitad de canción devuelve un contenedor distinto al de la fila cacheada ⇒ **un corte audible por ocurrencia**. El propio comentario dice que esta ruta **solo se volvió alcanzable para pistas CON FUNDIDO en 0.6.108**. Dispara cuando la calidad preferida (Lossless/Saavn) **cae a Opus** a mitad de canción | **NO TOCAR sin saber la calidad de audio configurada del usuario:** con Opus (por defecto) el guard **no puede** dispararse y #57 sería otra cosa; con Lossless/Saavn dispara cada vez que hay caída de formato. Descartado ya: SponsorBlock (hace un `seekTo` adelante cada segundo, pero viene **apagado** y el usuario confirmó no ver el interruptor) y `instantSilenceSkip` (hardcodeado a false). | — | 🔴 abierto |
| 77 | **`YTPlayerUtils.forceRefreshForVideo` es un STUB VACÍO** (un solo `Timber.d`) | Todo comentario que dice "limpia las cachés de descifrado" **miente**: `performAggressiveCacheClear` es en la práctica `songUrlCache.remove` + `removeResource`; el tercer paso no hace nada. Estaba sosteniendo el razonamiento de varios fixes | Documentado, **no arreglado**: el cifrado acaba de repararse y funciona; tocar su invalidación ahora es riesgo sin necesidad. **LECCIÓN: un helper con nombre descriptivo puede estar vacío — verificar el cuerpo antes de apoyarse en él para justificar un cambio.** | — | 🔴 abierto |

| 78 | **MI PLACEBO #57 (3er intento, parcial): el guard de formato solo se arregló en el ARRANQUE, no a mitad de canción** | Con la pista clave del usuario (estaba en Lossless/Saavn, no Opus), ataqué el `throw` deliberado del guard de formato. Encontré el límite exacto por bytecode: `position != 0` ⟺ el extractor YA está comprometido ⟺ el `throw` está justificado; en `position == 0` (arranque de pista) el extractor re-analiza esos bytes y el `throw` es daño puro (error fatal + re-prepare = un corte al empezar la canción). Lossless hace una **búsqueda difusa externa con 9s de tope**, nodeterminista por intento → el mismo tema sale flac ahora y Opus luego = el "de manera random". **PERO la auditoría demostró que una re-resolución a MITAD de canción SIEMPRE reabre en offset ≠ 0**, así que el `throw` sigue disparando para el caso reportado (corta + salta adelante). Mi cambio solo quita el corte de ARRANQUE | Gate `dataSpec.position != 0L` (arreglo real del corte de arranque en Lossless/Saavn). **#57 sigue ABIERTO para el caso a mitad de canción** — no lo cierro. **LECCIÓN: un límite `A ⟺ B` hay que probar las DOS direcciones; "position 0 ⇒ no comprometido" es cierto, pero "no comprometido ⇒ position 0" NO, y confundirlas hace creer que cubres más de lo que cubres.** Confirmación: logcat en Lossless durante un corte — las líneas "Format fallback detected AFTER fetch" aparecen o no. | 0.6.113 | ⚠️ parcial, #57 sigue abierto |
| 79 | **MI TRAMPA: mi hardening `codecs = ""` RECREABA el mecanismo de #57** | Al blindar el parse del códec, caí a `""` cuando el mimeType no traía `codecs=`. Pero una fila con códec vacío **se lee como OPUS** en el guard (`== "flac"`/`"mp4a.40.2"` ambos false) ⇒ para un usuario Lossless/Saavn **cada apertura parecería un desajuste** → purga los bytes en curso + re-resolución completa a mitad de canción = registro #40 (stalls de 33s / bucle), permanente. Peor que el crash que evitaba. Y la auditoría cazó que dejé un 3er sitio (`DownloadUtil.kt:149`) con el `split` viejo | Función compartida `codecsFromMimeType()` en los 3 sitios: token explícito → contenedor por mimeType → códec de la fila previa → "" solo como último recurso. **LECCIÓN: un valor "vacío/seguro" no es seguro si aguas abajo se INTERPRETA como otra cosa. Trazar cómo se LEE el default antes de elegirlo.** | 0.6.113 | ✅ |

| 80 | **Redmi Note 13 Pro no reproduce (log 20-jul): mi recuperación de sesión era MEDIO anónima + no rotaba invitado** | El cifrado ya funciona (ni una línea de descifrado en el log). Pero el reintento de sesión de la 0.6.111 se disparaba y **volvía a fallar con LOGIN_REQUIRED**. Dos defectos: (a) `resolvePlaybackData` recalculaba `isLoggedIn = cookie != null` **ignorando `noLogin`**, así que el reintento minaba el poToken con el `dataSyncId` de la cuenta y tomaba ramas de logueado — pero enviaba SIN cookie ⇒ sesión atada a cuenta, sin auth ⇒ rechazada. **Es el hallazgo H4 que una auditoría ya me marcó y solo arreglé a medias.** (b) La rama auth-shaped **no rotaba la sesión de invitado**, a diferencia de la rama de invitados. Como `MAIN_CLIENT` (ANDROID_VR) es `loginSupported=false` —ya anónimo en el 1er intento— y AÚN así dio LOGIN_REQUIRED, el muro está en la **IP/dispositivo/visitorData**, no solo en la cookie; un reintento anónimo reusa el mismo visitorData marcado y falla igual | (a) `isLoggedIn = cookie != null && !noLogin` + quitar el `&& cookie == null` redundante del guard de skip; (b) `BotDetectionMitigator.rotateGuestSession()` antes del reintento (espeja la rama de invitados) para una `visitorData` limpia — toca SOLO visitorData, nunca la cookie/dataSyncId de la cuenta. **ALCANCE HONESTO: arregla cookie caducada + sesión de invitado marcada. Si YouTube bloqueó la CUENTA o la IP en duro, ninguna app lo arregla → re-login o cambiar de red. El próximo log lo distingue: un "OK" de WEB_REMIX/ANDROID_CREATOR tras rotar = arreglado; un 2º LOGIN_REQUIRED = bloqueo duro.** **LECCIÓN: al copiar un patrón (rama de invitados), copiar TODO su comportamiento — el skip de clientes Y la rotación; medio patrón se ve igual que el entero hasta que un caso real lo expone.** | 0.6.114 | ⚠️ parcial, depende del dispositivo |

| 81 | **YouTube ROTÓ el reproductor OTRA VEZ → `dfc3d2b2`** (log Redmi, 0.6.114) | `No hardcoded config for hash: dfc3d2b2` + `SIG/N-FUNCTION EXTRACTION FAILED` + `No cipher config for player hash dfc3d2b2`. El fix de `882a803a` (#65) era correcto pero YouTube avanzó una versión (sts 20648→20649). Sin config, los clientes que usan cifrado no resuelven | Config derivada igual que #65: el sitio de firma es idéntico posición-por-posición — `OI(35,7321,x9(6,8270,q.s))` ↔ 882a `WB(27,5330,Vk(38,2047,Z.s))`. Verificado: `OI` global 3-arg XOR (gemelo de `WB`; la otra `OI` es método `g.h.OI` que la llamada no resuelve), `g.uS` clase URL gemela de `g.yk`, sts 20649. Publicado data-only. **LECCIÓN: la rotación del reproductor es RECURRENTE — el auto-reparado por config remota es la vía correcta; cada rotación nueva = derivar+publicar, sin APK.** | remoto | ✅ |
| 82 | **ABIERTO: muro anti-bot + WebView no se crea en el Redmi** (mismo log) | Aparte del cifrado, el log muestra `Inicia sesión para confirmar que no eres un bot` (bot-wall) Y `Failed to get/create CipherWebView`. La WebView que genera el PoToken/cifra falla al crearse → sin PoToken → YouTube bloquea por bot. El usuario probó WiFi y datos: falla igual. Mi rotación de sesión de invitado (0.6.114) se dispara pero no limpia el muro | **Específico del dispositivo/IP, no del cifrado.** Sospecha: Android System WebView desactualizado/deshabilitado, o MIUI matando la WebView de fondo. Remedios a probar por el usuario: actualizar "Android System WebView" en Play, reiniciar el teléfono, o esperar (los flags de IP de YouTube caducan). **No arreglable 100% en la app si YouTube marcó la IP/cuenta en duro.** | — | 🔴 abierto (dispositivo) |

| 83 | **MI ERROR en la config dfc3d2b2 (#81): omití el `x9` interno → 403 en TODOS los streams** | El teléfono del dueño resolvía la canción (portada visible) pero cada stream daba `IO_BAD_HTTP_STATUS (403)`. Causa: publiqué `sigJsExpression=OI(35,7321,INPUT)`, **omitiendo el interno `x9(6,8270,...)`** por analogía con 882a (`WB(27,5330,INPUT)`), donde el interno `Vk(38,2047,...)` **sí era decodeURIComponent** (no-op sobre la firma ya parseada). En dfc3d2b2 esa suposición es FALSA: con C=6, `x9` **no** toma rama de decode → hace una transformación real. Omitirla firma mal → el CDN rechaza con 403. Eso es el "todas fallan" tras resolver | Replicar la cadena EXACTA del player: `OI(35,7321,x9(6,8270,INPUT))`. El motor sustituye INPUT→sig (`CipherWebView.kt:183`), quedando idéntico byte-a-byte a `OI(35,7321,x9(6,8270,q.s))`. Correcto por construcción, sin revertir qué hace `x9`. **LECCIÓN: omitir un wrapper interno de la config SOLO es seguro si es demostrablemente no-op para la entrada real. Por defecto, replicar la cadena COMPLETA del player — correcta pase lo que pase con el interno.** El bug engañaba porque la canción SÍ resolvía; el fallo estaba río abajo, en el fetch del stream. | remoto | ✅ |

| 84 | **CORRECCIÓN de #83 + causa real: 403 = PoToken/IP, el cifrado está PROBADO correcto** | Ejecuté el player.js dfc3d2b2 dentro de su IIFE en jsdom (igual que CipherWebView inyecta). Resultado: `x9(6,8270,sig)` = identidad (firma sin cambios), `OI(35,7321,x9(...))` == `OI(35,7321,sig)` idénticos, n-transform `g.uS.get('n')` transforma bien. **Mi diagnóstico de #83 (que omitir `x9` causaba el 403) estaba MAL** — mi trazado a mano de la rama XOR falló; la config siempre fue correcta. El 403 real: YouTube exige **PoToken** en la URL; `Failed to get/create CipherWebView` = la WebView del PoToken no arranca en ese dispositivo → sin PoToken → 403 en todo. Clientes anónimos → bot-wall desde su IP. WiFi y datos fallan igual | Dispositivo/IP, no config. Pruebas: **VPN a otro país** (IP limpia; distingue IP-flag de dispositivo), cambiar proveedor de WebView en MIUI, re-login YT. **LECCIÓN dura: NO derivar/depurar cifrado ofuscado A MANO — EJECUTARLO en jsdom inyectando dentro del IIFE da verdad determinista. Mis 2 errores en dfc3d2b2 vinieron de trazar ramas a ojo.** Mensaje 'age-restricted' de IO_BAD_HTTP_STATUS es comodín engañoso (arreglar en build). | — | 🔴 abierto (dispositivo) |

| 85 | **MI CRASH: `onMediaItemTransition` revienta con cola VACÍA** (crash report Redmi 0.6.114) | `ArrayIndexOutOfBoundsException: length=0; index=-1` en media3 desde `onMediaItemTransition`. Mi walk de autocarga (#57) hacía `getNextWindowIndex(player.currentMediaItemIndex, ...)`; en timeline vacía `currentMediaItemIndex` = `C.INDEX_UNSET` (-1) → `windows[-1]` sobre array de largo 0 → crash. Alcanzable cuando la cola se vacía (todas las canciones fallando al resolver — justo la tormenta de sesión podrida del dueño). Lo dispara un toque | Guard: si `timeline.isEmpty || idx == INDEX_UNSET` → devolver `Int.MAX_VALUE` (sin autocarga, sin tocar la timeline en -1). El `getNextWindowIndex` hermano de :3228 ya está dentro de `if (!timeline.isEmpty)` — seguro. **LECCIÓN: toda llamada a `getNextWindowIndex/getPreviousWindowIndex` debe guardarse contra timeline vacía / índice -1; `currentMediaItemIndex` es -1 en cola vacía.** | 0.6.115 | ✅ |
| 86 | **CONFIRMADO en dispositivo: 403 del Redmi era SESIÓN PODRIDA, no cifrado ni PoToken** | Usuario: "ya las canciones suenan, tuve que desinstalar la app, reinstalar e iniciar sesión". Reinstalar+relogin limpió el estado de sesión corrupto → streams resuelven. **Confirma la prueba jsdom (#84): el cifrado siempre estuvo bien.** El 403 no era ni mi config, ni PoToken en sí, ni IP — era estado de sesión local corrupto en ese dispositivo | Nada que arreglar en código para eso (cura = reinstalar+relogin). **LECCIÓN: un 403 sistémico tras resolver, con lo demás funcionando, puede ser SESIÓN LOCAL CORRUPTA — reinstalar+relogin antes de asumir IP/cuenta baneada. Y NUNCA derivar cifrado a mano: ejecutarlo (jsdom, dentro del IIFE) da la verdad — mis 2 errores en dfc3d2b2 y el falso diagnóstico del 403 vinieron de razonar el JS ofuscado a ojo.** | — | ✅ (usuario) |

| 87 | **Aleatorio Mejorado ("no-repeat con memoria persistente") — feature nueva + 10 hallazgos de auditoría propios** | Shuffle con memoria por-contexto que nunca repite, recuerda la última canción y las ya oídas al volver días después, re-baraja de verdad el resto no-oído en cada toque, y al agotar el contexto continúa con la radio infinita (ya no aleatoria). Extiende sistemas ya existentes (`applyShuffleOrder`, `shufflePlayedIds`, traspaso Path-B a radio) en vez de reinventar. DB v38→v39 no-destructiva (`enhanced_shuffle_played` + `enhanced_shuffle_context`), identidad de contexto `PL:<id real>` / `AP:<auto>` / `LIB:<tab>` / null. La 1ª implementación (WIP d361442) traía **10 defectos** cazados por auditoría adversarial (arreglados en 6346a6d): el crítico #2 — la re-siembra ponía la cola-oída DETRÁS de las no-oídas y, como `applyShuffleOrder` no se re-invoca a mitad de cola (crossfade off por defecto), esa cola REPETÍA canciones hasta el último ítem → roto el no-repeat; +races de escritura, growth sin poda, tab LIKED/LIBRARY/UPLOADED compartiendo bucket | #2 → traspaso TEMPRANO a radio en cuanto el pool no-oído se vacía (una vuelta antes del último ítem); escrituras serializadas por `Dispatchers.IO.limitedParallelism(1)`; poda de huérfanos al arrancar; `LIB:${filter.name}` por pestaña; union sin `clear()` en la re-siembra. **LECCIÓN: una feature de reproducción NUEVA merece la misma auditoría adversarial que un fix — la 1ª versión "compilaba y parecía correcta" y aun así rompía su propia promesa central.** 138/138 verde | 6346a6d | ⚠️ sin publicar (falta prueba en dispositivo) |
| 88 | **MI BLOCKER: el fix de poda (#3 de la fila 87) BORRABA la memoria no-repeat de las auto-playlists en CADA arranque** | La auditoría del propio fix 6346a6d encontró que las auto-playlists (Me gusta/Descargadas/Subidas/Exportadas) se encolaban con `contextId "PL:<virtual>"`, pero esos ids virtuales **nunca existen en la tabla `playlist`**. La poda de huérfanos (`DELETE ... WHERE contextId LIKE 'PL:%' AND substr(contextId,4) NOT IN (SELECT id FROM playlist)`) los borraba en **cada lanzamiento** → memoria de Me-gusta (el target de shuffle más común) muerta entre sesiones = la feature convertida en no-op justo donde más importa. No crash, feature-defeat silencioso | Prefijo distinto `AP:` para auto-playlists → la poda solo casa `PL:%` (playlists reales borrables), así que su memoria persiste para siempre (esas virtuales nunca se borran). Sin cambio de DAO/esquema; las filas `PL:liked` viejas de mis test builds se auto-limpian con la poda existente. Además gate del traspaso temprano en `MEDIA_ITEM_TRANSITION_REASON_AUTO` (un skip manual ya no completa el ciclo ni borra memoria). **LECCIÓN: un id virtual (`liked`/`downloaded`) que comparte prefijo con ids reales acaba atrapado por una consulta que asume "existe en su tabla" — darle su propio espacio de nombres. Y auditar el FIX, no solo el código original.** 138/138 verde | 41b738f | ⚠️ sin publicar (falta prueba en dispositivo) |

| 89 | **🔴 MI CRASH UNIVERSAL: 0.6.117 se cerraba al abrir en TODO el que actualizaba (migración 38→39 "not found")** | `IllegalStateException: A migration from 38 to 39 was required but not found` en el arranque, al abrir la DB v38 existente. La migración `MIGRATION_38_39` estaba **definida y registrada** — pero en el builder EQUIVOCADO. Hay **DOS** builders de `InternalDatabase`: (a) `InternalDatabase.newInstance` en `MusicDatabase.kt` (MUERTO, nadie lo llama) y (b) `AppModule.provideInternalDatabase` (el que **Hilt inyecta de verdad**). Registré la migración solo en el (a). El (b) llegaba hasta `MIGRATION_37_38` → el contenedor de Room no tenía el paso 38→39 → `onUpgrade` lanza. **Peor:** el propio `AppModule` ya traía un comentario del autor de la 37→38 advirtiendo EXACTO esto ("MUST be here (this is the builder Hilt injects); registering it only on the unused MusicDatabase.newInstance would crash every user on update") — lo pasé por alto. No lo cazaron ni los 138 tests (nunca abren la DB real inyectada por Hilt contra un archivo v38) ni el compile (ambos builders compilan) — crash SOLO en runtime, en dispositivo | Añadir `MIGRATION_38_39` a `AppModule.provideInternalDatabase.addMigrations(...)`. Las tablas son aditivas (ningún dato tocado); la migración en sí ya era correcta y casa byte-a-byte con las entidades. **LECCIÓN PERMANENTE: cada migración nueva va en `AppModule.provideInternalDatabase` (el builder que Hilt inyecta), NO en `MusicDatabase.newInstance` (muerto). Un unit test verde NO prueba la migración — solo un update real v(N-1)→vN en dispositivo lo hace. Antes de publicar un bump de DB: grep de TODOS los `addMigrations` y confirmar que el de DI tiene el paso nuevo.** Ver [[db-migrations-go-in-the-hilt-injected-builder]] | 0.6.118 | ✅ |

| 90 | **Aleatorio Mejorado saltaba a la canción equivocada CON CROSSFADE ON (reporte del dueño: "suena que viene la otra y luego salta a otra que no es")** | Con la transición suave activada (ajuste permanente del dueño), cada auto-avance pasa por `performCrossfadeSwap`, que **salta a propósito `onMediaItemTransition`** (sus propios comentarios lo documentan). Pero TODA la contabilidad del enhanced shuffle vivía en `onMediaItemTransition`: el add al `shufflePlayedIds` (B5) y el `insertEnhancedPlayed` persistente. Resultado: bajo crossfade las canciones auto-avanzadas **jamás se marcaban como reproducidas** → el set quedaba casi vacío → el `applyShuffleOrder` por-swap (`beginCrossfadeSwap`) re-barajaba un pool donde nada constaba como oído → canciones ya oídas resurgían como "siguiente" (el salto audible: el secundario precargó B, el re-barajado movió la aguja a C). El propio diseño del feature se probó SOLO contra la ruta de transición normal | Registrar la canción del swap en el swap site (`beginCrossfadeSwap`, tras `performCrossfadeSwap`, dentro del guard de shuffle existente): add a `shufflePlayedIds` + insert por el single-lane writer, espejo del bloque B5 — ANTES del re-apply del orden, para que las oídas se hundan y el self-reset de ciclo las cuente. Cero cambios al fundido (math/curva/duración intactos). **LECCIÓN: en este repo el crossfade convierte CADA avance en un camino que ESQUIVA los callbacks de media3 — cualquier lógica nueva colgada de `onMediaItemTransition` DEBE espejarse en `performCrossfadeSwap`/`beginCrossfadeSwap` o no existe para usuarios con crossfade (que es el default de la casa). Ya pasó antes (`currentPlayingMediaId`, quality-pin) y volverá a pasar.** | 0.6.119 | ⚠️ pendiente confirmación en dispositivo |

| 91 | **Descargas por trozos de 5MB (port endurecido de Echo-Music 5.2.81) — REEMPLAZA el truco `&range=0-N` en la URL** | Cambio de la RUTA DE DESCARGA (solo `DownloadUtil` + `ChunkingDataSource.kt` nuevo; streaming/`MusicService` y `AudioExportService` intactos). `ChunkingDataSource` re-abre la fuente cada 5MB vía cabeceras Range sobre la URL YA resuelta (bypass del throttling de googlevideo en conexiones largas). Orden atómico del factory: resolver FUERA, chunker DENTRO — cifrado/PoToken + upserts se resuelven UNA vez por descarga, nunca por trozo. Endurecido vs upstream: (a) su `read()` tragaba TODA excepción → un error transitorio truncaba la descarga en silencio y quedaba `STATE_COMPLETED`; aquí solo 416 y `POSITION_OUT_OF_RANGE` son fin-de-stream, el resto se relanza para que DownloadManager reintente; (b) gate de host: solo se trocea `googlevideo` — Qobuz FLAC/Saavn CDN pasan 1:1 sin Range games. El `&range=0-N` se borra en el MISMO commit: los dos trucos son mutuamente excluyentes (el parámetro de URL gana a la cabecera Range → cada trozo se serviría desde el byte 0) | Pruebas pendientes en dispositivo: (a) descarga OPUS normal + mix >30 min, comparar velocidad; (b) pausar/matar a mitad y reanudar — debe CONTINUAR, no reiniciar; (c) modo avión a mitad — debe ir a FAILED/reintento, JAMÁS `COMPLETED` truncada (el bug upstream que el endurecimiento arregla); (d) una descarga Qobuz LOSSLESS y una Saavn completan y reproducen | — | ⚠️ sin publicar (falta prueba en dispositivo) |

| 92 | **El Aleatorio Mejorado seguía REPITIENDO pese a marcar bien las reproducidas (reporte del dueño en 0.6.119) — TRES caminos rotos + 10 defectos propios cazados por verificación** | (a) `playQueue` jamás sembraba la memoria persistente cuando la cola arrancaba con shuffle YA encendido (true→true no dispara `onShuffleModeEnabledChanged`, el ÚNICO sitio de siembra) → la UI (DB) marcaba ✓ pero el ORDEN (set en memoria) repetía; (b) los botones "Aleatorio" de las pantallas NI ENCENDÍAN el modo shuffle (solo `items.shuffled()`) → todo el sistema bypasseado en cualquier config; (c) al agotarse el ciclo bajo crossfade, el self-reset de `applyShuffleOrder` corre SÍNCRONO dentro del swap → mi primer "fix" en `scheduleCrossfade` era **código muerto** (solo alcanzable por skips manuales — justo lo prohibido). La verificación adversarial del fix cazó además: doble-fade bajo REPEAT_ONE con fade corto, toggle stale destructivo en la ventana READY-wait, siembra al contexto equivocado con 2 playQueue rápidos, falso positivo del tail-silence en pausas musicales | (a) helper `seedEnhancedShuffleFromDb` llamado también desde playQueue; (b) `ListQueue.startShuffled` — el botón enciende el modo tras aterrizar los items (+persistir canción #1 al habilitar); (c) handoff a radio REUBICADO dentro de `beginCrossfadeSwap` (único sitio donde el agotamiento es detectable a tiempo) + gate `!radioSeedInFlight` en el self-reset + cancelar triggers al COMMIT del fade + gates vivos + umbral tail 3.5s + recheck en el hop. **LECCIÓN: la maquinaria de "cambió la canción" tiene DOS mundos (transición normal vs swap de crossfade) y CADA pieza nueva debe existir en ambos; y un fix de reproducción se verifica adversarialmente ANTES del tag — las 2 rondas cazaron 10 defectos que "compilaban y parecían correctos".** 144/144 verde | 0.6.120 | ✅ (pendiente confirmación en dispositivo) |
| 93 | **Batch de ports Echo-Music 5.2.81 (25 items analizados: 9 integrados / 16 omitidos con evidencia)** | Adversarial-verificados contra ambos árboles ANTES de portar. Integrado: fix parser sync (secciones "recomendadas" contaminaban playlists — bug REAL que Aura tenía), offline-sin-datos (`recoverSong.isOfflinePlayback`, solo sitios servidos por DESCARGA — deviación deliberada: streaming-cache sigue online para alimentar Mix), Listen Together 1er-toque (+take atómico), volumen NaN, portadas tablet (Top/Caché, patrón propio), base scroll-to-highlight (INERTE — cableado completo requiere `highlightKey` en ~15 pantallas, diferido a propósito), Data Saver (OFF por defecto, solo degrada), chunker 5MB endurecido (solo googlevideo, relanza errores reales — el de upstream truncaba en silencio; reemplaza el truco `&range=0-N`, mutuamente excluyentes), playlist "Recomendado para ti (IA)" (CONCEPTO re-construido: keyless, Hilt, id fijo, timestamp arreglado — el suyo requiere API key + usa el builder muerto + timestamp stale). Omitido con evidencia: edad (su match `"age"` solo-inglés + JioSaavn primer-resultado = bug que ya arreglamos), haptics (el suyo vibra con jitter), calidad-inmediata (su propio log dice "NEXT SONG"), refetch/uptime/prompt-IA (Aura superset), índice lossless GitHub (infra del dev de Echo + imán DMCA) | **LECCIÓN: el changelog de este upstream OVERSELLS crónicamente — cada item se verifica en SU código y en el nuestro antes de decidir; "Aura ya lo hace mejor" fue el veredicto en 16 de 25.** Su retirada de Saavn "por inconsistencia del servidor" respalda que #57 sea server-side. Fila 91 = pruebas de dispositivo pendientes del chunker | 0.6.121 | ⚠️ pendiente dispositivo |

| 94 | **Auditoría TOTAL del aleatorio (3 familias de ataque) tras "siento que repite al activarlo" — 11 fixes en 0.6.124** | El síntoma era real y multi-causa: (a) la canción de ARRANQUE se elegía uniforme sobre TODA la lista (P/T de abrir con repetida, ~50% a mitad de ciclo); (b) el menú "⋯→Aleatorio" (PlaylistMenu) bypasseaba TODO el sistema (sin contextId, sin modo); (c) el traspaso B3 borraba memoria en CUALQUIER reason (un SEEK manual al último ítem = wipe parcial); (d) la rama ShufflePlaylistFirst no tenía sink de reproducidas; (e) el agotamiento se juzgaba contra la timeline VIVA (quitar canciones = wipe en falso); (f) filas duplicadas del mismo videoId sonaban doble; (g) la apertura de ciclo se perdía al morir el proceso; (h) hideExplicit anulaba el contexto de biblioteca para siempre; (i) lo oído SIN shuffle era invisible a la memoria (percepción #1); (j) el fin de cola lineal dejaba el contexto pegado → la radio nocturna escribía ids ajenos sin límite; (k) la dedupe sin gate cambiaba comportamiento con el feature OFF | Arranque unplayed-first en 4 pantallas + menú; contexto en el menú (PL:/AP:); wipe B3 gated (AUTO+repeatOff+exhausted) con detach SIEMPRE que la radio toma la cola; sink en rama ShufflePlaylistFirst; coverage-gate `radioSeedPool.size` en exhausted Y en el self-reset; dedupe por mediaId gated en el hint; re-persistir el opener tras el wipe (FIFO del single-lane); bucket `:NX` para hideExplicit; inserts persistentes des-gated de shuffleModeEnabled (lo lineal cuenta) en transición Y swap. Verificación pre-tag cazó 3 MED más (dedupe sin gate, wipe por self-reset en timeline recortada, contexto pegado en fin lineal). **LECCIÓN: "activar el aleatorio" tiene ~7 superficies de entrada (2 botones, FABs, menú ⋯, toggle del player, cola, restore) y CADA una necesita el mismo contrato de memoria; y todo juicio de "ciclo completo" debe compararse contra el CONTEXTO, nunca contra la timeline viva.** 148/148 verde | 0.6.124 | ✅ (pendiente dispositivo) |
| 95 | **Cola infinita consciente del género (queja del dueño: la radio de una playlist de SALSA derivaba de género)** | La continuación no tenía NINGUNA noción del contexto terminado: semillas del tail vivo (que en cada re-siembra ya ERA la radio derivada → deriva compuesta), lane-keeping muerto bajo crossfade (Path A esquivado por el swap) y anclado a la canción actual (ya derivada), y `orderedByTaste` solo con taste GLOBAL. Al terminar CUALQUIER contexto finito (playlist/álbum/EP/single, uniforme) se construye ahora un `ContextProfile` del pool COMPLETO (`reco/ContextProfile.kt`: artistas + géneros GenreCache + hint débil de idioma) y: (a) término aditivo ACOTADO `ctx ∈ [-4, +6]` en la clave de `orderedByTaste` (artista del contexto −3, género del contexto −4·share, género CONOCIDO ajeno +6 — demotion, JAMÁS drop; idioma solo suaviza −1); (b) semillas por clúster de género del contexto + ancla de re-siembra (tail ∩ contexto, fallback al pool) + prime desde el clúster dominante; (c) enrich WiFi-only de los artistas del contexto | **COTAS EXACTAS E INVARIANTES: `ctx` clamped [-4,+6] (misma clase que pull≤8 / soft+6); `index` sigue dominando (#25); artista/género DESCONOCIDO = 0.0 EXACTO (regla #39/#41 — cache-derived nunca filtra: la cola infinita no puede colapsar a la biblioteca); gate de señal mínima (coverage ≥0.3 O ≥3 artistas conocidos) — por debajo, TODO no-op byte-idéntico a hoy; perfil null/fallo de build = comportamiento de hoy; mood/chip explícito limpia el steer (#34 gana el usuario); #22 no-repeat intacto (el término solo reordena supervivientes); sin red nueva salvo el enrich WiFi-only acotado (40/semáforo 4); crossfade/EQ/licencia sin tocar. Steering en `orderedByTaste` cubre AMBOS mundos (swap y transición — fila 90).** Tests puros `ContextProfileTest` (8) | — | ⚠️ sin publicar (falta prueba en dispositivo) |

| 96 | **🔴 AUDITORÍA TOTAL de funciones/características/botones (orden del dueño: "siento que hay más funciones que no hacen lo que deben") — ~127 hallazgos confirmados con file:line en 4 informes adversariales** | El dueño reportó dos síntomas (el diálogo de borrar playlist sin opción de YouTube; el perfil de audiófilo seleccionado imposible de localizar) y exigió una revisión completa. 4 auditores independientes (reproductor+cola, biblioteca/playlists/descargas, home/cuentas/widgets/LT, ajustes) barrieron cada control trazando onClick→ViewModel→efecto real. Resultado: **la queja era correcta y su alcance mucho mayor de lo reportado.** Familias encontradas: (A) **crossfade-bypass, otra vez y peor** (fila 90 se repitió en 6 sitios NUEVOS): scrobbling Last.fm/LB, seguimiento de Cast, SponsorBlock (¡ON por defecto y por tanto muerto por defecto!), prefetch de la siguiente, **paginación de cola** (playlist larga → saltaba a radio infinita en vez de continuar), `currentMediaMetadata` (widget/notificación/Auto congelados en la canción anterior) y `previousMediaItemIndex`; además velocidad/tono y salida de audio preferida se perdían en cada swap; (B) **pérdida de datos**: "me gusta" en bloque de YouTube reconstruía la fila con `totalPlayTime=0`/`inLibrary=null` → reseteaba estadísticas y EXPULSABA canciones de Biblioteca; borrado en bloque parqueaba una canción no seleccionada en posición `Int.MAX_VALUE`; likes con `update` (no-op sin fila); doble alta remota en playlists de YouTube; (C) **botones inexistentes por un detalle de Kotlin**: `listOfNotNull(if (x) { NewAction(play); NewAction(shuffle) })` — un bloque vale su ÚLTIMA expresión, así que "Reproducir" se construía y se tiraba en los ⋯ de álbum/playlist; (D) **acciones que se ejecutaban DOS VECES** por invocar un lambda `@Composable` durante la composición con reset en `LaunchedEffect` (una recomposición provocada por la propia acción reentraba); (E) **interruptores placebo**: Liquid Glass "en el reproductor" sin consumidor (y pagando backdrop = batería), tema dinámico nunca renderizado, notificaciones de actualización ignoradas por el worker, `MixSortDescendingKey` con la MISMA cadena que `AlbumSortDescendingKey`, defaults de UI que no coinciden con los del motor (precarga 10 vs 2, historial 1 vs 30s, cachés 512/1024 vs 2048/ilimitado), offload forzado a false por dos claves muertas; (F) **estados de error inexistentes**: 5 pantallas con `onFailure { reportException }` que nunca marcan fallo mientras la UI trata "sigue null" como "sigue cargando" → shimmer eterno sin reintentar; (G) enlaces de invitación de Listen Together a un dominio de OTRO fork (404) y su lista de servidores REEMPLAZADA en cada arranque desde el repo de un tercero; (H) Last.fm daba por bueno todo scrobble (la API responde 200 con `"error"` en el cuerpo) | Arreglado por familias: (A) helpers compartidos `applyAutoAdvanceSideEffects()` + `maybeLoadMoreQueuePages()` + `applySponsorBlockFor()` llamados desde AMBOS mundos, `currentMediaMetadata`/`playbackParameters`/`preferredDeviceId` propagados en el swap; (D) `SideEffect` en vez de `LaunchedEffect` (se aplica sincrónicamente antes de cualquier recomposición). **LECCIÓN MAESTRA (reincidencia de la fila 90): el crossfade ON convierte cada avance en un camino que ESQUIVA los callbacks de media3; ya van 3 rondas de bugs por lo mismo. Regla permanente: TODA lógica de "empezó una canción" vive en un helper compartido, NUNCA suelta dentro de `onMediaItemTransition`. Segunda lección: un `onFailure { reportException }` NO es manejo de error — si la pantalla no distingue fallo de carga, el usuario ve un esqueleto eterno. Tercera: un bloque Kotlin en `listOfNotNull` descarta todo menos su última expresión — usar `buildList`.** | 0.6.136 | ✅ publicado; transiciones y estabilidad CONFIRMADAS a oído por el dueño (2026-07-28: "las transiciones las hace correctamente, ya no hay cortes, el crash no ha vuelto a pasar"). Resto de arreglos de la auditoría: verificados por compilación + 165/165 tests + 3 rondas adversariales, pendientes de uso prolongado |

| 97 | **🔴 Los dos PoToken de YouTube iban CRUZADOS — cada petición llevaba el binding del otro** | `PoTokenGenerator.kt:193-196` construía `PoTokenResult(playerRequestPoToken = streamingPot, streamingDataPoToken = playerPot)`. Los bindings de contenido son fijos y NO son intercambiables: el token de la petición `/player` debe ir ligado al **videoId** y el de la URL de stream (`?pot=`) a la **sesión** (`dataSyncId`/`visitorData`). Aura mandaba cada uno al revés. El consumo (`YTPlayerUtils.kt:947/1101` y `:1189`) siempre fue correcto, así que no había dos errores que se cancelaran: ambas peticiones salían mal firmadas. Ambos campos son `String`, y encima se asignaban con argumentos con NOMBRE, lo que hace que el cruce *parezca* correcto al leerlo — el compilador no puede cazarlo y ningún test lo cubría. Candidato directo a los 403 `WEB_REMIX` y a las URLs de stream que YouTube invalida a mitad de canción (#57) | Invertido a `playerRequestPoToken = playerPot, streamingDataPoToken = streamingPot`, con el binding de cada uno documentado en el sitio. Los nombres de las variables locales (`playerPot` / `streamingPot`) YA decían dónde iba cada una: el error fue solo en la asignación. **LECCIÓN: dos parámetros del mismo tipo primitivo y adyacentes son un cruce esperando ocurrir; los argumentos con nombre dan una falsa sensación de seguridad porque el nombre del PARÁMETRO se lee y el de la VARIABLE no. Cuando dos valores del mismo tipo tienen semánticas distintas y no intercambiables, el sitio de construcción necesita un comentario que ancle cada binding — o tipos distintos.** Encontrado auditando el ítem "PoToken session management" del changelog de upstream 5.2.85: su changelog era humo, pero su código tenía este mapeo BIEN y el nuestro mal | 0.6.140-beta10 | ⚠️ pendiente prueba en dispositivo (¿bajan los 403 y los cortes?) |
| 98 | **`App.onCreate` corría ENTERO en los procesos desechables `:crash` y `:phoenix` — dos escritores sobre el mismo índice de caché de media3** | El propio archivo documenta desde hace versiones que `onCreate` corre en TODOS los procesos (`App.kt:66-69`), pero **solo** `scheduleNonCriticalWork` tenía la guarda `isDefaultProcess()`. Todo lo demás se ejecutaba también en `:crash` (CrashActivity) y `:phoenix` (el trampolín de reinicio), incluido `downloadUtilLazy.get()`, que construye los dos `SimpleCache` (`@PlayerCache` y `@DownloadCache`) sobre un `StandaloneDatabaseProvider` compartido. media3 protege un directorio de caché contra una segunda instancia **solo dentro del mismo proceso** (un set estático de directorios bloqueados), así que nada impedía que `:crash`/`:phoenix` abrieran los MISMOS directorios mientras el proceso principal seguía vivo → dos escritores sobre `cached_content_index`, justo en el momento en que la app ya está cayendo. Además, cada proceso desechable escaneaba ambos directorios de caché completos, cargaba la .so de Superpowered, hacía una petición de red y dejaba dos colectores de Flow infinitos — batería y calor gastados por un proceso que muere en segundos | Guarda única `if (!isDefaultProcess()) return` antes de todo el trabajo pesado de `onCreate` (los launches de caché/EQ, Superpowered, configs remotas, exit-reasons y el mirror del DataStore). Lo que sigue corriendo en todos los procesos es lo barato y necesario: `CrashHandler`, el guard del looper, Timber/AppLogger. **LECCIÓN: un comentario que documenta un peligro NO es una guarda. `App.kt` explicaba el problema multiproceso con detalle en dos sitios y aun así solo UNA de las siete cosas peligrosas estaba protegida — documentar el riesgo dio la sensación de haberlo tratado.** | 0.6.140-beta10 | ⚠️ pendiente dispositivo |
| 99 | **El volumen de Cast era un no-op silencioso tras reanudar sesión: el deslizador se movía y el altavoz no** | `castSession` (`CastConnectionHandler.kt:54`) se asignaba en UN solo sitio, `onSessionStarted:354`. Ni `onSessionResumed` ni la rama "ya conectado" de `initialize()` lo asignaban, así que en toda sesión REANUDADA (volver a primer plano, reinicio del proceso mientras se emitía) `setVolume` escribía sobre `null`. Y como `_castVolume.value = clampedVolume` se ejecutaba igual, la UI publicaba un volumen que el dispositivo nunca recibió: fallo invisible, sin log, sin error | `castSession = session` en ambos sitios + `adoptSessionVolume(session)` para sembrar el deslizador con el volumen REAL del dispositivo. `setVolume` ahora sale temprano con un `Timber.w` si no hay sesión y **no mueve el mirror**. **LECCIÓN: publicar el estado optimista junto al efecto secundario es lo que convirtió un bug en un bug INVISIBLE — el mirror solo debe avanzar cuando la escritura de verdad ocurrió.** | 0.6.140-beta10 | ⚠️ pendiente dispositivo (requiere Chromecast) |
| 100 | **La puerta de título de Saavn (arreglo de un bug real) introdujo 9 defectos propios en dos rondas — el peor mataba JioSaavn entero para escritura no latina** | El bug de partida era real: el emparejador Saavn no tenía puerta de título, y como su marcador es ADITIVO, duración (+30) + artista (+20) + explícito (+5) = 55 ≥ MIN_CONFIDENCE 40 con similitud de título CERO → sonaba una canción distinta. Pero el arreglo trajo lo suyo: (a) `[^a-z0-9]` **borraba todo carácter devanagari/CJK/hangul/cirílico** → conjunto de tokens vacío → rechazo del 100% de los candidatos; JioSaavn es un servicio **indio**, así que el arreglo mataba su mercado principal (en beta9 esas canciones SÍ sonaban, por los +50 de duración+artista); (b) `\p{Mn}` también borra las matras y el virama indios, que reescriben la palabra hindi; (c) la contención asimétrica NO bloqueaba el caso que su propio comentario decía bloquear (`"Sola"` → `"Ella Baila Sola"` daba 1.0); (d) con contención por primera palabra, `"Nunca"` → `"Nunca Es Suficiente"` pasaba; (e) el umbral 0.5 es EXACTAMENTE el jaccard de "una palabra de dos", que cubre a la vez un acierto real y una sustitución errónea; (f) `"with"` como marcador de crédito truncaba `"Dancing With Myself"` a `"Dancing"`, que casa exacto con `"Dancing Queen"`; (g) la contención por subconjunto dejaba pasar `"Gone With The Wind"` → `"Gone"`; (h) la abstención por escritura se activaba con un lado vacío, abriendo la puerta a todo candidato si el título no cargaba; (i) el log de rechazos iba a `Timber.d`, que `AppLogger` NO persiste en release — justo la línea que haría falta para diagnosticar | Normalización que preserva `\p{L}\p{N}` (toda escritura) plegando solo U+0300–U+036F (diacríticos latinos, no matras) + tabla para las latinas sin descomposición NFD (ß ø ł ı đ þ æ œ); **abstención explícita** cuando un lado usa una escritura que el otro no usa en absoluto (XOR, porque lo COMÚN es el título mixto tipo `"केसरिया (Lofi)"`), de modo que la puerta nunca puede empeorar una biblioteca en otra escritura; crédito por **igualdad** entre algún segmento del título de YouTube (partido por guiones/pipes, quitados paréntesis y créditos) y el título del candidato — nada de subconjuntos; umbral 0.6 (mayoría estricta de palabras); tokens de 1 carácter admitidos si son no-ASCII (un kanji ES una palabra); log a `.i()`. 30 tests. **LECCIÓN: 7 de los 9 defectos los cazó la VERIFICACIÓN ADVERSARIAL o un test que escribí para fijar el requisito, no la compilación ni el criterio — y los 2 peores solo dañaban idiomas que el dueño no escucha, así que el "probé y funciona" jamás los habría detectado. Un arreglo de emparejamiento se valida contra un corpus multi-idioma o no está validado.** | 0.6.140-beta10 | ⚠️ pendiente dispositivo |

| 101 | **🔴 El aleatorio repetía OTRA VEZ (5ª ronda) — cinco causas simultáneas, cuatro nunca tocadas por las cuatro rondas anteriores** | Auditoría de 4 lentes tras "siento que sigue poniendo repetidas", con el dueño habiendo CONFIRMADO el arreglo en 0.6.138. (a) **El restore nunca arrancaba la sesión de shuffle**: media3 ignora `setShuffleModeEnabled` cuando el valor no cambia, así que no emite evento. El arranque pone el flag desde `ShuffleModeKey` con el player VACÍO (la sesión aborta por `mediaItemCount == 0`), `playQueue` se salta a propósito su reset-a-false en `isRestore`, y el restore por-cola asigna DE NUEVO el mismo valor → no-op silencioso. Neto: shuffle activo con el set de reproducidas VACÍO y el orden aleatorio propio de media3, con toda la memoria persistente intacta en la DB y NADIE leyéndola. Como el móvil del dueño mata la app constantemente, ese era su camino habitual. (b) **`applyShuffleOrder` anclaba la canción actual INTERCAMBIÁNDOLA con el slot 0**: el slot 0 es por construcción la mejor candidata NO reproducida y la actual acaba de sonar (su slot está hundido en la zona de reproducidas), así que el swap desterraba una canción sin oír a territorio ya oído. Peor al cerrar ciclo: con UNA sola sin oír, ésta ocupa el slot 0, el swap la entierra y el slot 1 — literalmente la siguiente que suena, verificado desensamblando `DefaultShuffleOrder` de media3 1.10.1 — queda con una repetida. El verificador calculó ~49 repetidas seguidas en una lista de 50; además `isEnhancedContextExhausted()` seguía false, así que el traspaso a la radio tampoco rescataba. (c) La rama `ShufflePlaylistFirst` barajaba el grupo AÑADIDO como un bloque indiferenciado → al entrar la radio infinita, cada tema suyo ya oído volvía a mezclarse uniformemente en cada re-aplicación (con crossfade ON, en CADA cambio de canción). (d) Los menús "···" de álbum, álbum de YouTube y artista pasaban una lista pre-desordenada con el MODO apagado — orden congelado que esquiva el sistema entero; es el defecto exacto que las filas 92(b)/94(b) arreglaron para el menú de playlists y estos tres se quedaron fuera. (e) Reconocedor: la rama laxa de `bestSongMatch` usaba contención de subcadena bidireccional, y "ella baila sola" CONTIENE "sola" | (a) `beginShuffleSession()` extraída del listener y llamada explícitamente cuando la asignación no puede disparar el evento; (b) **rotación** en vez de intercambio (`System.arraycopy`, guarda `k > 0`), que preserva el orden relativo de todo lo demás; (c) partición reproducidas/no-reproducidas también en el grupo añadido; (d) `startShuffled = true` en los tres menús; (e) puerta por palabras delante de la contención. **LECCIÓN CAPITAL: `applyShuffleOrder` no tenía NI UN TEST — de ahí las cinco rondas. Ahora la ordenación vive en `playback/ShuffleOrdering.kt` (puro, sin Android) con 8 tests, incluido uno que EJECUTA el swap viejo y afirma el resultado erróneo exacto, para que un refactor no pueda reintroducirlo en silencio. Segunda lección: "el callback se disparará" es una suposición, no un hecho — media3 calla en toda asignación que no cambie el valor, y ese silencio ya causó las filas 92(a) y ésta.** | 0.6.140-beta11 | ⚠️ pendiente dispositivo (¿aguanta una vuelta completa sin repetir?) |
| 102 | **La memoria del aleatorio no existía para la mitad de la app, y donde existía se guardaba en dos sitios distintos** | Continuación de la fila 101 (los hallazgos que no cabían en beta11). (a) **Android Auto**: `onSetMediaItems` devuelve los ítems directo a media3 sin pasar JAMÁS por `playQueue`, único escritor de `shuffleContextId` → todo lo reproducido desde el coche se apuntaba en el bucket de la ÚLTIMA lista abierta en el móvil (envenenando una lista que ni sonaba, mientras la que sonaba no aprendía nada), y su "Aleatorio" era un `songs.shuffled()` con el modo APAGADO. (b) **"Mi Top" y "Caché"**: cero contexto, cero memoria entre días. (c) **Álbumes y artistas**: encendían el modo pero no podían tener memoria — no existía namespace para ellos, así que el no-repeat moría con el proceso. (d) **Buckets divergentes**: `LibrarySongsScreen` escribía `LIB:LIKED` y `AutoPlaylistScreen` `AP:liked` para LA MISMA colección → entrar por Biblioteca el lunes no enseñaba nada a la tarjeta del martes. (e) **Carrera de contexto**: `shuffleContextId` se cambiaba a la cola NUEVA de forma síncrona mientras sus ítems aún se descargaban (segundos en datos móviles), así que cada avance de la cola VIEJA en esa ventana se archivaba en la nueva. (f) `Queue.Status.reanchor` resolvía a la PRIMERA aparición de un id (rebobinando colas con la misma canción dos veces) y caía a índice 0 cuando el ancla se filtraba (reinicio de cola que el usuario nunca pidió). (g) El tick periódico de posición escribía sin el guardián de coherencia que 0.6.139 añadió al otro escritor → un cierre en la ventana "sucia" restauraba una posición que no correspondía a la cola guardada | `adoptExternalQueue()` + `pendingExternalShuffle` consumido en `onTimelineChanged` (el primer instante en que los ítems externos SON la timeline y una sesión puede arrancar); contextos nuevos `AP:top:<periodo>`, `AP:cached`, `AL:<id>`, `AR:<id>`; `LibrarySongsScreen` remapeada a los ids canónicos `AP:`; adopción del contexto movida DENTRO del bloque async junto al guardián `currentQueue === queue` que ya existía; `reanchor` casa la misma OCURRENCIA y cae al siguiente SUPERVIVIENTE, no al 0; el tick periódico vacía la cola sucia antes de escribir la posición. **Decisión de diseño clave: `contextId` subió a la INTERFAZ `Queue` en vez de seguir leyéndose como `(queue as? ListQueue)?.contextId`. Un agente había "resuelto" la memoria del álbum convirtiéndolo a `ListQueue`, lo que cambiaba QUÉ SUENA DESPUÉS del álbum (de la radio de álbum de YouTube a la radio propia) — un cambio de comportamiento que nadie pidió. Con el contexto en la interfaz, `LocalAlbumRadio` conserva su continuación Y gana memoria. LECCIÓN: cuando un arreglo obliga a cambiar el TIPO de una cosa, el problema suele estar en dónde se lee el dato, no en la cosa.** Prefijos nuevos verificados contra la poda `PL:%` de `DatabaseDao` (fila 88) | 0.6.140-beta12 | ⚠️ pendiente dispositivo |
| 103 | **Cast: el volumen del altavoz no se reflejaba en la app + 4 MB de tipografías muertas en cada APK** | (a) `castSession` solo se asignaba en `onSessionStarted`, así que en toda sesión REANUDADA `setVolume` escribía sobre null mientras el espejo `_castVolume` avanzaba igual: el deslizador se movía y el altavoz no, sin log ni error. Y no había escucha de cambios hechos EN el dispositivo (mando de la tele, botones del altavoz), así que el deslizador derivaba de la realidad. (b) `res/font/sans_flex.ttf` (3.997 MB) tenía CERO referencias, y `bbh_bartle*` solo lo referenciaban dos declaraciones Kotlin duplicadas que nadie consumía | (a) `castSession = session` en resume y en la rama "ya conectado", `adoptSessionVolume()` que siembra desde la sesión viva y engancha un `Cast.Listener` (removido en cada teardown), y `setVolume` que NO mueve el espejo cuando no hay sesión — publicar estado optimista junto al efecto es lo que hacía el fallo invisible. (b) Borrados los 5 archivos muertos tras comprobar Kotlin, XML, temas, estilos y manifiesto en los tres flavors. **LECCIÓN: un recurso ausente referenciado solo desde XML NO falla al compilar — revienta en ejecución; toda limpieza de recursos se verifica con búsqueda en XML, no solo en Kotlin.** | 0.6.140-beta12 | ⚠️ pendiente dispositivo (Cast necesita Chromecast) |

| 104 | **🔴 INCIDENTE 0.6.140 (clientes): micro-tirones en Android Auto** | Regresión REAL de 0.6.140, pero no donde apuntaban mis sospechas (dos de tres se refutaron con evidencia: el volcado cada 10 s ya existía en 0.6.139 idéntico, y las rutas del campo `service` recién activado son triviales). La causa: en 0.6.139 el "Aleatorio" del coche era un `songs.shuffled()` de una sola vez con el MODO apagado — la maquinaria por-canción del shuffle **jamás corría en el coche**. 0.6.140 arregló eso (fila 102) y con ello activó, en las colas MÁS GRANDES de la app (un tap de canción encola la biblioteca ENTERA, sin tope), el `applyShuffleOrder` completo EN CADA cambio de canción, en el hilo PRINCIPAL, exactamente durante el fundido de 5 s con dos decodificadores — y `setShuffleOrder` re-difunde el orden completo al coche por Binder y marcaba la cola sucia (volcado completo ≤10 s después). La rampa de volumen del fundido corre en ese MISMO hilo → sus pasos se retrasan → tirones audibles alrededor de cada cambio | **Saltarse el re-apply cuando nada relevante cambió** (`shuffleOrderStale`: lo arman appends/toggle/aterrizaje de semilla/ediciones de cola; lo consume el re-apply del swap). Posponer el hundido de la recién-oída es seguro dentro de una vuelta: el orden anterior ya tenía TODO lo no-oído por delante de la aguja. + `applyingShuffleOrder` suprime el marcado sucio/stale de nuestros PROPIOS setShuffleOrder (media3 despacha síncrono en el mismo hilo → flag plano sin carrera; el archivo de cola guarda orden de TIMELINE, así que esos volcados escribían bytes idénticos). + el tick periódico ya no duplica el volcado del loop de coherencia dedicado. **DESCARTADO con evidencia: podar ítems ya reproducidos de la timeline (rompería cobertura fila 94(e) y la partición posicional) y "comparar órdenes y saltar si iguales" (el orden legítimamente difiere en cada frontera por el jitter aleatorio — jamás saltaría). LECCIÓN: activar una función en una superficie donde nunca corrió (el coche) es un CAMBIO DE CARGA, no solo de corrección — el costo por-frontera que el móvil toleraba es audible en el coche.** | 0.6.141 | ⚠️ pendiente coche real |
| 105 | **🔴 INCIDENTE 0.6.140 (clientes): la cola inteligente predice cosas SIN RELACIÓN con lo que suena** | Tres causas confirmadas, dos de ellas de SIEMPRE (no regresión — los clientes las notaron al crecer): (a) **las colas externas (Android Auto) nunca pasan por `playQueue`, único escritor de `radioSeedPool`/`contextProfile`** → al terminar la cola del coche, la radio sembraba y ORIENTABA desde la ÚLTIMA colección del teléfono — la playlist de AYER decidiendo las recomendaciones de HOY (con proceso fresco: la del restore). Literalmente la queja. (b) **la orientación por género es un EMPUJÓN acotado** ([-4,+6] contra una clave dominada por el índice): desplaza ~10 puestos, JAMÁS quita — un lote malo de YouTube para un contexto nicho (salsa, worship) sonaba igual, solo un poco más tarde. (c) **GenreCache es el punto único de fallo de TODA la orientación y se moría en silencio**: un fallo transitorio de red se cacheaba PARA SIEMPRE como "género desconocido" (envenenamiento), y el gate solo-WiFi deja el cache vacío de por vida para usuarios de datos móviles → orientación muerta sin síntoma. Además (d) tap simple en el coche con shuffle recordado ON: sin `beginShuffleSession` → el set de reproducidas de la cola ANTERIOR podía agotar la del coche en 1-2 canciones | (a) `adoptExternalQueue` limpia pool/perfil/steer y `onTimelineChanged` re-siembra el pool desde los ítems REALES del coche al aterrizar (paridad multi-seed con playQueue) + `beginShuffleSession(isUserActivation=true)` para el tap simple con modo ON; (b) **drop suave fuera-de-contexto en `appendSeed`**: quita SOLO candidatos con género CONOCIDO y share 0.0 en el perfil, conserva TODA lane del contexto (share>0, una playlist mixta no colapsa a su género dominante), desconocidos INTACTOS (#39/#41), solo con ≥10 supervivientes (nunca-silencio), gated en el MISMO toggle del usuario que el drop de paginación (default ON) + traza `CTX_DROP`; (c) fallo≠miss (fallos NO se persisten; set negativo en memoria por sesión; aborto de lote a los 3 fallos seguidos; ráfaga 40→15) + preferencia real para datos móviles (default = solo WiFi, comportamiento de hoy) + el perfil INACTIVO se reconstruye cuando el cache ya aprendió (antes se construía una vez y quedaba muerto toda la vida del contexto). **LECCIÓN: un sistema de recomendación con tres eslabones (semillas → señal de género → empujón) falla en silencio por CUALQUIERA de los tres; ninguno tenía traza, así que "predice mal" era indiagnosticable. Ahora CTX_DROP + NO_REPEAT dan la evidencia.** | 0.6.141 | ⚠️ pendiente escucha real |

| 106 | **🔴 BLOQUEANTE 0.6.142: la queja original del dueño, INVERTIDA — suscripción masiva a la cuenta EQUIVOCADA al cambiar de cuenta** | Alcanzable con TODO por defecto, sin herramientas de desarrollo: (1) instalación NUEVA → `App.applyLibraryUploadOptInV1` + `LibraryUploadOptIn.decide(stored ?: freshInstall)` enciende `YtmUploadSyncKey` **sin preguntar**; (2) inicias sesión en la cuenta A y `markArtistsSubscribedOnYtm` (`DatabaseDao.kt`) estampa `followedByUserAt` **al por mayor desde la lista remota de A** — cientos de filas; (3) «Cerrar sesión (mantener datos)» → `clearArtistAccountSyncMarkers` limpia `ytmSyncedAt` + `unfollowedByUserAt` pero **CONSERVA `followedByUserAt` a propósito** (borrarlo destruiría follows sin subir), y `forgetAccount` quitaba seis claves de sesión de las que `YtmUploadSyncKey` **NO era una** → el interruptor de subida seguía ENCENDIDO; (4) inicias sesión en la cuenta B; (5) cualquier sincronización («Sincronizar todo», el worker programado, el botón de subida) → toda esa fila casa con `artistsPendingSubscribe` + `ArtistSyncPolicy.maySubscribe` y `LibraryUploadSync` dispara `subscribeChannel(id, true)` contra **B**, 150 por pasada, encadenables 20 veces. B queda suscrita a la lista de A: invisible hasta que abra YouTube y envenena sus recomendaciones para siempre. **El argumento de seguridad del propio código era autocontradictorio**: `ArtistSyncPolicy` justificaba conservar `followedByUserAt` diciendo (a) que es «una afirmación sobre el USUARIO, no sobre ninguna cuenta» — FALSO, lo escribe la lista remota de A — y (b) que el efecto entre cuentas queda «detrás del interruptor que el usuario tiene que encender él mismo» — FALSO en instalación nueva | Una línea: `forgetAccount` escribe `settings[YtmUploadSyncKey] = LibraryUploadOptIn.onAccountDetached()` **dentro del mismo `dataStore.edit`** que borra la sesión. Desvincular una cuenta revoca el consentimiento dado para ESA cuenta; el dueño de la siguiente lo concede él mismo en Ajustes ▸ Sincronización — que es exactamente lo que el comentario ya afirmaba y ahora por fin es cierto. **`false` explícito, NO `remove()`**: `decide` resuelve un valor AUSENTE a `freshInstall`, así que quitar la clave permitiría re-encenderla; `afterDetachAndMigration` fija que el `false` almacenado sobrevive a la migración de una sola vez en las cuatro combinaciones. **NO debilita el requisito en vivo**: ni `ArtistEntity.toggleLike`, ni `SongEntity.toggleLike`, ni `flushPendingUnsubscribes` leen ese interruptor — verificado. Comentarios corregidos en los tres sitios que repetían la afirmación falsa (`ArtistSyncPolicy`, `DatabaseDao.clearArtistAccountSyncMarkers`, `App.forgetAccount`). **LECCIÓN: cuando un dato se conserva «porque es del usuario, no de la cuenta», hay que comprobar QUIÉN LO ESCRIBE — aquí lo escribía la cuenta, y el comentario llevaba rondas justificando lo contrario. Un comentario que argumenta seguridad es código: hay que verificarlo o borrarlo.** | 0.6.142 | ⚠️ pendiente dispositivo (cambio real de cuenta A→B) |
| 107 | **El arreglo de la ronda anterior creó un marcador de «dejar de seguir» que revertía una suscripción hecha en YouTube** | Introducido POR el arreglo del bloqueante anterior: `ArtistEntity.localToggleLike` pasó a estampar `unfollowedByUserAt` en **todo** artista no local (necesario — tras `MIGRATION_39_40` una suscripción real y un marcador incidental son indistinguibles, y no registrar la intención es lo que hacía evaporarse el unfollow). Pero `toggleLike` era **fire-and-forget**: llamaba a `subscribeChannel(cid, false)` y **nunca escribía de vuelta**. Tras un unfollow en vivo que SÍ llegó, la fila conservaba el marcador con `ytmSyncedAt` NULL: inerte **y además inalcanzable por toda limpieza**, porque `artistsPendingUnsubscribe` exige `ytmSyncedAt IS NOT NULL` y por eso el retiro por STALE_INTENT tampoco lo veía. Si dentro de `PENDING_UNFOLLOW_TTL_DAYS` (30) el usuario cambiaba de idea y se suscribía a ese mismo artista **directamente en YouTube**, la relectura estampaba `ytmSyncedAt`, `afterRemoteSubscriptionSeen` conservaba el marcador a propósito → forma de baja pendiente legítima → y `flushPendingUnsubscribes` (libre del interruptor a propósito) revertía **en la misma pasada** la suscripción que el usuario acababa de hacer | Escritura de vuelta **solo en ÉXITO**: `ArtistSyncPolicy.liveCallHonouredTheUnfollow(subscribing, confirmedByAccount)` decide, y `DatabaseDao.confirmArtistUnsubscribed(id, unfollowedAt)` retira el marcador. Una instrucción cumplida deja de ser una instrucción. **El caso de FALLO es deliberadamente el contrario**: si la llamada no llegó (sin conexión, cookie caducada, canal sin resolver) el marcador SOBREVIVE, porque es todo el mecanismo de reintento — retirarlo ahí sería destruir un unfollow real, el bloqueante que todo ese archivo existe para evitar. El éxito es el único discriminador posible: mirando la fila almacenada, «llegó y luego cambió de idea» y «no llegó y sigue pendiente» son idénticos. La UPDATE lleva tres condiciones de carrera (`followedByUserAt IS NULL`, `unfollowedByUserAt IS NOT NULL`, `unfollowedByUserAt <= :unfollowedAt`) para no pisar un re-follow ni una intención MÁS NUEVA llegada mientras la llamada estaba en vuelo, y es una UPDATE dirigida en vez de escribir la entidad (la instantánea es previa a la red y pisaría `name`/`thumbnailUrl`/`channelId`). El parámetro de `toggleLike` es **obligatorio, sin valor por defecto**: un default dejaría que la próxima llamada reintrodujera el fallo callándose. Sin migración de esquema. **LECCIÓN: «fire-and-forget» y «marcador persistente de intención» son incompatibles — si nadie confirma que la orden se cumplió, la orden queda de pie para siempre y algún día se re-arma sola.** | 0.6.142 | ⚠️ pendiente dispositivo |
| 108 | **CAMBIO EN EL REPRODUCTOR CLÁSICO hecho por la ronda anterior de la «Interfaz nueva»: la barra ONDULADA sí bloquea ahora al invitado de Escuchar Juntos** | Al extraer la línea de tiempo a `ui/component/PlayerSlider.kt::PlayerProgressSlider` (una sola implementación para las cinco formas de reproductor), el parámetro `enabled` pasó a aplicarse a **las cuatro** ramas de estilo. Antes, en el clásico, solo DEFAULT y SLIM recibían `enabled`: las dos ramas ONDULADAS (`WavySlider` y `SquigglySlider`) **nunca lo recibían**, así que un invitado de Escuchar Juntos con el estilo «ondulado» podía arrastrar la barra y provocar un `seekTo` que el anfitrión no había pedido, mientras que con DEFAULT/FINO el mismo gesto estaba correctamente bloqueado. **Es una CORRECCIÓN de un fallo real, pero es un cambio de comportamiento del reproductor CLÁSICO**, no solo de la interfaz nueva: se registra aquí porque el dueño exige que toda alteración del clásico quede anotada, y porque si alguien reporta «ya no puedo mover la barra en modo ondulado» la respuesta es *era un fallo, ahora coincide con los otros tres estilos* | `PlayerProgressSlider` aplica `enabled` de forma uniforme (`PlayerSlider.kt:78-143`) y lo documenta en su KDoc. Ninguna otra ruta cambió: con `enabled = true` (el caso normal, sin Escuchar Juntos) las cuatro ramas se comportan exactamente igual que antes. Efecto colateral que también se corrige en esta ronda: `auraSliderColors()` (`ui/newui/AuraPlayer.kt`) pintaba los colores DESHABILITADOS idénticos a los habilitados, así que el invitado veía una línea de tiempo con aspecto normal que se tragaba cada toque en silencio — ahora se apaga a `OnGroundDisabled` como hace el clásico. **LECCIÓN: al unificar N copias de un control en una sola, hay que enumerar QUÉ parámetros recibía cada copia — la copia que se «portaba bien» puede estar tapando que otra nunca recibió la guarda.** | 0.6.143 | ⚠️ pendiente dispositivo (requiere sesión de Escuchar Juntos como invitado + estilo ondulado) |

| 109 | **🔴 BLOQUEANTE 0.6.143: el reproductor vertical NUEVO salía SIN PORTADA Y SIN CANVAS con los ajustes de fábrica — lo único que la beta existe para enseñar** | Cadena completa, verificada: `App.kt:604` siembra `PlayerBackgroundStyle.APPLE_MUSIC` en TODA instalación nueva; `Thumbnail.kt` ocultaba su bloque entero de portada/carrusel/canvas con la condición `playerBackground == APPLE_MUSIC && !isLandscape`; y `AuraPlayer.kt` llamaba a `Thumbnail(...)` sin `isLandscape`, que por defecto es `false`. Resultado: caja `weight(1f)` VACÍA justo donde va la carátula, con la insignia «CANVAS ▸ EN MOVIMIENTO» pudiendo dibujarse sobre la nada. **La regla en sí no estaba mal — estaba en el sitio equivocado**: describe al reproductor CLÁSICO en vertical, que con estilo Apple Music pinta la portada a pantalla completa como fondo (`Player.kt:1255-1332`), así que una tarjeta cuadrada delante sería portada sobre portada. El nuevo dibuja `auraScreenBackground(bloom)` y ninguna portada a pantalla completa, de modo que heredaba un agujero. Efecto secundario del mismo error: `getTextColor(DEFAULT)` devuelve `onBackground`, que en tema CLARO es tinta casi negra sobre el suelo casi negro de Aura | La decisión pasa al **ANFITRIÓN**, que es quien sabe lo que pinta: `enum class ThumbnailHost { CLASSIC, OPAQUE_DARK }` (`Thumbnail.kt`). `CLASSIC` es el valor por defecto y conserva la regla histórica **byte por byte** — ningún sitio de llamada clásico cambia. `OPAQUE_DARK` (el reproductor nuevo, `AuraPlayer.kt`) declara «pinto mi propio suelo oscuro opaco y NUNCA la portada a pantalla completa», así que la carátula y el canvas se dibujan siempre, **en los siete valores del ajuste a la vez**, el texto va en blanco sobre oscuro y se salta el segundo `statusBarsPadding` (su cabecera ya consume el inset). **No se arregla un valor dejando rotos los otros seis: no queda ni una rama por estilo.** LECCIÓN: una condición que menciona un AJUSTE pero describe un LAYOUT pertenece a quien controla el layout; en cuanto aparece un segundo anfitrión, la regla se convierte en un agujero silencioso para él | 0.6.143 | ⚠️ pendiente dispositivo |
| 110 | **CAMBIOS EN EL LADO CLÁSICO hechos por esta ronda: el interruptor del EQ deja de mentir cuando lo escribe otro, y dos puertas duplicadas pasan a tener una sola definición** | (a) `AxionEqViewModel._enabled` era una lectura de UNA SOLA VEZ de `echo_eq_prefs["enabled"]`, tomada al construir el ViewModel. Ese archivo tiene **tres escritores**: el propio ViewModel, `MusicService.applyEqForCurrentOutput` (perfiles por salida, `MusicService.kt:1385`) y la migración de siembra (`App.kt:900`). Con cualquiera de los otros dos, el flujo quedaba obsoleto y la pantalla del EQ enseñaba un interruptor que no correspondía al estado real hasta reconstruirse. Salió a la luz al enrutar la barra de motor del reproductor nuevo (`EQ ON/OFF`) a través del ViewModel: hacerlo sin esto habría sido un RETROCESO frente al `OnSharedPreferenceChangeListener` que esa barra tenía por su cuenta. (b) `showPlayerVolumeControl` y `swipeLyricsGestureArmed` existían como expresiones sueltas en los dos reproductores | (a) `OnSharedPreferenceChangeListener` **de solo lectura** dentro del ViewModel (`AxionEqViewModel.kt`), guardado en un campo porque SharedPreferences solo mantiene referencias débiles, y dado de baja en `onCleared`. Asigna el flujo y **nada más**: no llama a `setEnabled` ni a `applyToService`, así que no puede escribir una preferencia, re-emitir un perfil ni tocar el DSP — reaccionar a una escritura ajena no debe convertirse en una segunda escritura. Sin bucle: `StateFlow` no re-emite un valor igual. **Cambio de comportamiento visible**: el interruptor de Ajustes ▸ Ecualizador ahora puede moverse solo cuando un perfil por salida lo enciende; antes se quedaba quieto y mentía. (b) Ambas puertas viven ahora en `ui/player/PlayerAppearancePrefs.kt` como funciones PURAS y las llaman los dos reproductores; en el clásico la expresión resultante es idéntica (`lyricsVisible = true` porque allí `isFullScreen` solo lo enciende la UI de letras, `Player.kt:1640/1770`). **Es lo que por fin hace testeables los cinco arreglos de la ronda anterior, que no tenían NI UN test**: `PlayerAppearancePrefsTest` (14 casos) fija la derivación de colores de botón, la polaridad del control de volumen (la clave se llama `hidePlayerSlider`, NO oculta la línea de tiempo y su fila se llama `hide_player_volume` — quitar la negación invierte el interruptor) y los cuatro términos + el signo del gesto de letras. LECCIÓN: enrutar una lectura «al dueño del dato» solo es una mejora si el dueño está VIVO; si su estado es una foto de la construcción, centralizar empeora la frescura | 0.6.143 | ⚠️ pendiente dispositivo |

## Reglas de contenido de las copias de seguridad (usuario)
- La copia debe guardar **SOLO lo de la biblioteca** (la DB `song.db`): canciones, playlists, likes, artistas, álbumes, historial. **NADA de `settings.preferences_pb`** (cookie/tokens/toggles) para que el restore siempre sea funcional. EQ presets = mantener (contenido del usuario). La licencia (`jr_license`) JAMÁS viaja.
- Backup = **solo local** (Drive descartado por el usuario).
| 111 | **🔴 «Estaba en una playlist, entré al artista, di para atrás y me manda al INICIO» — no era un fallo de navegación, era un GESTO OCULTO en la propia flecha de atrás** | Reportado por el dueño. `NavControllerUtils.backToMain()` hacía `popBackStack()` **en bucle** hasta llegar a una raíz de pestaña (`Screens.MainScreens` = inicio/buscar/escuchar juntos/biblioteca), y estaba enganchado como `onLongClick` **en la MISMA flecha de atrás** cuyo `onClick` es `navigateUp` — en 44 pantallas, entre ellas `ArtistScreen.kt:1227` (el repro exacto), `AlbumScreen.kt`, las cinco de playlist, las cuatro sub-pantallas de artista, `HistoryScreen`, `RecognitionScreen` y ~17 de ajustes/legales. El disparador era el `combinedClickable` de `ui/component/IconButton.kt`: pulsación larga de Compose por defecto (~500 ms), **sin háptica, sin aviso visual, sin confirmación** y con `contentDescription = null`, así que ni se anunciaba ni se podía descubrir. Un pulgar lento o apoyado destruía la cadena entera en silencio. Y como paraba en la raíz **de esa** cadena, el destino cambiaba según cómo hubiera empezado → se percibía como aleatorio. Ya estaba señalado en `docs/UI_INVENTORY.md:3580` y `:3608` y nadie lo actuó. **Segunda cara del mismo fallo:** otras 7 pantallas pasaban `onLongClick = {}` para «desactivarlo» — pero un handler NO nulo hace que `combinedClickable` **se trague** la pulsación larga, así que en esas flechas un toque lento no hacía *nada en absoluto* (ni atrás, ni atajo). **DESCARTADO tras verificarlo (no perseguirlo otra vez):** ninguna navegación de contenido pasa `popUpTo` — los 40+ `navigate("artist/…")` y `navigate("album/…")` son pelados; la barra inferior usa el patrón Material correcto; el lado del *push* está limpio | **Gesto ELIMINADO, no domesticado.** Un atajo que nadie puede descubrir y que destruye el estado de navegación en silencio es peor que no tener atajo, y además es **redundante**: la barra inferior ya lleva a cada raíz de pestaña con UN toque, etiquetado y visible. Ni siquiera se podía «hacer deliberado», porque su destino era intrínsecamente impredecible (dependía del origen de la cadena). Concretamente: `IconButton.onLongClick` pasa a **anulable pero SIGUE SIENDO OBLIGATORIO** (`(() -> Unit)?`, sin valor por defecto); los 56 argumentos (44 `backToMain` + 7 `{}` + variantes con guarda multilínea) pasan a `onLongClick = null`; se borran los 49 imports y la propia `backToMain()`. Pasar `null` es lo único que de verdad desactiva el gesto: la pulsación cae a `onClick` al soltar, así que **un toque lento se comporta exactamente igual que un toque normal**. `Screens.MainScreens` sigue vivo (lo usa la barra inferior, `MainActivity.kt:795`). **🔴 TRAMPA QUE PISÉ Y QUE NO HAY QUE VOLVER A PISAR: mi primer intento fue ponerle `= null` por defecto y BORRAR el argumento en los 56 sitios. Compilación ROTA con ~250 errores en ~20 archivos.** Este `IconButton` se diferencia del de `androidx.compose.material3` **únicamente** en ese parámetro, y decenas de archivos importan LOS DOS. En cuanto `onLongClick` tiene valor por defecto, todos los parámetros tras `onClick` lo tienen, y una llamada pelada `IconButton(onClick = …) { }` encaja con **ambas** firmas → *Overload resolution ambiguity*, más el cascadeo de «@Composable invocations can only happen from the context of a @Composable function». Mantenerlo obligatorio es justo lo que separa las dos funciones: sin argumento resuelve a Material3, con argumento resuelve a la nuestra — igual que antes. **NO darle nunca un valor por defecto a `onLongClick`** (avisado en el KDoc). **LECCIÓN 2: en este `IconButton`, `onLongClick = {}` NO desactiva la pulsación larga — la SECUESTRA. Para desactivarla hay que pasar `null`. Solo poner un `onLongClick` no nulo cuando la pulsación larga sea una función real y descubrible (p. ej. un menú contextual).** Excepción consciente: `ArtistAlbumsGridScreen.kt:120` conserva `onLongClick = {}` porque ahí es `Modifier.combinedClickable`, donde el parámetro **ya era opcional** — la lambda vacía fue una decisión, no relleno obligatorio, y quitarla haría que una pulsación larga reprodujera la canción. Guardián: `ui/component/IconButton.kt` (el KDoc explica el porqué) | 0.6.144 | ⚠️ pendiente dispositivo |
| 112 | **Misma zona, tres desperfectos verificados que agravaban lo anterior: la reentrada borraba la pila, el NavHost reconstruía el grafo en cada fotograma, y una ruta se comía a otra** | (a) `MainActivity.handleDeepLinkIntent` hacía `popUpTo(graph.startDestinationId)` para los accesos directos del lanzador («Buscar»/«Biblioteca»). La app es `singleTask`, así que en arranque **caliente** eso entra por `onNewIntent` con una cadena viva y la tiraba entera → volver a la app por un acceso directo te reseteaba por Inicio, exactamente la misma sensación que el gesto oculto. El widget de playlist tenía el defecto opuesto: `launchSingleTop` pelado solo de-duplica si el destino ya está **encima**, así que N toques en la misma tarjeta apilaban N copias. (b) `ui/utils/AppBar.kt` — `appBarScrollBehavior()` es `@Composable` pero devolvía una instancia **NUEVA en cada llamada, sin `remember`**. `MainActivity.kt:957` la llamaba y **tiraba el resultado**, y acto seguido volvía a llamarla. Esa instancia inestable la captura la lambda constructora del `NavHost`, invalidando su `remember` interno y re-ejecutando `createGraph()` en cada recomposición: desperdicio puro justo en el camino de código del que se queja. (c) `NavigationBuilder.kt` registraba `artist/section_buffer` **después** de `artist/{artistId}`; ambos patrones casan con esa URL y quién gana lo decide la puntuación de mejor-coincidencia de Navigation, no el orden → «ver todo» en una sección sin `moreEndpoint` (`ArtistScreen.kt:916`) podía abrir `ArtistScreen` con `artistId = "section_buffer"` en vez de la rejilla de álbumes | (a) Nueva `NavController.navigateToReentryTarget(route)` en `ui/utils/NavControllerUtils.kt`, usada por las dos ramas: si la ruta **ya está** en la pila se vuelve a ella con `popBackStack(route, inclusive = false)`; si no, se **apila encima** de lo que el usuario estuviera haciendo, de modo que su cadena sobrevive debajo y «atrás» le devuelve ahí. En arranque frío sigue siendo no-op (el destino inicial ya sale del mismo intent). Ya no se menciona `startDestinationId`. (b) `remember(state, snapAnimationSpec, flingAnimationSpec)` alrededor del `AppBarScrollBehavior`, con `canScroll` envuelto en **`rememberUpdatedState`** en vez de usarlo como clave: la instancia queda estable pero sigue llamando a la lambda MÁS RECIENTE, así que es imposible leer una obsoleta (aquí `inSearchScreen` es un `derivedStateOf` y `playerBottomSheetState` un objeto recordado, pero es una utilidad pública y no debe depender de eso). Se borra la llamada desechada de `MainActivity.kt:957`. (c) La ruta pasa a llamarse **`artist_section_buffer`** — sin barra no puede colisionar con `artist/{artistId}` pase lo que pase; se elimina la ambigüedad en vez de reordenar y confiar en la heurística del matcher. Verificado antes de renombrar: **ningún** sitio compara rutas con `startsWith("artist/")`. LECCIÓN: reordenar `composable(...)` NO es un arreglo fiable de rutas que se solapan — hay que hacer que los patrones no puedan casar a la vez | 0.6.144 | ⚠️ pendiente dispositivo |

| 113 | **«Se acabó la lista, entró la cola inteligente y me puso una de otro género, luego una del mío, luego otra que no tenía nada que ver» — el patrón INTERMITENTE era el diagnóstico** | Cuatro fallos encadenados, todos verificados en el código: (a) `ContextProfile.steerTerm` daba **exactamente 0.0** a un candidato de género DESCONOCIDO, mientras uno del género correcto ganaba hasta −4 → una canción del género equivocado en el puesto 2 de relevancia le pasaba por delante a una del género bueno en el puesto 8; (b) `MusicService.withExplorationQuota` corre **DESPUÉS** de ordenar y reserva ~1 de cada 5 huecos para un artista que el perfil de gustos no conoce — y un candidato del que no sabemos el género es «nuevo» casi por definición, así que la cuota ascendía al frente justo a los que el timón acababa de retrasar (el código YA conocía este peligro para el grupo CTX_SINK: el desplazamiento +1000 existe por eso, pero nunca se cerró para el timón normal); (c) el enriquecimiento de géneros corría **fire-and-forget DESPUÉS** de encolar el lote, así que el lote que se PUNTUABA veía el caché como estaba ANTES de aprender nada — el siguiente lote, ya enriquecido, salía bien: de ahí bien/mal/bien/mal en vez de mal siempre; (d) el enriquecimiento era **solo-WiFi por defecto**, o sea muerto en el coche, que es donde él escucha. **No hay ninguna «expansión de artistas relacionados»** (verificado por grep): lo que él percibe como «artistas relacionados que no cantan el mismo género» es la relevancia de YouTube colándose por el agujero del género desconocido | (a) `ContextProfile.UNKNOWN_GENRE_PUSH = 1.0` — **UNO**, porque el término `index` de la clave de orden avanza exactamente 1.0 por puesto: un desconocido pierde el empate contra uno del género correcto y nada más. Sigue sin ser un filtro ni un descarte, es 6 veces menor que el empujón del género CONOCIDO-equivocado (+6), un artista del propio contexto se detecta **sin caché** y se lleva el tirón de −3, y solo actúa si el perfil conoce algún género — así que la regla #39/#41 (una señal derivada del caché no puede colapsar la cola sobre la biblioteca) sigue en pie por construcción; un lote donde NADA se conoce sale en el orden de relevancia de YouTube porque todos suman el mismo +1. (b) UNA regla cubre los dos casos: un candidato que el timón EMPUJÓ HACIA ATRÁS (`ctx > 0`) no puede reclamar un hueco reservado de exploración. Solo sale de la partición «nuevos»: conserva su posición ordenada, no se elimina, y si TODOS los nuevos están bloqueados la cuota simplemente no hace nada. (c) El enriquecimiento pasa a correr **ANTES** de puntuar en los dos caminos (siembra de radio y paginación), con presupuesto de 1,5 s (`ENRICH_BEFORE_SCORE_MS`); se lanza en `scope`, así que el timeout cancela **solo la espera** — la ejecución sigue y llena el caché igual que antes, y en el camino de paginación es **la misma ejecución** movida de sitio, sin red ni batería extra. Nunca puede sonar: en la siembra la canción que termina sigue sonando, en la paginación quedan ~5 temas por delante, y el único caso sin colchón (`resumeAfterSeed`, el reproductor parado esperando estos ítems) se salta la espera entera. **Trampa que pisé y corregí antes de compilar: la espera estaba DESPUÉS de capturar `liveIndex`, o sea reintroduciendo exactamente la obsolescencia contra la que avisa el comentario de esa línea — ahora va lo PRIMERO de `appendSeed`.** (d) `GenreEnrichOnMobileKey` pasa a ON por defecto (`GenreCache.kt` + `ContentSettings.kt`, deben coincidir o el interruptor mentiría): son como mucho 15 consultas diminutas por disparo, solo de artistas nunca vistos, con los fallos definitivos cacheados para siempre → el coste decae a cero. **Sin clave de migración**: es un default de LECTURA, y quien lo apagó a mano tiene `false` GUARDADO, que sigue ganando. Traza nueva `CTX_GENRE` a nivel INFO (conocidos vs desconocidos por lote, cuántos quedan fuera de la exploración, cobertura del perfil) para que esto se resuelva con un log compartido y no a ojo | 0.6.145 | ⚠️ pendiente dispositivo (fin de lista → cola infinita, con datos móviles) |
| 114 | **«En Android Auto otra vez sigo teniendo micro cortes» — el arreglo de 0.6.141 sigue intacto; esto es coste que quedaba en el mismo camino** | Verificado primero que NO es la regresión anterior volviendo: `MusicService.kt:9294` sigue condicionando el re-aplicado a `shuffleOrderStale`, la precomprobación O(1) sigue ahí y `onMediaItemTransition` solo se arma con un SEEK manual. Lo que quedaba: (a) `shuffleItemKeys` llamaba a `player.getMediaItemAt(i)` **por ítem** — que en el bytecode de media3 es literalmente `getCurrentTimeline().getWindow(i, window).mediaItem`, o sea una comprobación de hilo y un salto a `playbackInfo` repetidos N veces en el hilo principal; (b) `(0 until n).toMutableList().sortByDescending { keys[it] }` **boxeaba** un `Integer` por ítem y un `Double` por comparación — O(n log n) objetos efímeros en el hilo principal (el array de claves ya se había aplanado a `DoubleArray` justo por esto; la lista de índices se quedó fuera); (c) el caché de puntuación se vaciaba **ENTERO** cada vez que cambiaba el perfil de gustos (TTL de 5 min), aunque el artista principal de un ítem no puede cambiar con el perfil; (d) el tap del `ForwardingAudioSink` hacía `buffer.duplicate()` **en el hilo de audio** para CADA buffer, incluso cuando `measureExternal` iba a volver en sus dos primeras líneas — que es el caso normal a 16 bits, donde ese procesador ya está dentro de la cadena del sink | **Solo coste, cero comportamiento.** (a) Una sola lectura de `currentTimeline` y una `Timeline.Window` reutilizada para todo el recorrido — misma expresión que hace `getMediaItemAt`, verificada en el bytecode de `media3-common 1.10.1` (`BasePlayer.getMediaItemAt`), con la ventaja extra de usar una `Window` propia en vez de la compartida del reproductor. (b) `ShuffleOrdering.sortIndicesByKeyDescending`: merge sort ascendente por anchura sobre `IntArray`, sin boxeo, **estable**, comparando con `java.lang.Double.compare` — exactamente lo que hace `compareByDescending` al pasar por `Comparable` boxeado, y `Arrays.sort(Object[], Comparator)` es TimSort, o sea también estable. **El orden es IDÉNTICO, no equivalente**: 5 tests diferenciales lo comparan elemento a elemento contra la expresión sustituida, con tamaños 0..4096, claves cargadas de empates, `NaN` y ceros con signo (donde `<`/`>` SÍ discreparían de `Double.compare`), más el invariante de que ninguna canción ya escuchada adelanta a una no escuchada. (c) El caché se parte en dos por su vida real: el gusto se vacía al cambiar el perfil, **el artista sobrevive** — mismos valores, menos trabajo. (d) La asignación queda tras una comprobación de dos volátiles (`needsExternalMeasure()`), las mismas dos condiciones que la llamada ya comprobaba de primero; la contabilidad de la marca de agua no se toca, así que un enganche posterior mide desde donde habría medido igualmente. (e) `prepareSecondaryPlayer` copiaba la cola ENTERA con `player.getMediaItemAt(i)` **por ítem** (mismo defecto que (a)) hacia un `mutableListOf()` **sin dimensionar**, una vez por canción. **Primero se verificó que NO se puede preparar «solo la siguiente canción»**: en el intercambio, `performCrossfadeSwap` hace `player = nextPlayer` — el secundario **SE CONVIERTE** en el reproductor vivo, así que necesita la cola completa (todo lo que el usuario puede rebobinar y todo lo que queda por venir); recortarla destruiría la cola una vez por canción. Lo que sí se puede quitar es el coste por ítem de LEERLA: una sola lectura de `currentTimeline`, una `Timeline.Window` reutilizada y el `ArrayList` predimensionado. En una cola de 3.000 eso elimina **6.002 reentradas al reproductor** (3.001 `getCurrentTimeline` + 3.001 `verifyApplicationThread`) y **15 realojamientos del ArrayList** (~8.317 copias de referencia). El paseo del orden de barajado reutiliza ahora **ese mismo objeto Timeline** en vez de leerlo otra vez — nada puede mutarlo entre medias porque el secundario **no** registra a `MusicService` como listener (`createExoPlayer`: `if (!isSecondary) addListener(this@MusicService)`), así que la garantía pasa a ser estructural en vez de un comentario. **Mismos `MediaItem`, mismo orden, mismo `setShuffleOrder` — la cicatriz del orden barajado que se pierde (filas 90/92/94/96/101/102) NO se reabre**, y no se toca ni la forma ni el tiempo ni las curvas del fundido, ni el gapless, ni el reloj dual, ni el nivelado de sonoridad del entrante. Sin `runCatching` a propósito: hoy tampoco lo hay, y tragarse un fallo ahí entregaría al secundario una cola PARCIAL que se vuelve la viva en el intercambio. **NO tocado a propósito**: `spaceArtists` (ya acotado a O(n·60·5) con corte temprano, sin asignaciones, y su ventana cae a 0 justo en el caso denso que sería caro); **`sec.setMediaItems(items)` en sí** (crea N `MediaSource` en el hilo principal — inherente a que un ExoPlayer sostenga una cola de N: los `MediaSource` no se pueden compartir entre dos reproductores y ExoPlayer está confinado al hilo principal, así que ni se reutilizan ni se mueven fuera); **reutilizar el secundario cuando solo cambió el ORDEN y no el contenido** (haría falta re-buscar el nuevo objetivo y re-cebar sonoridad y silencio-de-entrada para una canción posiblemente distinta — justo el riesgo de salto de nivel en el intercambio; además es el caso raro, una vez por vuelta o tras un salto manual, no el de la radio continua); el bucle por muestra del detector de silencio (~96k llamadas/s solo mientras está enganchado, ~0,1% de CPU, no produce cortes). Matiz que conviene recordar: la copia de la cola ocurre en la PRECARGA (~9 s antes del fundido), no durante el fundido. La línea `SHUFFLE_SPACING` ahora lleva `ms=` (el re-aplicado completo en el hilo principal) para que el siguiente reporte se pueda medir en vez de suponerse | 0.6.145 | ⚠️ pendiente dispositivo (Android Auto, cola larga tras varios `appends`) |
| 115 | **CAMBIO DE COMPORTAMIENTO EN EL LADO CLÁSICO: la función nueva «¿Volver a la cola anterior?» sale TAMBIÉN en la interfaz clásica, a propósito** | Pedida por el dueño: «estoy en una lista, veo una canción que me gusta, me meto en su álbum y lo pongo entero; cuando le dé para atrás y vuelva a donde estaba, que me dé la opción de volver a la cola anterior». **No es un juguete de la beta**: el aviso vive en `MainActivity`, o sea sale en las DOS interfaces, y por eso se anota aquí — todo usuario del reproductor clásico ve a partir de ahora una barra emergente que antes no existía. Si alguien reporta «me sale un aviso raro al salir de una lista», la respuesta es *es la función nueva, y se apaga en Ajustes ▸ Reproductor ▸ Cola*. **Dos desperfectos cazados en la revisión de la propia función, antes de publicarla**: (a) la regla trataba como «desvío» CUALQUIER salto desde una lista a algo con otro contexto, así que pasar de la playlist A a la playlist B — que es *decidir* qué escuchar, no irse a mirar nada — también ofrecía arrastrarte de vuelta: un aviso durante la navegación normal; (b) salía viva para todo el mundo **sin interruptor y sin caducidad**, de modo que apagarla habría exigido un cambio de código, y una oferta armada antes de mandar la app a segundo plano podía aflorar mucho después, desconectada de lo que el usuario estuviera haciendo, con una cola entera retenida en memoria mientras tanto | (a) **La regla se reescribe sobre el concepto, no sobre el prefijo**: UNA sola lista, `LISTENING_LIST_PREFIXES = PL: / AP: / LIB:`, usada en LOS DOS lados — dejar una lista de escucha por algo que NO es una lista de escucha (`AL:` álbum, `AR:` artista, o una cola sin contexto) es un desvío; lo demás no. Verdictos: lista→álbum/artista/cola-sin-contexto **SÍ** (el caso del dueño; el botón Reproducir del álbum y un toque en una pista arrancan un `LocalAlbumRadio` con contexto **null**, así que una regla atada a `AL:` habría compilado, dado test verde y no disparado JAMÁS en sus manos); biblioteca→álbum/artista **SÍ** (`LIB:LIBRARY` es su biblioteca entera como una lista, la cola más larga que la app construye y la de cursor menos reproducible a mano); playlist A→playlist B, lista→auto-lista, lista→biblioteca y biblioteca→lista **NO** (cambiar de lista es decidir, no desviarse); mismo contexto a ambos lados **NO**, y por construcción; álbum→álbum, artista→artista, álbum→artista y álbum→lista **NO** (un álbum no es origen: pulsar Reproducir otra vez cuesta la posición de un tema, mientras que el cursor de una lista sí se pierde de verdad — y además solo llevaría `AL:` si se arrancó desde su botón Aleatorio, así que disparar por él sería disparar en una de dos acciones idénticas a la vista); origen sin contexto (radio, mezcla, búsqueda, el `EmptyQueue` del restore) **NO**. Matriz completa fijada por test (`PreviousQueueOfferTest`, 3×3 orígenes × destinos además de los casos con nombre). (b) **Interruptor** `PreviousQueueOfferKey` (`PreferenceKeys.kt:340`, por defecto **ON**) con su fila en Ajustes ▸ Reproductor ▸ Cola (`PlayerSettings.kt:1042`), y se honra **en la CAPTURA**, no en el aviso ni en la acción: `MusicService.captureQueueForResumeOffer` (`MusicService.kt:412`) devuelve en su PRIMERA línea leyendo un espejo `@Volatile` (`previousQueueOfferEnabledHint`, sembrado desde la foto de preferencias del arranque en frío, `MusicService.kt:1740`, para que no exista una ventana antes de la primera emisión del colector) — con el interruptor apagado no se toma instantánea ninguna, así que no hay cola retenida ni botón que no haga nada; apagarlo con una oferta ya armada la retira en el acto desde el mismo colector. (c) **Caducidad de 10 minutos** (`PreviousQueueRule.OFFER_TTL_MS`, `PreviousQueueOffer.kt:42`) sobre `elapsedRealtime()` — cuenta el tiempo que el móvil pasa dormido, que es justo el caso que acota. Se comprueba en TRES sitios porque el temporizador es un `delay` del hilo principal y **no avanza en sueño profundo**: el servicio suelta la instantánea al vencer, la carcasa se niega a levantar un aviso ya caducado (`MainActivity.kt:1167`) y `resumePreviousQueue` se niega a reinstalar una cola vencida. **Invariantes respetados**: la instantánea sigue siendo **solo memoria** (deliberadamente fuera de `saveQueueToDisk`: una muerte de proceso la tira), reutiliza `PersistQueue`/`toPersistQueue`/`toQueue` (no hay segunda serialización), no toca semántica de reproducción, EQ, crossfade, Superpowered, offload ni licencia, y el aviso es un snackbar: no interrumpe el audio, no roba el foco y desaparece solo | 0.6.145 | ⚠️ pendiente dispositivo (lista → álbum → atrás; y comprobar que con el interruptor en OFF no sale nunca) |
| 116 | **🔴 SEGUNDA VEZ que una señal derivada del caché se usa como FILTRO de exclusión (regla #39 otra vez): la radio infinita se estrechaba sobre los artistas que él YA tiene** | Colisión entre dos cambios de la MISMA ronda (fila 113), ninguno de los dos culpable por sí solo — por eso cada uno pasó su propia revisión. (a) `ContextProfile.steerTerm` pasó a devolver `UNKNOWN_GENRE_PUSH` (+1) en vez de 0.0 para un candidato de género DESCONOCIDO siempre que el perfil conozca algún género; (b) `MusicService.orderedByTaste` añadió `if (ctx > 0.0) explorationBlocked.add(id)`, escrito pensando en el género CONOCIDO-equivocado. Juntos, ese `ctx > 0` captura **todos** los candidatos de género desconocido. Y `GenreCache` solo se llena con artistas que ya rondan al usuario, así que **un artista que la radio descubre por PRIMERA vez es desconocido POR CONSTRUCCIÓN**: con caché frío o parcial, `withExplorationQuota` veía `isFresh = ... && mediaId !in blocked` falso para cada candidato nuevo, `fresh` quedaba vacío, `if (fresh.isEmpty() \|\| known.isEmpty()) return this` se rendía y la cuota de exploración (~1 de cada 5 huecos reservados para un artista desconocido) **no hacía nada en ninguna continuación automática**. La cabeza de cada lote la decidía entonces `index - pull`, y `pull` favorece hasta 8 puestos a los artistas que ya están en el perfil de gustos, sobre una página de ~12-25. O sea: exactamente lo que la fila #39 prohíbe — una señal sacada de un caché sesgado usada para excluir categóricamente todo lo demás de la reserva de descubrimiento | La regla se estrecha al ORIGEN de la señal, no a su valor: `ContextProfile.blocksExploration(steerTerm, candidateGenre) = steerTerm > 0.0 && candidateGenre != null` — **solo un género que sabemos NOMBRAR y sabemos fuera de contexto compra una exclusión**; la ausencia de género no compra ninguna. En `MusicService.orderedByTaste` el `lane` se saca fuera del `steerTerm` (`ctxLane`) y se pasa a esa función; si la búsqueda del lane lanza, `ctxLane` queda null, así que un fallo solo puede hacer la regla **más permisiva**, nunca bloquear a ciegas. Los ids de `CTX_SINK` siguen bloqueados: entran por `deprioritized` al construir el conjunto, no por esta regla. `UNKNOWN_GENRE_PUSH` vuelve a ser lo que su propio KDoc promete: un empujón de UN puesto en la clave de orden, jamás un filtro. Tests nuevos en `ContextProfileTest`: `unknownGenreNeverBuysAnExplorationBlock` (perfil con géneros + candidato desconocido → empujado, pero SIGUE siendo elegible para la reserva) y `knownOffContextGenreStillBlocksExploration` (rock bajo un perfil de salsa, y también el caso ablandado por el desempate en español +5 → siguen bloqueados, así que el arreglo original de «deja de mezclar géneros» no se deshace). **LECCIÓN, ya por segunda vez: antes de usar un valor derivado de `GenreCache` (o de cualquier caché que solo se llene con lo que el usuario ya tiene) pregúntate si «no lo sé» acaba comportándose como «no». Un NUDGE puede tratar el desconocido como sospechoso; una EXCLUSIÓN no puede.** También se corrigieron dos comentarios que afirmaban propiedades falsas en el mismo lote (sin cambio de comportamiento): `needsExternalMeasure` no lee «dos volátiles» (`isActive` es un campo normal), y el enriquecimiento movido a antes de puntuar no usa «los mismos artistas» que el fire-and-forget que sustituye (lee la página CRUDA, porque la lista final aún no existe — mismo tope de 12 y misma puerta de WiFi) | 0.6.145 | ⚠️ pendiente dispositivo (fin de lista → cola infinita, con el caché de géneros recién instalado/vacío: deben aparecer artistas nuevos) |
| 117 | **Las filas de reproducción automática de la cola CLÁSICA no se podían tocar (el mismo fallo que él reportó, arreglado solo en la interfaz nueva)** | `AutomixSongRow` (`ui/player/Queue.kt`) montaba `combinedClickable(onClick = {}, onLongClick = …)`. Como `onLongClick` NO es nulo, el modificador SÍ se instala: la fila hace ripple, parece pulsable y no hace absolutamente nada. Afecta a la pestaña RELACIONADO entera y al pie de «reproducción automática» de A CONTINUACIÓN, es decir a todos los usuarios de la estable — la única entrada real eran los dos iconos pequeños o el menú de pulsación larga | `onClick` reproduce igual que la interfaz nueva: `playNextAutomix(item, at)` + `seekToNext()` + `playWhenReady = true` salvo casting, y respetando la misma regla de invitado de Escuchar juntos que ya usaban las filas de la cola. La APARIENCIA de la fila no cambia; solo deja de ser un placebo. Guardián `ui/player/Queue.kt` (`AutomixSongRow`, el bloque `combinedClickable`) | 0.6.145 | ✅ (encontrado al PORTAR a la clásica el arreglo de `ui/newui/AuraQueue.kt`) |
| 118 | **El índice de composición llegaba a `removeAt` sin comprobar: quitaba de la reserva de autoplay la canción EQUIVOCADA, o crasheaba** | `MusicService.playNextAutomix` / `addToQueueAutomix` hacen `automixItems.value.toMutableList().apply { removeAt(position) }` SIN comprobar límites, y la reserva se vacía desde FUERA de la hoja de cola (el reproductor encola `automix[0]` cuando la cola se agota; el servicio republica la lista entera en cada cambio de pista). Un toque que caía entre el encogimiento de la lista y la recomposición de la fila borraba otra canción (la tocada sobrevivía y podía encolarse dos veces = duplicado), y un toque en la última fila tras un encogimiento llamaba a `removeAt(size)` → `IndexOutOfBoundsException` en el hilo principal dentro de un `onClick` = cierre de la app | La posición se resuelve por IDENTIDAD en el momento del toque, no por el índice de la fila: `automixPoolPosition(pool, mediaId)` (`ui/player/Queue.kt`, `internal` para poder fijarlo con test) y `if (at >= 0)` en las tres acciones (tocar la fila, «reproducir siguiente», «añadir a la cola»). El `index` de la fila ya solo decide `listItemShape`. Test guardián `app/src/test/kotlin/iad1tya/echo/music/ui/player/AutomixPoolPositionTest.kt` | 0.6.145 | ✅ (encontrado al PORTAR a la clásica el `poolPosition()` de `ui/newui/AuraQueue.kt`) |
| 119 | **Ajustes ▸ «Escuchar juntos» de la interfaz CLÁSICA envenenaba la pila guardada de pestañas — el mismo mecanismo que la fila anterior de Biblioteca→Ajustes** | `Screens.ListenTogether` es un destino de PRIMER NIVEL (`Screens.MainScreens`) y, con «Escuchar juntos en la barra superior» apagado, una celda real de la barra inferior. `SettingsScreen.kt` llegaba a él con un `navigate()` pelado, encima de Ajustes, que a su vez estaba encima de una pestaña. El siguiente toque de pestaña hace `popUpTo(start){saveState}` y androidx-navigation archiva un pop de varias entradas como UNA sola pila guardada, indexada por el destino MÁS PROFUNDO; `restoreState` la devuelve entera, así que esa pestaña abría Escuchar juntos — para siempre, porque la restauración recrea la forma que produce el guardado | `navController.navigateAsTab(Screens.ListenTogether.route)` — el mismo bloque de opciones que usa la barra inferior, ya `internal` en `MainActivity.kt` justamente para estas puertas. Sin cambio visual. Guardián `ui/screens/settings/SettingsScreen.kt` (celda «Escuchar juntos»). ⚠️ Queda una puerta CRUDA en `MainActivity.kt:1999` (`SettingDialoge.onNavigate` → `navigate(route)`), archivo bloqueado por otro agente: mientras siga así, entrar a Ajustes por ese diálogo sigue guardando `settings` en la pila de la pestaña | 0.6.145 | ✅ (encontrado al PORTAR a la clásica el arreglo `navigateAsTab` de la interfaz nueva) |
| 120 | **La portada del reproductor CLÁSICO se dibujaba como un RECTÁNGULO con dos barras grises cuando su hueco es más bajo que ancho** | `calculateThumbnailDimensions` calcula el cuadrado del retrato SOLO con el ancho (`containerWidth - 64dp`) e ignora el alto que recibe (la rama apaisada ya hacía `minOf(ancho, alto)`); el tope por altura se le pasaba únicamente al anfitrión `OPAQUE_DARK` (interfaz nueva) y la clásica seguía mandando `Dp.Unspecified`. Donde el hueco es más bajo que `ancho - 64dp`, `Modifier.size(thumbnailSize)` lo recorta la restricción de altura entrante: el «cuadrado» se mide como rectángulo ancho, la imagen (`ContentScale.Fit` por defecto) se ajusta por altura y los laterales sobrantes se pintan con `surfaceVariant`, además de aplicar el radio de esquina del usuario al rectángulo en vez de a la portada. Alcanzable en estable: pantalla dividida / ventana múltiple en vertical, pantalla exterior de plegable, y cualquier móvil al reducir Ajustes ▸ Tamaño de pantalla o fuente — y la clásica llega antes al recorte porque gasta ~60 dp del hueco en `ThumbnailHeader` | Dejar de tratar el tope por altura como privilegio de un anfitrión: `else -> maxHeight` y `isWideLayout -> minOf(WIDE_MAX_THUMBNAIL_SIZE, maxHeight)`. `calculateThumbnailDimensions` solo recorta HACIA ABAJO, así que donde el hueco es alto (el móvil normal) el valor es idéntico al de hoy y la clásica no cambia en nada; donde es bajo, la portada vuelve a ser cuadrada. Test guardián `app/src/test/kotlin/iad1tya/echo/music/ui/player/ThumbnailDimensionsTest.kt` | 0.6.145 | ✅ (encontrado al PORTAR a la clásica el `maxThumbnailSize` de la interfaz nueva) |
| 121 | **🔴 El actualizador instalaba SIEMPRE la versión ANTERIOR salvo que el usuario pulsara «Borrar actualizaciones descargadas» (reportado por el dueño; afecta a TODOS los usuarios de la estable)** | **Dos causas, ambas silenciosas — ni cierre ni mensaje de error: el usuario ve el diálogo de instalación normal, instala, y sigue en la versión vieja creyéndose al día, así que todo arreglo que le mandamos es invisible (y sus reportes de bugs no son fiables: no sabemos qué versión corre de verdad).** (a) **El nombre de archivo era FIJO**: `UpdateDownloadWorker` guardaba todo en `echo_updates/echomusic.apk`, así que el APK de la versión anterior seguía ahí con el nombre exacto que la siguiente descarga iba a usar. (b) **WorkManager REPRODUCE el último resultado de un trabajo único durante días**: al abrir la pantalla, `getWorkInfosForUniqueWorkLiveData("update_download")` entregaba de inmediato el `SUCCEEDED` de la actualización ANTERIOR, con su `file_path` en `outputData` → `isDownloadComplete = true` y `downloadedFile` = ese APK viejo → el botón ponía **«Instalar»** nada más entrar y instalaba el archivo viejo, **sin descargar nada**. La única comprobación previa (`if (!downloadedFile.exists())`) solo miraba si el archivo EXISTÍA, y `ApkSignatureVerifier` solo mira el certificado — que el APK viejo también pasa, porque es un APK legítimo de Aura. **Nada leía la versión del archivo.** Por eso borrar los APK descargados «arreglaba» el flujo: al no existir el archivo, el guardia reseteaba el estado y el botón volvía a ser «Actualizar». Con el nombre fijo, además, un reintento pedía `Range: bytes=<tamaño del APK viejo>-` sobre el APK NUEVO → 206 pegando la cola del nuevo sobre el cuerpo del viejo (archivo Frankenstein) o **416 → `Result.retry()` para siempre**, dejando el trabajo único ENQUEUED con la URL VIEJA: con `ExistingWorkPolicy.KEEP`, cada toque posterior de «Actualizar» era absorbido por ese trabajo caducado, que descargaba el APK ANTERIOR. También faltaba control de descarga truncada (el bucle de lectura termina limpio cuando la conexión muere → se daba por completa). **(c) Y una TERCERA vía con el mismo síntoma, encontrada al auditar el chequeo**: `checkForUpdate` decidía «hay actualización» con `currentClean != targetClean` — cualquier release DISTINTA, también una ANTERIOR. Como `releases/latest` de GitHub **se salta las prereleases** (el CI publica los `-beta` como prerelease), a quien va en una beta se le ofrecía la estable anterior como si fuera nueva: aceptarla es bajar de versión. `isNewerVersion` existía en el archivo pero **no se llamaba desde ningún sitio**, y su `split(".")` habría dado 0.6.146-beta1 → [0,6,0] (`"146-beta1".toIntOrNull()` = null), o sea habría considerado nueva cualquier 0.6.x | **Nombre versionado + verificación del propio archivo + estado por versión.** (1) `UpdateApkFiles.kt` (nuevo, lógica pura): `apkFileName("v0.6.146")` → `Aura-Hi-Res-Player-0.6.146.apk`, con saneado del texto (llega de la API de GitHub → entra en una ruta) y `.apk.part` para lo no terminado (un parcial no se lista como APK ni puede llegar al instalador). (2) `ApkVersionInspector.kt` (nuevo): `PackageManager.getPackageArchiveInfo` lee versionName/versionCode **del APK en disco** — lo único que un archivo rancio o truncado no puede falsear (si no parsea, devuelve null = truncado). Si no coincide, **se borra y se vuelve a descargar**; nunca se instala. Veredicto en `UpdateApkFiles.verdictFor` (puro): coincide con el tag → instalar; no coincide pero versionCode > instalado → instalar y avisar en el log (que el tag y el versionName se separen no puede dejar la app sin poder actualizarse NUNCA); si no → RECHAZAR. (3) La pantalla se ata a la versión ofrecida: el observador filtra por la etiqueta `update_version:<versión>`, así que un `SUCCEEDED` de otra release ya no la puede tocar, y el `enqueueUniqueWork` usa REPLACE cuando el trabajo pendiente es de OTRA versión (KEEP sigue valiendo para la misma → un re-toque reanuda). (4) Tope de 5 intentos: un trabajo en bucle de reintentos ya no acapara el hueco único. (5) Limpieza: cada descarga borra los APK de otras versiones de la carpeta privada y deja **una sola** copia en Descargas (antes se acumulaba un APK de ~100 MB por release, y una re-descarga añadía un `… (1).apk`); el filtro solo toca nombres `Aura-Hi-Res-Player-*.apk` que la app misma escribe, nunca `-NOSUB` ni nada ajeno. **«Borrar actualizaciones descargadas» pasa a ser mantenimiento, no un remedio: el flujo normal ya no lo necesita.** Guardianes: `echomusic/updater/UpdateApkFiles.kt`, `ApkVersionInspector.kt`, `downloadmanager/UpdateDownloadWorker.kt` (nombre versionado + `.part` + verificación), `echomusicupdater.kt` (filtro por etiqueta de versión + verificación antes de instalar). (6) `UpdateApkFiles.isNewerRelease` sustituye al `!=`: partes numéricas comparadas como NÚMEROS (0.6.9 < 0.6.10) y sufijo de prerelease por DEBAJO de la versión sin él (0.6.146-beta1 < 0.6.146), así que una beta ya no recibe la estable anterior como «actualización». Test `app/src/test/kotlin/iad1tya/echo/music/echomusic/updater/UpdateApkFilesTest.kt` (23 casos) | 0.6.146 | ⚠️ pendiente dispositivo (con un APK viejo ya en disco: la pantalla debe ofrecer «Actualizar», descargar de verdad, y la versión instalada al terminar debe ser la nueva) |
| 122 | **La cola del reproductor ANCHO clásico ignoraba el Cast: tocar una fila mientras emites saltaba en el MÓVIL, no en el altavoz** | `LandscapeQueuePane` (`ui/player/Player.kt`) no recibía el estado de Cast aunque el reproductor sí lo tiene en ámbito, así que su `onClick` hacía siempre `togglePlayPause()` / `seekToDefaultPosition()` sobre el reproductor LOCAL. Emitiendo, el altavoz seguía con la canción anterior y el toque parecía no haber hecho nada. Encontrado al construir la columna de cola de la interfaz nueva, cuya versión (`auraJumpToQueueWindow`) sí tiene las ramas | Pasarle `castHandler` / `isCasting` / `castIsPlaying` (no re-derivarlos con otro `collectAsState`, o esta columna tendría su propia opinión de «estamos emitiendo» que puede discrepar un fotograma con la del transporte) y replicar la lógica de la interfaz nueva: fila actual → play/pause del receptor; otra fila → `navigateToMediaIfInQueue`, y si el receptor no tiene ese id en su cola, saltar localmente (que es lo que vuelve a emitir la pista correcta). Compila en las DOS variantes: en FOSS `navigateToMediaIfInQueue` es un stub que devuelve `false` | 0.6.146 | ✅ (encontrado al PORTAR a la clásica la columna de cola de la interfaz nueva) |
| 123 | **Cuarta puerta cruda de navegación: el diálogo de BÚSQUEDA DE AJUSTES podía envenenar la pila de una pestaña** | `SettingDialoge.onNavigate` (`MainActivity.kt`) hacía `navController.navigate(route)` con la ruta que devuelve el buscador — y esa ruta puede ser de PRIMER NIVEL (`settings` lo es bajo el armazón nuevo, y también cualquier `Screens.MainScreens`). Mismo mecanismo que las filas 119 y la de Biblioteca: dos destinos de primer nivel apilados, el siguiente toque de pestaña los archiva como UNA pila indexada por el más profundo, y esa pestaña abre la pantalla equivocada el resto de la sesión | Distinguir por tipo de ruta: `navigateAsTab` SOLO si es de primer nivel, `navigate` normal para las de detalle. Esto último es esencial — `navigateAsTab` hace `popUpTo(start)`, así que aplicarlo a `album/…` o `settings/xxx` tiraría la pila del que llamó. Con esto no queda ninguna puerta cruda a un destino de primer nivel en toda la app | 0.6.146 | ✅ (predicha por el KDoc de `navigateAsTab` y localizada por la verificación adversarial final) |

| 124 | **Vídeo se traba y al rato continúa (post-#43)** | (a) `PlayerVideoSurface` seguía llamando `setVideoTextureView` en **cada** `AndroidView.update` cuando `isAvailable` — media3 **no** es no-op: teardown + rebind → fotograma congelado que luego vuelve. (b) `maybeRecoverStuckVideo` trataba `STATE_BUFFERING` ≥3s como “atascado” y hacía `prepare()` → reinicio del stream en rebuffers normales (= traba → continúa) | (a) rebind solo si cambia la instancia de TextureView o `videoUrl`; (b) recovery **solo IDLE**; BUFFERING cancela el job. Guardianes: `PlayerVideoSurface.kt`, `MusicService.maybeRecoverStuckVideo` | 0.6.161 | ⚠️ pendiente dispositivo |

| 125 | **Cortes de audio en canciones CON vídeo disponible, modo canción (pantalla on/off)** | `prefetchCurrentVideoUrl` resolvía `preferVideo=true` en cada transición y, tras el primer uso de vídeo, **sin tope** — competía por los mutex globales de cipher/PoToken con el resolve de audio del hilo del loader → buffer starve. También `maybeWarmVideoConnection` sin gate térmico/pantalla | Cap SIEMPRE ≤8/sesión; saltar si `audioStreamResolveInFlight` o `YTPlayerUtils.isStreamResolveBusy` o `ThermalManager.isHot`; warm solo con pantalla interactiva + no hot + locks libres. Guardián `MusicService.prefetchCurrentVideoUrl` / `maybeWarmVideoConnection` | 0.6.176 | ⚠️ pendiente dispositivo |
| 126 | **#57 mid-song: throw de formato cortaba la canción** | El format-guard lanzaba `PlaybackException` en offset≠0 al cambiar de contenedor (Lossless/Saavn) → corte audible | Ya no lanza: purga caché + upsert FormatEntity y continúa. Guardián el bloque mid-stream en el ResolvingDataSource de `MusicService` | 0.6.176 | ⚠️ pendiente dispositivo |
| 127 | **Cola automática mezclaba tutoriales / vídeos no musicales** | Solo existía `filterVideoSongs` (forma MV vs audio); ítems con `musicVideoType == null` entraban por radio/related | `filterNonMusicForAutoQueue` en appends automáticos + skip en `onMediaItemTransition` si `YouTubeQueue` y tipo null. Guardián `queues/Queue.kt` + transición en `MusicService` | 0.6.176 | ⚠️ pendiente dispositivo |
| 128 | **Android Auto inalámbrico: canciones se paran a cada rato** | `AUDIO_BECOMING_NOISY` pausaba; resume solo con `ResumeOnBluetoothConnectKey` (default OFF) y solo A2DP/SCO | Con sesión Auto: marcar `pausedByNoisy` y reanudar al recuperar ruta (BUS/USB/DOCK/A2DP); pause de usuario limpia el flag. Guardián `becomingNoisyReceiver` + `audioDeviceCallback` | 0.6.176 | ⚠️ pendiente dispositivo |
| 129 | **Tras reiniciar el móvil no quedaba la continuación** | `onDestroy` no corre en reboot duro; solo timers periódicos | Receiver `ACTION_SHUTDOWN`/`REBOOT` → `saveQueueToDisk(synchronous)` + posición síncrona. Guardián `shutdownSaveReceiver` | 0.6.176 | ⚠️ pendiente dispositivo |
| 130 | **Aleatorio/radio: ventana anti-repetición corta** | `recentRadioIds` capped a 60; restore shuffle #101 ya existía | Cap `recentRadioIds` → 120; verify restore `beginShuffleSession` true→true + New UI `AP:`/`LIB:` contextIds. Guardián `rememberRecentRadioId` | 0.6.176 | ⚠️ pendiente dispositivo |
| 131 | **Artista New UI: año del álbum cortado bajo la portada** | `AuraDoubleRowShelf` usaba `auraShelfCardStackHeight(AuraAlbumShelfWidth)` pero `AuraTypedYtCoverCard` dibujaba a `shelfWidth * 1.05` → ~8dp de menos; además bloques de subtítulo = exactamente `lineHeight` recortaban glifos | Misma escala en rowHeight + cardScale; stack text +80.dp; +2.dp en bloques título/subtítulo. Guardián `AuraArtistScreen` álbumes shelf + `auraShelfCardStackHeight` / `AuraCoverCard` | 0.6.177 | ⚠️ pendiente dispositivo |
| 132 | **🔴 0.6.176 PLACEBO: álbumes/playlists no reproducían (toque vacío o sonaba otra cosa)** | `filterNonMusicForAutoQueue()` se encadenó en `playQueue` para TODAS las colas. Canciones DB tienen `musicVideoType=null` salvo vídeo → cola vacía → `return@launch` silencioso; seguía la cola/radio anterior | Quitar filtro de `playQueue` inicial; `YouTubeQueue.automaticRadio`; NO_MUSIC skip solo si `automaticRadio`; nextPage filtra solo radio/album-radio. Guardián `MusicService.playQueue` + `YouTubeQueue.automaticRadio` | 0.6.178 | ⚠️ pendiente dispositivo |
| 133 | **AutoEqScreen: “Auto-EQ activado” apagaba el EQ maestro** | Switch cableado a `setEnabled()` | OFF→`clearAutoEq()`, ON→`reEnableAutoEq()`; checked=`autoEqActive`. Guardián `AutoEqScreen` | 0.6.178 | ⚠️ pendiente dispositivo |
| 134 | **Avisos: poco frescos / sin popup / sin caducidad** | Solo cold start + throttle 5 min; sin TTL; leídos seguían en lista | Refresh onStart + poll 1h; TTL 24h. **Owner 0.6.198: NUNCA popup** — solo punto rojo en avatar; abrir Avisos marca leídos. Guardián `OwnerAnnouncements.applyPopup` = null + `AuraNoticesScreen` markAllRead + `AuraShell` BadgedBox | 0.6.179→ | ✅ owner: sin popup |
| 135 | **Avisos: el último leído parpadeaba al abrir la app** | `loadCache` llamaba `applyPopup` sin filtrar leídos | Popup desactivado por completo (`applyPopup` siempre null). Guardián `OwnerAnnouncements.applyPopup` | 0.6.181→ | ✅ |
| 136 | **Instalación nueva: salían TODOS los avisos de las últimas 24h (incl. errores viejos)** | El feed remoto conserva varios ítems; sin historial local, todos contaban como no leídos | Bootstrap one-shot: solo el aviso MÁS NUEVO queda activo; el resto se marca leído. Guardián `OwnerAnnouncements.consumeHistoricalBacklogOnce` | 0.6.183 | ⚠️ pendiente dispositivo |
| 137 | **Miniatura de notificación muestra el icono de Aura en vez de la portada** | `CoilBitmapLoader.createFallbackBitmap()` dibujaba `ic_launcher_nobg` sobre placa teal. `loadBitmap` devolvía eso si Coil/HTTP fallaban; `CacheBitmapLoader` lo memoizaba como carátula de la canción. Si el future fallaba del todo, MediaStyle sustituía el **smallIcon** (el mismo launcher) como LARGE artwork | `CoilBitmapLoader.kt`: fallback = placa opaca `#121212` SIN logo; `MAX_ARTWORK_PX` sigue 384 (#26/#54). Nunca devolver el launcher como artwork | 0.6.189 | ✅ |
| 138 | **EQ gráfico: no se podía desplazar con el dedo presionado izquierda/derecha** | `EqBandSlider.pointerInput` ponía `bandDragActive=true` en `awaitFirstDown` → `horizontalScroll(..., enabled=false)` al tocar una banda. El Slider rotado también capturaba el gesto | `AxionEqScreen.kt` `BandEqCard`: scroll horizontal sigue enabled al tocar; overlay `detectVerticalDragGestures` solo consume el eje vertical (fader). #49(b) intacto: en pane ancha se estira con `weight`, debajo se hace pan | 0.6.189 | ✅ |
| 139 | **🔴 0.6.190: Buscar vacío / no responde** | All-tab hacía `if (first is ArtistItem) take(2)`. El "Top result" de YTM es una estantería **mixta** (card de artista + canciones). Truncarla dejaba 2 filas y el resto parecía muerto. Tras 3 fallos de red el skeleton no tenía fin | `forAllTabSearchPreview()`: cap 2 **solo** si TODOS son artistas. `OnlineSearchViewModel.hasFailed` + Reintentar. Guardián `SearchAllTabPreview.kt` | 0.6.191 | ⚠️ pendiente dispositivo |
| 140 | **🔴 0.6.190: artista en carga infinita / sin canciones** | Con biblioteca local se **ocultaban** las populares de YTM (`isArtistPopularSectionTitle`) y se ponía la biblioteca en ese hueco. Sin cache de página = shimmer; con cache = página casi vacía | No filtrar populares. "Tu biblioteca" una vez, **después de álbumes y vídeos** (no en el hueco de populares). Sin duplicar "De tu biblioteca" de YTM. Guardián `AuraArtistScreen` `onlineArtistSections` + `artistLibraryInsertIndex` | 0.6.191 | ⚠️ pendiente dispositivo |
| 141 | **Novedades: el mismo disco/canción en varias estanterías** | Héroe = radar+nuevos; luego se volvían a pintar ambos; charts alimentaban featured **y** listening; moment (semana) ⊂ top diario (histórico) | `claimUnique` first-seen across shelves; listening excluye ids de featured. Guardián `AuraNovedadesScreen` + `NovedadesViewModel.applyCharts` | 0.6.191 | ⚠️ pendiente dispositivo |
| 142 | **Aleatorio con memoria solo en playlists creadas; ✓ de ya oídas desaparecía con el interruptor OFF** | (a) Listas seguidas / YTM abrían `YouTubePlaylistQueue` **sin** `contextId`. (b) Álbum/EP: Reproducir y toque de pista no pasaban `AL:`. (c) `rememberPlayedShuffleSet` devolvía vacío si `EnhancedShuffleKey` OFF, así que la marca no usaba `totalPlayTime`. (d) `incrementTotalPlayTime` era UPDATE y no insertaba la fila | `OL:<browseId>` compartido local/online; `AL:` en Play/toque; ✓ = memoria ∪ `totalPlayTime`; insert antes de incrementar. Guardián `ShuffleContexts` / `YouTubePlaylistQueue.contextId` / `rememberHeardSongIds` | 0.6.192 | ⚠️ pendiente dispositivo |
| 145 | **Sincronización automática de biblioteca era placebo: nunca se programaba al arrancar** | `YtmAutoSyncFreqDaysKey` default 0 (off); `YtmAutoSyncWorker.schedule` solo al tocar una fila en `YtmSyncScreen`. `App.onCreate` no reafirmaba el unique work | Default 3 días si la clave no existe; `scheduleFromPrefs` desde `App.scheduleNonCriticalWork` cada arranque (proceso default, IO). `0` guardado = el usuario apagó. Guardián `YtmAutoSyncWorker.scheduleFromPrefs` + `App.scheduleNonCriticalWork` | 0.6.193 | ✅ |
| 143 | **Me gusta + auto-descarga: el volumen SUBE un poco a mitad de canción** | Tres agujeros. (1) `cleanupCrossfade` ponía `lastNormalizedId = null` al TERMINAR el fundido — la canción entrante ya llevaba 5–9 s sonando, así que el candado de ganancia quedaba desarmado el resto del tema. (2) Ventana de 8 s que aplicaba `loudnessDb` real cuando llegaba (Me gusta suele ser al empezar). (3) La descarga escribe `FormatEntity` (Room Flow) → `setupLoudnessEnhancer` recomputaba. El ramp nativo de Safe Volume (~300 ms) no oculta un +3…7 dB: se oye como “subió un poco”. | Ganancia CONGELADA para este play (`isPlayingLoudnessFrozen`). El resolve del stream (misma respuesta que la URL, sin red extra) llama `lockLoudnessIfCurrent` ANTES de que `open()` devuelva → el primer sample ya va nivelado. `cleanupCrossfade` ya no desarma el candado. Medición solo se cachea para la SIGUIENTE vez. Guardián `MusicService.lockLoudnessIfCurrent` + `AudioGain.isPlayingLoudnessFrozen` | 0.6.194 | ⚠️ pendiente dispositivo |
| 144 | **Escuchar juntos: crear sala se pega en Galaxy; los móviles nunca suenan a la vez** | (a) Handshake WSS en `CONNECTING` sin watchdog — Galaxy/Private DNS/Knox/HF sleep pueden completar TCP y nunca el 101; `connect()` se niega a reintentar. (b) `isCreatingRoom` no se pintaba (solo Unirse tenía spinner) y el menú del reproductor ignoraba `ConnectionError`. (c) Cada móvil stremea YouTube por su cuenta; `serverTime` nunca llega; tolerancia 2–3 s = eco permanente; PING esperaba 25 s antes del primer RTT; heartbeat cada 10 s | Watchdog 20 s; CREATE/JOIN fallan a la primera (sin 15 reintentos); timeout 15 s si el servidor no contesta; spinner Crear; RTT/2 (`networkCompensatedPosition`) en PLAY; tolerancia 400/600 ms; heartbeat 5 s; ping al conectar. Guardián `ListenTogetherClient.armHandshakeWatchdog` + `ListenTogetherSync.networkCompensatedPosition` | 0.6.194 | ⚠️ pendiente dispositivo |
| 146 | **Artista New UI: Tu biblioteca pegada bajo las populares aplastaba el diseño** | `insertLibraryAfterPopular` la metía en el primer hueco no-popular (antes de álbumes/vídeos) | Insertar **después** de álbumes y vídeos (`artistLibraryInsertIndex`, rank ≤ 5). Populares intactas. Guardián `AuraArtistScreen.artistLibraryInsertIndex` | 0.6.194 | ⚠️ pendiente dispositivo |
| 147 | **Reproductor: el círculo de descarga salía enorme** | El anillo usaba `AuraSpacing.MinTouchTarget` (48 dp) sobre un glifo de 20–22 dp | Anillo = glifo + 8 dp. El 48 dp sigue siendo solo el área táctil del Box. Guardián `AuraPlayer.kt` `downloadRing` | 0.6.194 | ⚠️ pendiente dispositivo |
| 148 | **Vídeo↔canción: al volver no cambia; descarga pegada; se para al inicio; título largo cortado** | (a) `swapToVideo` ponía `playWhenReady=false` hasta 2.5s de buffer / 10s — pausa garantizada, y si salías antes `exitVideoMode` capturaba “pausado” y el siguiente enter no reanudaba ni disparaba `onRenderedFirstFrame`. (b) Portada encima del TextureView hasta el primer frame: al reentrar el decoder ya había pintado → overlay eterno. (c) `sendPauseDownloads` *antes* de que el swap confirmara: un early-out dejaba las descargas pausadas y el icono sin reacción; like-en-vídeo difería la descarga sin que la UI lo viera. (d) `basicMarquee(iterations=1)` solo recorría el título una vez | Quitar el start-gate; `videoSwapGeneration` ignora resolves viejos; overlay cede si `videoSize` ya existe; pausar descargas solo tras `swapToVideo` + `disarmVideoModeKeepAudio` reanuda; `PendingDeferredDownloads.pendingIds` + tap cancela diferidas/pausadas; marquee infinito. Guardianes: `MusicService.swapToVideo` / `disarmVideoModeKeepAudio`, `AuraPlayer` first-frame + download icon, `PendingDeferredDownloads.pendingIds`. **No tocar** #43/#124 superficie, INSTANT_VIDEO_SWAP off, EQ/Superpowered | 0.6.199 | ⚠️ pendiente dispositivo |
| 149 | **Álbum / EP / single / playlist seguían el listado viejo (portada en cada pista, sin líneas Apple)** | 0.6.198 solo puso `AuraSongRow` vertical: misma fila YTM. Apple: álbum = número+título+duración+hairline (portada solo en el héroe); playlist = thumb 50 dp+título+artista+duración+hairline. `AuraDivider` al 9% era casi invisible | `AuraAppleListRowFrame` + `AuraAlbumTrackRow` (sin artwork) + duración; hairline `ArtworkEdge`; álbum en orden de disco (no liked-first). Guardianes: `AuraAlbumScreen` `AuraAlbumTrackRow`, `AuraLocalPlaylistScreen`/`AuraOnlinePlaylistScreen`/`AuraCollectionScreens` `AuraAppleListRowFrame` | 0.6.199 | ⚠️ pendiente dispositivo |
| 150 | **Vídeo se corta al inicio, portadas parpadean/barras grises en mini reproductor, chips de inicio sin login, scroll vertical atascado en estantes y playlists IA subiéndose a YTM** | (a) `DefaultLoadControl` arrancaba con 400-700ms de buffer en vídeo provocando stall inicial. (b) `key(id)` en `AuraMiniPlayer`/`MiniPlayer` destruía el bitmap provocando destello blanco al cambiar canción, y `ContentScale.Fit` dejaba barras laterales. (c) `YouTube.home()` llamaba a `browse` sin `setLogin = true`. (d) `confirmArtistUnsubscribed` borraba `unfollowedByUserAt` provocando que `followArtistsWithContent` re-siguiera al artista. (e) Playlists de IA se creaban con `isLocal = false` subiéndose a YTM. (f) `LazyRow`/`LazyHorizontalGrid` flinging bloqueaba el scroll vertical padre. | (a) `ChunkingDataSourceFactory` en `videoDataSourceFactory`. (b) `AuraCover` con `fillBleed = true`, `youtubeCoverOrFallback` y eliminación de `key(id)` en mini players. (c) `YouTube.home(setLogin = cookie != null)`. (d) `confirmArtistUnsubscribed` conserva `unfollowedByUserAt`. (e) `isLocal = true` en `AiPlaylistGenerator`. (f) `Modifier.passVerticalToParent` en `AuraMotion.kt`, `AuraDoubleRowShelf` y estantes. Guardianes: `MusicService.kt`, `AuraContent.kt`, `AuraMotion.kt`, `YouTube.kt`, `DatabaseDao.kt`, `AiPlaylistGenerator.kt` | 0.6.200 | ⚠️ pendiente dispositivo |
| 151 | **MusicService crash on startup: `IllegalStateException: getBackBufferDurationUs not implemented`** | En Media3 `LoadControl` interface, los métodos deprecados por defecto lanzan `IllegalStateException("getBackBufferDurationUs not implemented")`. Al usar Kotlin class delegation (`by delegate`) sobre `LoadControl`, el compilador genera stubs que invocan el default throwing method en runtime. | Usar `DefaultLoadControl` directamente en `setLoadControl()` sin wrappers que deleguen la interfaz `LoadControl`. Guardián: `MusicService.kt` `setLoadControl(DefaultLoadControl.Builder()...build())`. | 0.6.201 | ⚠️ pendiente dispositivo |
| 152 | **Volumen de pistas silenciosas era muy bajo (Loudness regression)** | Se había bajado `maxBoostDb` en `AudioGain.kt` de 12.0 a 3.0, provocando que las pistas silenciosas no se pudieran igualar a las ruidosas, lo cual invalidaba el propósito del DSP limitador de Superpowered. | Restaurado el límite a 12.0. Guardián: `AudioGain.kt` `maxBoostDb` | 0.6.210 | ⚠️ pendiente dispositivo |
| 153 | **Botones de acción del reproductor muy pequeños en alta resolución** | `PlayerActionChip` e `PlayerLikeDislikePill` usaban un tamaño de icono fijo de `20.dp` y paddings que daban un alto total de 38.dp, lo que resultaba en botones inusables en pantallas de alta densidad (2712 x 1220, Redmi Note 13 Pro+ 5G). | Incrementado el padding vertical a 12.dp y los iconos a 24.dp para alcanzar un área táctil recomendada de 48.dp, preservando el diseño YTM. Guardián: `Player.kt` `PlayerActionChip` | 0.6.210 | ⚠️ pendiente dispositivo |

## Invariantes permanentes (no romper nunca)
- No romper demo/licencia/suscripción (paquete `license/` + Worker). Crossfade 9s intacto. Superpowered intacto. Branding solo Aura. DB con migraciones (sin wipe). Verificar calor + batería en cada cambio.

